name: Build Android APK

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Make gradlew executable
        working-directory: android
        run: chmod +x ./gradlew

      - name: Print debug keystore SHA fingerprints
        working-directory: android
        run: |
          ./gradlew signingReport > /tmp/signing.txt 2>&1 || true
          echo "=== SIGNING REPORT ==="
          cat /tmp/signing.txt

      - name: Check for signing secrets
        id: signing
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ]; then
            echo "release=true" >> "$GITHUB_OUTPUT"
          else
            echo "release=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build debug APK
        if: steps.signing.outputs.release == 'false'
        working-directory: android
        run: ./gradlew assembleProdDebug --stacktrace --no-build-cache

      - name: Build signed release APK
        if: steps.signing.outputs.release == 'true'
        working-directory: android
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: ./gradlew assembleProdRelease --stacktrace --no-build-cache

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: codespace-ide-apk
          path: android/app/build/outputs/apk/**/*.apk
          if-no-files-found: error
