# Integration: fresh source-only group audit

**State:** app source read; no device test run. `READ A/path:N` is 1-based in `android/app/src/main/java/com/codespace/ide/`, `READ V/path:N` is 1-based in `vscode-src/` (repo root `src/vs/`). Per COVERAGE-CHECK.md this group is ~4.7k lines across four clusters: (1) the SSH stack (direct SSHJ engine + CLI-profile live path + manager sheet), (2) the connectors system (backend API client, Connectors Hub UI, PAT dialog, agent connector tools), (3) the agent persistence trio (memory, scheduler, entity store) plus their tool exposure, and (4) the build-support surfaces (ports scanner, toolchain manager, download center, artifact manager, workspace-context provider, plus one dead Retrofit interface). **Boundaries owned elsewhere:** AgentApiServer (Terminal TP02), AgentTools dispatch + FlowGate approval cards (Chat CH02/CH03/CH05), McpClientManager (Extensions XF), GitHubAuth (Settings/SCM), SessionStateStore/SessionHandoffManager (Recovery, next group), NotificationStore (Settings). Nothing here is a device outcome or a code-fix authorization.

## Feature inventory

| ID | Source-traced feature |
|---|---|
| IN01 | Direct SSH engine: SSHJ-based `SshManager` — connect/auth (publickey or password), `testConnection`, `runCommand`, interactive PTY `openShell` via callbackFlow with byte-buffer reads, finally-disconnect leak fix. READ A/ssh/SshManager.kt:167-204,212-264. |
| IN02 | TOFU host-key pinning: `SshFingerprintStore` (filesDir/ssh-known-hosts.json, host:port → SHA256 base64), `TofuVerifier` — unknown host auto-trusts + stores, known-same accepts, mismatch REJECTS with actionable warning text. READ A/ssh/SshManager.kt:27-56,120-146. |
| IN03 | SSH CLI profiles (the LIVE path): `SshProfile` data class with tunnel fields (-L/-R), `buildCommand()` shell-quotes every token, `StrictHostKeyChecking=accept-new`; `SshProfileStore` typed `save(): Result<Unit>`. READ A/terminal/SshProfile.kt:11-35; A/terminal/SshProfileStore.kt:12-40. |
| IN04 | SSH Manager sheet: profile CRUD list, delete confirmation, edit dialog (host/port/username/keyPath/tunnel), save-failure snackbar, load off main thread. READ A/ui/panes/SshManagerSheet.kt:40-70,155,173,191-198. |
| IN05 | Terminal SSH connect: overflow-menu entry opens the sheet; `onConnect(label, command)` feeds the built ssh command into the terminal. READ A/ui/panes/TerminalPane.kt:674,1294,1863-1865. |
| IN06 | Backend connectors client: `ConnectorsApiClient` — status fetch, OAuth auth-url mint, callback completion, PAT save, disconnect, proxy call; typed `Result` everywhere; bounded error bodies (take(300/2000)); ONE 401→refresh→retry with tokens persisted to SecureTokenStore. READ A/data/ConnectorsApiClient.kt:28,58-80,90,127,139,156,173,185. |
| IN07 | Connectors Hub sheet: 19-service grid (5 OAuth live, 6 OAuth phase-2 rows, 7 PAT), busy/toast states, refresh, in-app WebView OAuth dialog with captured-callback completion. READ A/ui/screens/ConnectorsHubSheet.kt:45-68,232,351-381. |
| IN08 | PAT connector dialog: paste-token flow for PAT-type services; token sent once to backend, encrypted server-side, never stored on device. READ A/ui/screens/ConnectorPatDialog.kt:32-60. |
| IN09 | Agent connector tools: `AgentConnectorManager` — list (status lines), connect (external browser via ACTION_VIEW, PAT services steered to the hub), request_connector inline chat card side-channel, use (proxyCall, reply capped 6000 chars); GitHub deliberately excluded (separate device-flow system). READ A/agent/AgentConnectorManager.kt:36,43,74-77,83,114,149,160. |
| IN10 | Agent memory: `AgentMemory` — KV JSON in filesDir/agent_memory/memory.json, save/readAll (200-char display cap)/delete/get; tools save_memory/read_memory/delete_memory. READ A/agent/AgentMemory.kt:14-59; A/agent/AgentTools.kt:190-192. |
| IN11 | Agent scheduler: `AgentScheduler` — cron subset (@once, */N minutes, daily H:00; anything else silently reinterpreted as every-60-seconds), ScheduledExecutorService, tasks.json persistence, cancel, `restoreAll()` re-schedule API. READ A/agent/AgentScheduler.kt:16-17,42-97,99-116,117-125. |
| IN12 | Scheduled task execution: `runCommand` → ProotInstaller.execOnce (60s timeout), result discarded, exceptions swallowed; NO FlowGate involvement. READ A/agent/AgentScheduler.kt:130-135. |
| IN13 | Agent entity store: `AgentEntityManager` — one dir per entity under filesDir/agent_data/, one JSON file per record, create (id=currentTimeMillis)/read (filter match)/update/delete loops with per-record exception swallow; tools create/read/update/delete_entity. READ A/agent/AgentEntityManager.kt:15-30,32-55,57-78,80-98; A/agent/AgentTools.kt:197-200. |
| IN14 | Ports scanner: parallel TCP-connect probes of well-known dev-server ports (+custom), 200ms timeout, IO dispatcher, stateless object. READ A/diagnostics/PortsScanner.kt:14-49. |
| IN15 | Ports panel wiring: scanning state, custom port add/remove, rescan on customPorts change + manual. READ A/ui/screens/ProjectShellScreen.kt:4138-4147. |
| IN16 | Toolchain manager: scan of 11 tools (JDK, Gradle, Android SDK, build-tools, platform-tools, Flutter, Dart, Node, npm, Python, pip) via execOnce; ToolStatus with health/version/path/note/installCmd; Android-critical set drives overallHealthy. READ A/project/ToolchainManager.kt:34-83,325-326. |
| IN17 | Toolchain panel: auto-scan on open, Refresh, per-tool rows, install button → runs the suggested command in the terminal (visible, user-executed). READ A/ui/panels/ToolchainPanel.kt:37,43-49,82,211-214; A/ui/screens/ProjectShellScreen.kt:3632-3641. |
| IN18 | Download Center engine: `DownloadCenter` — StateFlow entry list, live progress (contentLength + byte count), typed Boolean result, failed download deletes partial file; cancel/retry/dismiss/clearFinished list ops. READ A/project/DownloadCenter.kt:25-42,56-99,101-104,121-134. |
| IN19 | Download Center panel: bottom tab rendering the StateFlow, active-count header, empty state ("Downloads initiated by the IDE appear here"), state-icon rows — fully read-only (no retry/cancel buttons). READ A/ui/panels/DownloadCenterPanel.kt:33-86,100-108. |
| IN20 | Build artifacts + workspace context: `BuildArtifactManager` (APK/AAB scan of gradle output dirs, typed delete Boolean, FileProvider share/install intents) + `WorkspaceContextProvider` (bounded tree: depth 3, 80 files, 4000 chars, ignore list; feeds chat system prompt). READ A/project/BuildArtifactManager.kt:56-99,103-141; A/ai/WorkspaceContextProvider.kt:19-25,43-117. |

## Cross-group connection ledger

| Edge | Producer -> consumer and seam |
|---|---|
| C-IN01 | Terminal TP03: ToolchainManager.exec, AgentScheduler.runCommand, and every future download-side shell call sit on the execOnce String seam — S01's upstream. READ A/project/ToolchainManager.kt:325-326; A/agent/AgentScheduler.kt:130-135. |
| C-IN02 | Chat CH02/CH05: all six integration tools are exposed through AgentTools.executeTool — entity path traversal (IG01) and the scheduler gate bypass (IG02) are escapes at the SAME agent-tool boundary CH02 maps; the CH05 loopback-token fix must also gate these tools. READ A/agent/AgentTools.kt:190-203. |
| C-IN03 | Terminal TP02: AgentApiServer 8765 /tool/{name} reaches these tools with no app consent — combined with get_secret (dumps SecureTokenStore incl. the backend JWT) a guest process can drive connector calls; IG15's no-per-call-consent compounds here. |
| C-IN04 | Settings SK01: every integration JSON store (profiles, known-hosts, tasks, memory, entities) is the same non-atomic writeText pattern — one shared atomic-write utility, settings-first per the owner ruling. READ A/terminal/SshProfileStore.kt:30-34; A/agent/AgentMemory.kt:26-28. |
| C-IN05 | SCM SG04: SshProfile.buildCommand is the POSITIVE counter-example GitService.clone needs — user strings interpolated into a command but shell-quoted token-wise. READ A/terminal/SshProfile.kt:22-35. |
| C-IN06 | Testing TG01/PR07 placebo family: the Download Center panel + cancel/retry semantics (IG04/IG05) join the family — surfaces and controls that do nothing while appearing to work. READ A/project/DownloadCenter.kt:101-104,121-134. |
| C-IN07 | Problems PR03: scheduler task output is discarded entirely — no Problems surface can ever see a scheduled-task failure; toolchain detection runs under the same execOnce output handling. |
| C-IN08 | Chat CH03: FlowGate approval cards cover run_command only; schedule_task/use_connector/create_entity have NO approval analog — IG02/IG15 are the ungated rows of the approval matrix, and the CH03 display fix design must extend the card rather than add isolated gates. |
| C-IN09 | Recovery (next group): SessionStateStore's app-start restore is the pattern AgentScheduler.restoreAll SHOULD follow — both read persisted JSON at startup; the scheduler's is simply never wired. One line at the Application/App startup. READ A/agent/AgentScheduler.kt:117-125. |
| C-IN10 | Chat/LSP: WorkspaceContextProvider feeds the chat system prompt, and SkillsCatalog.buildContextAttachment duplicates the concept — TWO context builders; any future chatContextService-style gating owns both. READ A/ui/screens/CopilotChatPanelOverlay.kt:490,517; A/chat/SkillsCatalog.kt:137. |

## VS Code clone comparison

| Area | Verified VS Code owner | Android contrast |
|---|---|---|
| Silent session re-auth | READ V/src/vs/workbench/services/authentication/browser/authenticationService.ts:277-302 — `getSessions` with challenge-based silent refresh via `getSessionsFromChallenges`; sessions stay valid invisibly. | READ A/data/ConnectorsApiClient.kt:58-80 — client-side ONE retry after explicit /auth/refresh, typed failures surfaced to the sheet; refresh failure falls through to a stale-token replay (honest failure, narrower recovery). |
| Token custody | READ V/src/vs/workbench/services/authentication/browser/authenticationExtensionsService.ts:37 — providers hold secrets in the extension host; consumers receive session handles, never raw provider tokens. | Same shape done right: PAT sent once to the backend (A/agent/…/ConnectorPatDialog + A/data/ConnectorsApiClient.kt:156) and proxyCall keeps tokens server-side (185) — strength; the divergence is no per-call scope/consent model (IG15). |
| Chat context | READ V/src/vs/workbench/contrib/chat/browser/contextContrib/chatContextService.ts:43 — registered context contributors with per-request canSendContext gating. | READ A/ai/WorkspaceContextProvider.kt:43-117 — one blanket 4000-char injection into every system prompt; no gating, no registration, plus a second builder (SkillsCatalog) with no shared seam. |
| Ports / tunnels | READ V/src/vs/workbench/services/remote/common/remoteExplorerService.ts:23-24 (TUNNEL_VIEW_ID) + tunnelModel.ts — CandidatePort discovery, forwarding, privacy options, open-in-browser actions. | READ A/diagnostics/PortsScanner.kt:25-49 — probe-only, static well-known list, no forwarding, no candidate discovery (IG14); honest about being detection, not tunneling. |
| Downloads | READ V/src/vs/platform/download/common/downloadService.ts:13,22 — `download(resource, target, callSite, cancellationToken)`: cancellation is a first-class token threaded through the transfer. | READ A/project/DownloadCenter.kt:101-104 — `cancel(id)` flips a state field; the transfer loop never checks it and COMPLETION OVERWRITES CANCELLED; the whole engine has zero callers (IG04/IG05). |
| Task scheduling/running | READ V/src/vs/workbench/contrib/tasks/browser/taskService.ts:14 + abstractTaskService.ts:202 — managed run: presentation config, problem matchers, output panels, resolution events. | READ A/agent/AgentScheduler.kt:130-135 — raw execOnce, result DISCARDED, exceptions swallowed, no output surface of any kind for a fired task. |
| Auth consent UX | READ V/src/vs/workbench/services/authentication/browser/authenticationService.ts:277-302 — every session request declares scopes; the user consents per scope-set. | READ A/agent/AgentConnectorManager.kt:160-176 — use_connector takes arbitrary method/endpoint on any connected service with no consent surface at all; the CH03 approval card exists only for shell commands. |
| Managed tunnel/SSH abstraction | READ V/src/vs/workbench/services/remote/common/remoteExplorerService.ts:11 (ITunnelService import) — one tunnel service contract both UI and extensions sit on. | TWO divergent SSH stacks: a hardened direct SSHJ engine with TOFU (IN01/IN02) that NOTHING calls, and a live ssh-CLI path with no fingerprint UI — no shared abstraction, the trust-critical code is the orphaned one (IG06). |

## Device checks (all pending)

| Check | What to verify on device |
|---|---|
| IT01 | Connectors Hub opens from In-Project Settings + chat overflow: 19 service rows render with correct names. |
| IT02 | Signed-out: hub shows the sign-in-required state, not a crash or silent blank (requireAccessToken message path). |
| IT03 | Airplane mode: hub shows the typed loadError row with the real HTTP/transport message. |
| IT04 | **IG07 probe:** connect gmail from the HUB → WebView dialog → record whether Google serves "disallowed_useragent" (blocked) vs consent page. |
| IT05 | Chat: `list_connectors` returns status lines for all services (CONNECTED/available/not-set-up) and the GitHub pointer. |
| IT06 | Finish hub OAuth consent → refresh → row flips to CONNECTED (completeOAuthCallback path). |
| IT07 | Same service via chat tool (connect_service) → EXTERNAL browser tab opens (contrast with IT04's in-app WebView). |
| IT08 | PAT service (e.g. Sentry): tap → PAT dialog, paste token, save → CONNECTED; token never in any local store (inspect filesDir). |
| IT09 | PAT save with an invalid token / offline → backend error text shown in the dialog (typed failure surfaces). |
| IT10 | Disconnect → row returns to available; a subsequent list_connectors reflects it. |
| IT11 | 401 refresh: open hub with an expired access token → one silent refresh + rows load, no red UNAUTHORIZED state. |
| IT12 | request_connector card: agent suggests a service → inline "Connect X" card appears in chat, opens the hub. |
| IT13 | use_connector live call → bounded reply (≤6000 chars), no raw token anywhere in the reply. |
| IT14 | SSH Manager sheet from terminal overflow: profile list or correct empty state. |
| IT15 | Create profile with port/tunnel fields → persists across app restart (SshProfileStore typed save). |
| IT16 | Edit + delete profile round-trip: delete confirmation works, list updates, no phantom rows on reload. |
| IT17 | Connect profile → terminal receives the full `ssh -o StrictHostKeyChecking=accept-new …` command with quoting intact. |
| IT18 | **IG06 probe:** after real SSH usage, filesDir/ssh-known-hosts.json does NOT exist — proves the TOFU stack never runs (vs CLI accept-new which never surfaces fingerprints in-app). |
| IT19 | Ports panel idle: empty list, scanning indicator brief, no stuck spinner. |
| IT20 | Proot terminal: `python3 -m http.server 8080` → ports rescan shows 8080. |
| IT21 | Custom port add via addPortText → probed on rescan; removal works. |
| IT22 | Well-known labels: 3000/5173/8080 show friendly names, custom shows "Custom". |
| IT23 | Toolchain panel auto-scan on open: all 11 tool rows with health/version/path, Android-critical summary correct. |
| IT24 | **IG08 probe:** break a tool (rename java binary) → rescan shows MISSING vs UNKNOWN distinction (exec failure vs genuinely absent). |
| IT25 | Missing-tool install button → terminal opens with installCmd visible; after running it, rescan flips to OK. |
| IT26 | Toolchain Refresh spam-tapped mid-scan → scanning state never sticks true. |
| IT27 | **IG04 probe:** trigger a real IDE download (rootfs/LSP) while watching the Downloads tab → stays empty ("downloads appear here" is never true). |
| IT28 | Downloads "clear finished" action on the (empty) list: no crash. |
| IT29 | Build an APK → Artifacts tab lists it with size/date; AAB from bundle path too. |
| IT30 | Artifact share → Android share sheet with the file; install → package installer opens the APK. |
| IT31 | Artifact delete → file gone from disk (recheck via terminal) + row removed (typed delete Boolean). |
| IT32 | Chat memory: save → restart app → read_memory returns it (memory.json persistence). |
| IT33 | Scheduler: `@once` echo task → fires ~5s later (observe effect); list_tasks/cancel round-trip. |
| IT34 | **IG03 probe:** schedule a */5 task, restart the app, wait past the mark → does NOT re-fire (restoreAll unwired). |
| IT35 | **IG01 probe:** create_entity with entity name `../probe` → read_entities `../probe` (or terminal ls of filesDir/agent_data parent) — proves records land OUTSIDE the store dir. |
| IT36 | Workspace context: open a project + file, ask the agent "what files do you see" → tree/current-file reflects WorkspaceContextProvider output (and stays ≤4000 chars). |

## Gaps

| Gap | Priority | Source-confirmed behavior / risk | Evidence and check |
|---|---|---|---|
| **IG01** | **HIGH in group, security (EX05 path-escape family), below the EX04/EX05/SG04 tier** | `AgentEntityManager.entityDir` builds `File(filesDir, "agent_data/$entity")` from the AGENT-CONTROLLED entity name with zero sanitization — `../` walks out of the store; create() then writes JSON records into any app-reachable directory, and update()/delete() parse + rewrite/delete ANY matching JSON files there. A prompt-injected model (or a model "helpfully" passing a path-like entity) reads, rewrites, or deletes JSON outside the store (project settings.json, .vscode configs). Not an unconditional escape (needs adversarial model input), hence one tier below the EX top tier — same classification reasoning as CH03. Fix class: whitelist entity names `[A-Za-z0-9_-]+`, canonicalize and verify parent. | `agent/AgentEntityManager.kt:15-19,21-30,57-98`; IT35. |
| **IG02** | **HIGH in group, security (agent-gate bypass — CH02/CH05 family)** | `schedule_task` executes arbitrary shell with NO FlowGate involvement: AgentScheduler.runCommand calls execOnce directly, unattended, from the executor thread, and persists in tasks.json. In AUTO flow a plain run_command is gated; scheduling is a lateral escape around the same gate — the agent can queue a destructive command that fires later with no approval, no pending card, no log. Result is discarded and exceptions swallowed (`catch (_: Exception) {}`), so a fired task is unverifiable (S01). Fix: approve the command at schedule time (FlowGate card) + persist the approved form + typed run record. | `agent/AgentScheduler.kt:56-86,130-135`; IT33. |
| **IG03** | HIGH in group, S01 false-persistence family | `restoreAll()` — the entire persistence promise of the scheduler ("Tasks persist as JSON and are re-scheduled on app restart", KDoc) — has ZERO callers: tasks.json is written but nothing re-schedules at app start. schedule() returns "Task scheduled (cron: …)" while the persistence the message implies does not exist beyond process lifetime. One-line fix at Application startup (C-IN09 pattern). | `agent/AgentScheduler.kt:117-125`; IT34. |
| **IG04** | HIGH in group, placebo-surface family (TG01/PR07) | The Download Center is orphaned end-to-end: `DownloadCenter.download()` has ZERO callers — no downloader in the app routes through it (ProotInstaller/LSP/rootfs run their own progress paths) — while the panel promises "Downloads initiated by the IDE appear here" and ProjectShellScreen only calls clearFinished(). A whole bottom tab that can never be non-empty. Fix: route the real downloaders through the typed engine (it is the correct shape), or remove the tab. | `project/DownloadCenter.kt:56`; `ui/panels/DownloadCenterPanel.kt:69-77`; `ui/screens/ProjectShellScreen.kt:5167`; IT27. |
| **IG05** | HIGH in group, placebo controls (family) | cancel() and retry() are semantically broken even as an API: cancel flips the entry to CANCELLED but the transfer loop never checks it and COMPLETION OVERWRITES CANCELLED→COMPLETED; retry flips to QUEUED with no engine watching — nothing ever restarts. Two controls that change UI state while doing nothing. | `project/DownloadCenter.kt:88-99,101-104,121-134`; IT27. |
| **IG06** | MEDIUM-HIGH in group | TWO divergent SSH stacks: the hardened direct SSHJ engine with TOFU fingerprint pinning (mismatch reject, actionable warning) is ORPHANED — zero SshCredentials callers — while the live path is the ssh CLI with `accept-new` and no in-app fingerprint surface at all. The trust-critical code is the dead code; 264 lines of untested security surface drifting. Wire the sheet/terminal to SshManager or delete the stack; SshFingerprintStore.save also swallows write failures (best-effort). | `ssh/SshManager.kt:27-56,120-146`; `terminal/SshProfile.kt:28`; IT18. |
| **IG07** | MEDIUM-HIGH in group, self-contradicting transports | The Hub runs OAuth in an in-app WebView ("avoids returning to external browser flow") while AgentConnectorManager's own documentation says Google/Slack BLOCK embedded WebViews for OAuth ("disallowed_useragent") and deliberately opens the external browser from chat — the SAME flow, two transports, and the Hub picked the documented-blocked one. Google sign-in from the Hub likely hard-fails with a browser error inside the dialog. | `ui/screens/ConnectorsHubSheet.kt:57-62,232,351-353`; `agent/AgentConnectorManager.kt:97-106`; IT04/IT07. |
| **IG08** | MEDIUM in group, S01 family | ToolchainManager.exec catches ALL exceptions to `""` — a hung/crashed proot, a permission failure, and a genuinely missing binary all render identically as MISSING; ToolHealth.UNKNOWN exists but is unreachable through this path. Detection lies about its confidence. | `project/ToolchainManager.kt:325-326`; IT24. |
| **IG09** | MEDIUM in group, dead-surface drift risk | The Retrofit `ApiService` interface (login/refresh/listRepos/createPr) + its AppModule provider have ZERO callers — auth is Firebase + GitHub device flow, the backend client is ConnectorsApiClient. A dead 4-endpoint auth surface that someone will eventually "fix" or trust. Delete or wire. | `data/ApiService.kt:16-33`; `di/AppModule.kt:116`. |
| **IG10** | MEDIUM in group, SK01 corruption family + unbounded growth | AgentMemory: non-atomic writeText on memory.json (one truncated write → readMap catch → EMPTY map returned silently — total memory loss with no error to the agent or user) and NO size cap: one huge save bloats every future read (readAll loads the whole file; the 200-char cap is display-only). | `agent/AgentMemory.kt:20-28,30-46`; IT32. |
| **IG11** | MEDIUM in group, S01 family | AgentEntityManager: record id = System.currentTimeMillis() — two creates in the same millisecond silently overwrite one record; per-record exceptions in read/update/delete loops are swallowed, so corrupt files are skipped and counts under-report with no signal. | `agent/AgentEntityManager.kt:23-30,51,66-72,89-94`. |
| **IG12** | MEDIUM in group | Backend URL hardcoded in TWO places: `API_BASE` (ConnectorsApiClient:28) and the literal `backendUrl` at ProjectShellScreen:3662 — the Railway→Render migration already proved this drifts silently (old URL dead on device). Single BuildConfig/source-of-truth needed. | `data/ConnectorsApiClient.kt:28`; `ui/screens/ProjectShellScreen.kt:3662`. |
| **IG13** | MEDIUM-LOW in group, S01 silent-misinterpretation | The cron parser is a substring guesser: anything not matching @once / `*/N * * * *` / `0 H * * *` falls to the else-branch and runs EVERY 60 SECONDS — a "daily backup" typo becomes a once-a-minute loop, while schedule() echoes the ORIGINAL cron back as confirmation. A false success report about cadence. | `agent/AgentScheduler.kt:91-97`; IT33. |
| **IG14** | LOW-MEDIUM in group, additive feature | PortsScanner is probe-only over a static well-known list: no candidate discovery, no forwarding (VS Code's tunnel model), rescan only on customPorts change or manual action — a dev server started after the last rescan is invisible until toggled. Documented as detection-not-tunneling, so this is an additive gap, not a defect of what exists. | `diagnostics/PortsScanner.kt:25-42`; IT19-IT22. |
| **IG15** | MEDIUM-HIGH in group, security-adjacent (agent boundary) | use_connector executes arbitrary method/endpoint against ANY connected service with no per-call consent surface — the CH03 approval card exists only for shell. A prompt-injected agent can call every connected SaaS API (read mail, delete resources) silently; blast radius is partially bounded by the backend proxy and service allowlist, but no scope declaration or user gate exists anywhere in the path. Pairs with IG02 as the ungated rows of the approval matrix (C-IN08); any CH03 card redesign must include connector calls. | `agent/AgentConnectorManager.kt:160-176`; IT13. |
| **IG16** | LOW-MEDIUM in group, SK01 batch item | Every integration JSON store (ssh-profiles, ssh-known-hosts, tasks.json, memory.json, entity records) is the same non-atomic `writeText` pattern — batch them all into the one shared atomic-write (tmp+rename) utility that Settings SK01 should produce, rather than five isolated patches. | `terminal/SshProfileStore.kt:30-34`; `agent/AgentMemory.kt:26-28`; `agent/AgentScheduler.kt:33-35`. |

**Strengths (recorded for the fix plan):** ConnectorsApiClient is the best S01 citizen in the codebase — typed Result on every call, bounded error bodies, one-shot 401 refresh-retry with persisted token pair; token custody matches VS Code's shape (PAT sent once, tokens never on device); TofuVerifier's mismatch path rejects AND tells the user the exact remediation; SshProfile.buildCommand shell-quotes every token (the SG04 counter-example); PortsScanner does real probes with parallelism and an honest scope comment; BuildArtifactManager's delete is typed and only removes state on success; ToolchainPanel's install button echoes a visible command into the terminal (user-executed, honest); WorkspaceContextProvider is properly bounded (depth 3 / 80 files / 4000 chars / ignore list).

**Cross-links:** IG01 → EX05 path-escape family + CH02 boundary (one fix design). IG02 → CH03/CH05 approval-matrix extension. IG03 → S01 false-persistence (SessionStateStore restore pattern, C-IN09). IG04/IG05 → TG01/PR07 placebo family. IG08/IG11/IG13 → S01 silent-failure/misreport family (typed execOnce result, TP03). IG10/IG16 → SK01/SK02 atomic-write + typed-store pass. IG12 → CH11/SG03 hardcoded-endpoint drift precedent. IG07 → real browser flow (chat path) is the working reference implementation. IG06 → decide wire-or-delete before Recovery's session-restore work touches startup ordering.
