# Chat / AI / settings: fresh source-only group audit

**State:** app and checked-out VS Code clone read; no device test run. `READ A/path:N` is 1-based in `android/app/src/main/java/com/codespace/ide/`, and `READ V/path:N` is 1-based in `vscode-src/` (repo root: `src/vs/` and `extensions/`). This group owns the AI chat stack end to end: the provider layer (`chat/ChatProvider.kt` registry + `chat/providers/` transports), key and endpoint management (`ChatKeyPool`, `ChatKeyFailover`, `CustomEndpointStore`, `AiKeysSection`), the chat panel (`CopilotChatPanelOverlay.kt` inline variant, sessions, attachments, slash commands, skills, retry/export), the agentic tool loop and permissions (`AgentTools`, `AgentFlowGate`, `ChatPermissionStore`), R6 staged edits (`PendingChangesStore`), plans and custom modes (`ChatPlanStore`, `CustomModeStore`), MCP (`agent/McpClientManager`, `McpPanel`/`McpServersSection`), and the settings surfaces (`SettingsScreen`, container management, clear-data, trash UI). The AI side of Problems/SCM attach (CW2 rows), the Terminal exec bridge, Editor buffer refresh, and VersionHistory mechanics are boundaries referenced, not re-audited. Nothing here is a device outcome or a code-fix authorization.

## Feature inventory

| ID | Source-traced feature |
|---|---|
| CF01 | Provider abstraction: one `ChatProvider` interface (id/displayName/defaultModel/isLocal/requiresApiKey/isAvailable/unavailableMessage + `complete`) with additive defaults for `completeStreaming`, `fetchModelInfos`, `countTokens`, `supportsImages`, `supportsAudio`, `defaultModelIsPlaceholder`; cloud and local-server providers register through `ChatProviderRegistry` with zero panel code changes (new provider = one file + one bootstrap line). READ A/chat/ChatProvider.kt:20-110; A/chat/ChatProviderRegistry.kt:12-33; A/chat/ProviderBootstrap.kt:19-52. |
| CF02 | Request model: `ChatRequest` (model, systemPrompt, convMsgs, apiKey, images, audios) with vendor multimodal parts converted inside providers; `ChatHttpException` carries statusCode + 429 retryAfterMs + `isWafBlock` (Cloudflare-style firewall block is classified distinctly from vendor auth rejection so failover never cools keys for it). READ A/chat/ChatProvider.kt:145-177. |
| CF03 | Multi-key pool: unlimited keys per provider in SecureTokenStore (`ai_<ID>`, `ai_<ID>_2`, ...), order in plain prefs, manual ACTIVE pick sorted first; `ChatKeyFailover.execute` walks keys with 401/403 cooldown-vs-429 same-key backoff, `hasStarted` guard prevents re-sending a stream that already produced output, info lines to Output channel. READ A/chat/ChatKeyPool.kt:24-80; A/ui/screens/CopilotChatPanelOverlay.kt:577-595; A/ui/screens/AiKeysSection.kt:512-537. |
| CF04 | Custom endpoints v2 (MK restructure, shipped): CRUD endpoint registry {id,label,baseUrl} persisted in prefs; each endpoint is its own provider instance (`custom` legacy, `custom_<slug>` new, no colons); per-endpoint keys via ChatKeyPool addressing; per-endpoint model registry (manual IDs + cached live /models with fetch timestamp); one-time legacy v1 migration (single-slot). READ A/chat/CustomEndpointStore.kt:5-49,60-120. |
| CF05 | AiKeysSection: per-provider key editors with live validation (try-every-key via failover, REAL vendor error text via thrown fetch failures), detected-foreign-key routing (a Gemini key pasted into the OpenAI slot auto-saves to GEMINI + live-checks the right provider), per-slot status lines, add/label/delete, manual model-ID editor for custom endpoints, endpoint URL editor. READ A/ui/screens/AiKeysSection.kt:446-537. |
| CF06 | Model selection: "auto" default resolves at send time to the active provider's default; per-provider default models; picker groups built from the registry (`buildCustomMenuGroups`); context gauge estimates the NEXT send's usage via provider `countTokens` (jtokkit/Gemini countTokens/Anthropic count_tokens) with heuristic fallback, unawaited so it never delays the reply. READ A/ui/screens/CopilotChatPanelOverlay.kt:251-329,699-706,1189-1196. |
| CF07 | Sessions: multiple named threads (id, title, mode, customModeId, updatedAt), sidebar with pin/auto-width, search + mode filter, rename (menu or /rename), delete (always keeps one), one-time migration folding the old flat history into a session. READ A/ui/screens/CopilotChatPanelOverlay.kt:159-233,943-1080. |
| CF08 | Session persistence: whole session list as one JSON blob in prefs, 50-message cap per session, role/text/rating only; `persistSessions()` on switch/new/send/rating and command handling; load sorts by updatedAt, tolerates missing legacy keys. READ A/ui/screens/CopilotChatPanelOverlay.kt:172-233,976-983. |
| CF09 | Send pipeline: implicit workspace context (toggleable R3-ATTACH), explicit attachments (chips + `#file` hash tokens, deduped by path), images/audio ride ONLY iteration 0 as structured vendor parts with capability refusal for unsupported providers, provider availability check, 10-iteration agentic loop, streaming deltas to live bubble with scroll-follow, send-while-loading queues one message. READ A/ui/screens/CopilotChatPanelOverlay.kt:497-616,1090-1130. |
| CF10 | Tool gating: `AgentFlowGate` levels (AUTO_ALL / AUTO_SAFE read-only pass / MANUAL pause-each) with per-tool "Always allow" persisted via ChatPermissionStore; approval surfaced as a floating card; custom-mode allowlist is RESTRICT-ONLY and never widens a gate; staged writes skip the gate (R6 decision #1). READ A/agent/AgentFlowGate.kt:28-46; A/ui/screens/CopilotChatPanelOverlay.kt:617-660; A/ui/screens/ChatApprovalCard.kt:40-89. |
| CF11 | R6 staged edits: `write_file` in AGENT mode stages into PendingChangesStore (never disk); read-through overlay in readFile/searchFiles so the model iterates on its own staged version; user-initiated Apply with fail-closed drift verification (disk-staged entries only), conflict card variants (Applied/Drift/Blocked/NotFound/Failed), rediff after drift, owner forceApply, checkpoints as `_prechat.bak` in the v2 dir with grouped retention, one undo snapshot gate per apply consumed by CodeEditor. READ A/chat/PendingChangesStore.kt:14-62,86-119,146-215,283-316; A/agent/AgentTools.kt:243-256,268-296. |
| CF12 | Apply/undo recovery: applyAll stops at first failure; `undoLastApply` restores the most recent apply batch from checkpoints and bumps appliedTick so open editors refresh; TimelinePanel restores call `bumpExternalRestore` through the same tick; `lastApplied` feeds the panel's undo affordance. READ A/chat/PendingChangesStore.kt:166-215,283-305. |
| CF13 | Plans (R7): `plan` tool stages a plan (ungated, cannot reach disk), ends the turn; durable halt via injected system-prompt rule until Approve/Reject/Revise; strict halt skips the rest of the tool round after a plan stages. READ A/ui/screens/CopilotChatPanelOverlay.kt:530-546,622-634,660-663; A/agent/AgentTools.kt:171-176. |
| CF14 | Slash commands + input history + skills: /clear /new /rename /models /skills /tools /help intercepted before the model; every input lands in the history walker; skills (file + MCP prompt sources) PREFILL and auto-attach, never auto-send; MCP prompt fetch pulls server-side content with the current input as its single optional argument. READ A/ui/screens/CopilotChatPanelOverlay.kt:1040-1076; A/chat/ChatSlashCommands.kt:1-72; A/chat/ChatInputHistory.kt:1-45. |
| CF15 | Retry + export (CW8): retry drops everything after the last user message and re-sends through the normal path; export writes the session to `<projectRoot>/.codespace/exports/chat-<timestamp>.md` with role headers, returns the relative path for a toast. READ A/ui/screens/CopilotChatPanelOverlay.kt:1199-1213,1762-1795; A/chat/ChatRetryExport.kt:66-120. |
| CF16 | Stop + quota surfacing: Stop cancels the coroutine (streaming reads cooperate) and keeps the streamed partial as a `[stopped]` message; 429/402 exceptions ALSO mirror into NotificationStore (AI source, warning severity, dedup by status) with a retry-after-seconds tip — not just the red bubble. READ A/ui/screens/CopilotChatPanelOverlay.kt:1078-1089,1155-1180. |
| CF17 | AgentTools: 30+ registered tools (run_command, files, git suite, save/get_secret, detect_secrets, web_fetch/search, memory, connectors, entities, scheduler, upload_file, install_package, plan, MCP bridge), all inside one try/catch that returns "Error executing <tool>" as the tool result; git tools route through proot with repoDir checks. READ A/agent/AgentTools.kt:152-160,168-215,316-341. |
| CF18 | run_command: executes inside the Ubuntu proot rootfs via execOnce (git/npm/apt only exist there); workdir translated host→guest when the model passes an Android host path (logged as C4), guest-style paths pass through; result recorded to TerminalAiBridge and truncated to 4000 chars. READ A/agent/AgentTools.kt:221-242. |
| CF19 | MCP manager: server config CRUD in `mcp_servers.json`, per-server enable, per-tool disable, env secrets resolved from SecureTokenStore at spawn; missing Node/Python runtimes DETECTED with guided install through proot; sessions cached and reused while alive; 60s call timeout; lazy first-chat discovery, manual refresh re-handshakes; prompts/list best-effort cache. READ A/agent/McpClientManager.kt:81-130,205-235,240-330. |
| CF20 | SettingsScreen: dark mode, App lock (SecureTokenStore), GitHub device-flow sign-in/out with username display, Ubuntu container backup/restore/reinstall with destructive-action confirms, "Restore previous session on project open" toggle, Clear Workspace Memory, formatter selection per language, per-item Clear Data dialogs, and the trash browser UI. READ A/ui/screens/SettingsScreen.kt:132-199,224-360,363-460,513-700. |

## Cross-group connection ledger

| Edge | Producer -> consumer and seam |
|---|---|
| C-CH01 | Editor group: applies bump `appliedTick` and gate ONE undo snapshot per path (`consumeUndoGate` consumed in CodeEditor's content effect — the same effect body where the #2734/#2753 declaration-order pitfalls live); open editors refresh through externalContentSync, the same cursor-mapped path G02 says can miss files. READ A/chat/PendingChangesStore.kt:283-316. |
| C-CH02 | SCM group: chat checkpoints are `_prechat.bak` entries in the SAME v2 dirs the 20s loop writes — grouped retention (SC16) is what protects them from eviction; conversely SG02's restore surfaces discard this store's overlay before copying back, and CH01's undo swallows the same class of copy failure SG02 does. Restore paths must be fixed together. READ A/chat/PendingChangesStore.kt:249-271; A/ui/panes/TimelinePanel.kt:256-264. |
| C-CH03 | Debugger/Proot group: DG02-class identity split — ScmState converts host→guest at ONE choke point (the good pattern), run_command translates workdir at ONE choke point, but the agent FILE tools do no translation at all (CH02); the DG02 ruling's "translation exists but is applied on one path and omitted on another" is exactly the shape here. READ A/agent/AgentTools.kt:221-306; A/scm/ScmState.kt:28-34. |
| C-CH04 | Notifications group: 429/402 quota errors mirror into NotificationStore from the chat catch — the same store SG04's token-bearing timeout text lands in; chat attachments (CW2 rows) are the exfil channel that promotion reasoning relies on, so SG04's fix must also scrub AI-source notification text. READ A/ui/screens/CopilotChatPanelOverlay.kt:1155-1180; A/ui/panes/SourceControlPane.kt:571-600. |
| C-CH05 | Problems group: CW2 attach rows feed build problems and SCM history into chat as context attachments; PR01/PR03's false build status can therefore be TOLD to the model as truth — the chat pipeline amplifies the S01 false-success family into AI context. READ A/chat/ChatAttachment.kt:1-120. |
| C-CH06 | Search/Explorer group: `searchFiles` honors the staged overlay (R6 read-through) and caps at 500 files/100KB; SR03/SR04 Replace All writes disk directly with no staged path — an agent that replaces via the UI tool then reads via read_file sees disk truth while its own write_file edits sit staged. READ A/agent/AgentTools.kt:268-296. |
| C-CH07 | LSP group: none direct, but the workspace context provider and `#file` attachments resolve paths the same way LS06/PR14's basename fallbacks break — PLAN A canonical keying would also stabilize attachment path resolution. READ A/ui/screens/CopilotChatPanelOverlay.kt:502-516. |
| C-CH08 | Terminal group: run_command spawns a fresh proot per call through the same execOnce as SCM (SG07 cost pattern); TerminalAiBridge feeds results back as terminal context; MCP sessions are long-lived processes OUTSIDE the PR13/DG13 per-call spawn hygiene pattern (deliberately, for stdio). READ A/agent/AgentTools.kt:221-242; A/agent/McpClientManager.kt:240-258. |
| C-CH09 | Tabs group: staged edits survive tab switches (store is path-keyed, TB08/G03's canonicalization is the fix family for any path-form drift between the model's paths and tab paths — the CH02 translation gap feeds the same mismatch). READ A/chat/PendingChangesStore.kt:86-119. |
| C-CH10 | Settings/container group: container Restore/Reinstall in Settings and the SCM git rootfs share one rootfs — a chat-installed package (install_package) or runtime (MCP) can be destroyed by a Settings restore the chat layer is never warned about. READ A/ui/screens/SettingsScreen.kt:363-460; A/agent/McpClientManager.kt:227-235. |

## VS Code clone comparison

| Area | Verified VS Code owner | Android contrast |
|---|---|---|
| Durable chat sessions | READ V/src/vs/workbench/contrib/chat/common/chatSessionsService.ts:234-265. IChatSessionItem carries metadata (title, read/unread state, isActive, controller) and providers can fork/transfer sessions; sessions are first-class workbench objects with lifecycle. | READ A/ui/screens/CopilotChatPanelOverlay.kt:159-233. App sessions are a prefs JSON blob with role/text/rating of the last 50 messages — attachments, images, tool iterations, plan state, and checkpoint references are all dropped on restart, and there is no fork/transfer/read-state (CH05). |
| Editing session snapshots | READ V/src/vs/workbench/contrib/chat/browser/chatEditing/chatEditingSession.ts:152,386-418. ChatEditingSession.createSnapshot(requestId)/restoreSnapshot snapshot PER REQUEST with undo stops, restorable from the checkpoint timeline UI. | READ A/chat/PendingChangesStore.kt:146-215,283-305. App checkpoints per APPLY (not per request) with a 1MB cap and grouped retention; undo is one-tap whole-batch and silently swallows per-file restore failures (CH01) — no timeline, no per-request restore points. |
| Tool permission levels | READ V/src/vs/workbench/contrib/chat/common/chatPermissionWarnings.ts:51-105. Auto-approval levels are ranked by reach, elevated levels need an explicit confirm dialog with detail, and per-session default level is a setting. | READ A/agent/AgentFlowGate.kt:28-46; A/ui/screens/ChatApprovalCard.kt:40-89. App has the same three-level shape (AUTO_ALL/AUTO_SAFE/MANUAL) + Always-allow, but switching to an elevated level needs no confirmation, and the MANUAL approval card shows only the first 160 chars of the args, clamped to 4 lines (CH03). |
| BYOK model management | READ V/src/vs/workbench/contrib/chat/browser/languageModelsConfigurationService.ts:35,241-247. Language Models editor: text-model based config parsed into provider groups, per-provider models curated and validated against the document. | READ A/chat/CustomEndpointStore.kt:5-49,60-120; A/ui/screens/AiKeysSection.kt:446-537. App's v2 endpoint registry (CRUD endpoints, per-endpoint keys, manual+live model groups) matches the VS Code shape closely — MK v1's single-slot breakage is fixed and shipped; per-entry delete + Refetch present. Strength, not gap. |
| Custom chat modes | READ V/src/vs/workbench/contrib/chat/common/chatModes.ts:39,77-105. ChatModes service stores custom modes per session type (tool sets, model, description) with a storage key namespace. | READ A/chat/CustomModeStore.kt:1-126; A/ui/screens/CopilotChatPanelOverlay.kt:1000-1010. App custom modes exist (tool allowlist RESTRICT-ONLY, per-project store, missing-mode fallback notice) — parity in substance; mode definitions are project-local files vs VS Code's global storage (divergence recorded, not a gap). |
| Quota notifications | READ V/src/vs/workbench/contrib/chat/browser/chatQuotaNotification.ts:18-33. Quota status notification with a persisted dismissal flag that survives reloads until the quota RECOVERS. | READ A/ui/screens/CopilotChatPanelOverlay.kt:1155-1180. App mirrors 429/402 into NotificationStore with a dedup key and retry-after tip — equivalent surfacing; no persisted dismissal (notifications reding on each occurrence; minor). |
| Inline editor chat | READ V/src/vs/workbench/contrib/inlineChat/browser/inlineChatController.ts:146. InlineChatController is an editor contribution: chat in the editor zone with widget/diff integration. | App has NO inline editor-zone chat at all — chat lives only in the panel (known I2 backlog item from the integration map; recorded here so the group doc is complete, not a new finding). |
| Secret storage | READ V/src/vs/workbench/api/common/extHostSecrets.ts:13. ExtensionSecrets implements vscode.SecretStorage — the single secrets primitive extensions must use. | READ A/data/SecureTokenStore.kt:17-30. App's EncryptedSharedPreferences + Android Keystore store IS the same single-primitive pattern and providers are contractually forbidden from inventing their own storage — contrast with SG04, where the SAME token escapes into git argv; the storage is fine, the transit is not. |

## Fresh scratch-only device checks, NONE RUN

Use a disposable project; record PASS/FAIL/NOT RUN with reason. Several checks need a real API key on a throwaway account.

| Test | Action -> pass criterion and diagnostic |
|---|---|
| AT01 CF03 | Put a VALID key in slot 2 and an INVALID key in slot 1 for one provider; send a chat. Pass: first request 401s, failover retries with slot 2 and the reply arrives; Output shows the cooldown line; slot 1 shows "cooling" state on next send. |
| AT02 CF03 | Force a 429 (small quota key, rapid sends). Pass: same key retried after retryAfterMs before failover; no cooldown line for 429. |
| AT03 CF03 | Start a streaming reply, then let it complete; send again. Pass: no duplicate content in the second reply (hasStarted guard resets per request). |
| AT04 CF04 | Add two custom endpoints with different keys and manual model IDs each; send on endpoint A's model, then B's. Pass: each request uses its OWN key+base URL; stale manual IDs from A are not offered under B's picker; both endpoints deletable and the picker updates. |
| AT05 CF04 | Migrate: with a v1 legacy custom endpoint configured, update the app build. Pass: endpoint appears once as `custom`, key intact, models still resolve. |
| AT06 CF05 | Paste a Gemini key into the OpenAI row. Pass: routing detection offers to save it to GEMINI; live check runs against Google with the real error text if invalid. |
| AT07 CF05 | Run live check with a REVOKED key. Pass: status shows the REAL vendor error (401 text), not generic "unreachable". |
| AT08 CF06 | Leave model on Auto with provider switched in Settings. Pass: next send resolves to the new provider's default (no stale vendor). |
| AT09 CF06 | Context gauge with a large file attached vs TokenCounter heuristic. Pass: gauge reflects provider countTokens (or heuristic if provider null) and does not delay the reply. |
| AT10 CF09 | Attach an image on a provider with supportsImages=false (e.g. Anthropic if set false). Pass: send REFUSES with a clear message, no API call wasted. |
| AT11 CF09 | Send while a reply is streaming, then send a THIRD message before the second completes. Pass: queued text = LAST message only, first queued silently replaced? Record actual behavior (CH07). |
| AT12 CF07/08 | Fill a session with 60 messages, restart the app. Pass: last 50 restored, order intact; record whether any tool/image context visibly lost mid-conversation. |
| AT13 CF08 | Restart the app after an image attachment turn. Pass: text persists; record how the missing image is (not) represented. |
| AT14 CF10 | Set flow to MANUAL; trigger a run_command. Pass: approval card shows tool + args; Reject skips with transcript note; Always Allow stores and future calls of that tool skip the gate. |
| AT15 CH03 | In MANUAL flow, trigger a run_command whose command is 400+ chars with a benign `; echo end` beyond char 160. Pass criterion question: record whether the card lets you see the full command (predicted NO — take(160) + 4-line clamp). |
| AT16 CF10 | Set flow to AUTO_SAFE; trigger read_file then run_command. Pass: read passes silently, run_command pauses. |
| AT17 CF11 | AGENT mode: ask the model to write_file a new file. Pass: NO disk write until Apply; read_file returns the staged version; transcript shows the staged note. |
| AT18 CF11 | Stage an edit for a CLOSED file, modify that file on disk externally, then Apply. Pass: Drift card variant with the disk content shown; rediff re-bases; Apply succeeds after. |
| AT19 CF11 | Stage an edit, make the target directory read-only (chmod 555 via terminal), Apply. Pass: Blocked/Failed variant with a real message, no partial write, `.chatapply.tmp` cleaned up. |
| AT20 CF12 | Apply a batch, then Undo. Pass: files return to pre-apply content; open editors refresh without cursor corruption. |
| AT21 CH01 | Apply a 2-file batch where one checkpoint dir is made unreadable (chmod), then Undo. Record: does the toast say "Restored 1 file(s)" with NO mention of the failure? Does the failed file stay as AI content? |
| AT22 CH01 | Apply a >1MB file, then Undo. Pass: file does NOT revert (checkpoint skipped by cap); record whether the UI says anything at Apply or Undo time about the missing checkpoint (predicted silent). |
| AT23 CF13 | Stage a plan; send a follow-up message before approving. Pass: model refuses to execute plan steps and points at the Approve card. |
| AT24 CF13 | Approve a plan; send "go". Pass: the loop executes steps; Reject/Revise clear the halt. |
| AT25 CF14 | Type /help, /clear, /rename test. Pass: commands act locally, never reach the model. |
| AT26 CF14 | Run a file skill with context. Pass: input prefilled + context attached, NOT auto-sent. |
| AT27 CF15 | Retry the last turn after an error. Pass: error message and replies dropped, question re-sent. |
| AT28 CH09 | Export with NO project open. Pass: error mentions the project. Then export with a read-only project root. Record: does the toast say "open a project first" even though a project IS open (predicted YES — write failure misattributed)? |
| AT29 CF16 | Stop mid-stream. Pass: partial reply kept with [stopped]; next send works (no stuck loading). |
| AT30 CF16 | Force a 429 from a provider. Pass: notification bell entry with retry-after; red bubble with vendor text. |
| AT31 CH02 | In chat, ask the agent to read a file by its GUEST path (`/root/<project>/...` as seen in terminal output). Record: "File not found" (predicted YES). Repeat with the host path. Pass criterion for the gap: same file, two path forms, one works one fails. |
| AT32 CH02 | Ask the agent to list_files on the guest project root. Record the same split. |
| AT33 CF17 | Ask the agent for git_status with repo_dir as a HOST path. Pass: guest-side .git check fails? Record the error string (git tools expect guest paths — the inverse split of CH02). |
| AT34 CF19 | Add an MCP server needing Node with no node installed. Pass: runtime detected, guided install runs, discovery then lists tools; all inside 60s per call. |
| AT35 CF19 | Disable one tool of an MCP server, refresh. Pass: tool hidden from the model's tool docs; re-enable restores it. |
| AT36 CF20 | Settings: container Restore with confirm; session-restore toggle off then relaunch. Pass: destructive confirm appears for restore; toggle honored (no files reopened). |

## Source-confirmed gaps for master fix-plan input

| Gap | Priority below owner's established top tier | Bounded source finding |
|---|---|---|
| CH01 | **TOP TIER (owner ruling 2026-09-24) — SG02's sibling: same silent-or-misreported recovery failure, different surface; fix together with SG02** | `undoLastApply` swallows per-file copy failures (`catch (_: Exception) { }`) and reports misleading results: a partial batch restore says "Restored 1 file(s)" with NO mention of the files that failed, and a total failure says "No checkpoints found to restore" even when checkpoints EXIST but the copies failed (read-only dir, chmod, path moved). It also never tells the user that a >1MB file got no checkpoint at Apply time. This is the recovery path for an AI apply — the same shape as SG02's restore-swallow on the timeline path; both must join the S01 typed-result design. READ A/chat/PendingChangesStore.kt:283-305,249-271. AT21/AT22. |
| CH02 | HIGH in group, DG02-class host/guest identity split at the agent tool boundary — **PLAN A RULING (2026-09-24): NOT covered by PLAN A as scoped; RECOMMENDED = separate translation choke point in AgentTools sharing ScmState's converter** | run_command translates its workdir host→guest at one choke point (AgentTools.kt:229-233, the C4 fix), but read_file/write_file/list_files/search_files take RAW `File(path)` with NO translation in either direction. The model is systematically exposed to GUEST paths (terminal output inside the rootfs, run_command results, workdir echoes) yet a guest-form path in read_file returns "File not found" — and the git tools expect GUEST repo_dir (inverse expectation). One tool layer, two path conventions, no single conversion point; PLAN A alone would not fix it (same ruling as DG02: the boundary translation is the missing piece). Note: `writeFile`'s direct-disk branch is unreachable today (AGENT mode intercepts write_file into staging before executeTool) — the split is live for read/list/search only. READ A/agent/AgentTools.kt:221-306. AT31-AT33. |
| CH03 | MEDIUM-HIGH in group, security-adjacent honesty-of-controls (SG01 family), BELOW the EX/SG04 tier — **TIER WEIGHING (2026-09-24): stays SG01-family (requires MANUAL flow + over-long command + human Approve; degrades a confirmation rather than escaping containment unassisted), but record the SG04 BRIDGE: one blind approval CAN read the argv token, so the display fix ships WITH the SG04 argv fix; reasoning in MASTER-GAPS.md** | In MANUAL flow the approval card is the ONLY human gate on arbitrary shell execution, but `argsSummary = toolArgs.toString().take(160)` and the card renders it with `maxLines=4 + Ellipsis` — a 400-char command with a destructive `;`-chain after position 160 is approved BLIND. VS Code's elevated-permission dialogs carry the full detail (chatPermissionWarnings.ts:87-105). An approval control that cannot display what it approves is not a gate. READ A/ui/screens/CopilotChatPanelOverlay.kt:630-637; A/ui/screens/ChatApprovalCard.kt:40-89. AT15. |
| CH04 | MEDIUM, session persistence fidelity | saveSessions persists ONLY role/text/rating of the last 50 messages. Attachments, images, audio, tool-chip entries survive as text only, plan state, checkpoint references, connector cards, and the queued message are dropped on restart; a restored conversation re-sends to the model WITHOUT its attachment content, silently changing what the model sees mid-thread. VS Code sessions are first-class objects with full metadata (chatSessionsService.ts:234-265). READ A/ui/screens/CopilotChatPanelOverlay.kt:172-233. AT12/AT13. |
| CH05 | LOW-MEDIUM, staging-failure falls back to an ungated direct write | If `PendingChangesStore.stage()` throws (the intercept wraps it in try/catch → stagedMsg=null), the loop falls through to FlowGate approval and then `executeTool("write_file")` → `writeFile()` writes DIRECTLY to disk — bypassing the entire R6 "never reaches disk without Apply" contract. stage() is defensive today so this is latent, but the fallback semantics are wrong: a staging failure should REFUSE, not route to the one code path that writes ungated. The same latent path is why CH02's write_file translation omission still matters. READ A/ui/screens/CopilotChatPanelOverlay.kt:628-640; A/agent/AgentTools.kt:177,256-261. |
| CH06 | LOW-MEDIUM, silent tool-result truncation misleads the model | run_command results `.take(4000)`, readFile `.take(8000)` after the >500KB refusal — with NO truncation marker, so the model believes it saw the entire output/file and can answer about content it never received. searchFiles caps at 500 files/100KB silently. readFile's only honest refusal is the size precheck; everything under the cap truncates silently. A one-line "(truncated)" suffix is the fix class. READ A/agent/AgentTools.kt:237,255,268-296. |
| CH07 | LOW, queued input single-slot silent overwrite | R8-QUEUE queues ONE message; a second message sent while loading REPLACES the first with no signal — the user believes both were sent. A queue depth indicator or multi-slot queue is the fix. READ A/ui/screens/CopilotChatPanelOverlay.kt:1096-1098. AT11. |
| CH08 | LOW, export failure misattribution (S01-adjacent) | `writeSessionMarkdown` returns null for BOTH "no project root" and "write failed" (catch-all), and the panel's error branch always says "Export failed — open a project first" — a real write failure (read-only root, disk full) is reported as a project problem. READ A/ui/screens/CopilotChatPanelOverlay.kt:1784-1793; A/chat/ChatRetryExport.kt:92-120. AT28. |
| CH09 | LOW-MEDIUM, FlowGate approval has no timeout and a single pending slot | `awaitApproval` suspends on a CompletableDeferred with no timeout; `pending` is one slot. If the approval card is lost (panel closed by a config change path that keeps the scope alive, or a future headless caller), the chat stays loading forever with no prompt — the Stop button is the only escape and it is not discoverable as such. A stale `pending` from a cancelled coroutine is cleared only because stopChat nulls it. READ A/agent/AgentFlowGate.kt:28-46. |
| CH10 | LOW, MCP discovery is once-per-process with no change signal | `discoveryStarted` latches true for the process lifetime: adding/removing servers requires the manual Refresh button (nothing watches mcp_servers.json), and discovery failures surface ONLY as Output-channel lines — a misconfigured server looks identical to "no tools" in the chat picker unless the user reads logs. READ A/agent/McpClientManager.kt:258-280. AT34/AT35. |
| CH11 | LOW, runBlocking bridge for MCP tool calls | executeTool bridges MCP calls with `runBlocking { McpClientManager.callTool(...) }` — inside the 10-iteration agent loop this blocks the coroutine's IO thread for up to the 60s MCP timeout per tool call; a slow server serializes the whole agent turn and cannot be cancelled mid-call by Stop. READ A/agent/AgentTools.kt:206-212. |
| CH12 | LOW, persisted-key plaintext adjacency in prefs index | ChatKeyPool stores the slot ORDER and LABELS in plain SharedPreferences — non-secret by design and values stay in SecureTokenStore, but labels are user-entered and ride the unencrypted prefs; consistent with VS Code's settings-vs-secrets split, recorded for completeness, no action implied. READ A/chat/ChatKeyPool.kt:24-52. |
| CH13 | LOW, session blob write amplification | saveSessions serializes EVERY session (all messages, last 50 each) on every persist — O(total messages) per send/rating/command, on the UI-thread prefs apply path; with many sessions this is measurable main-thread work. A per-session file or delta write is the fix class. READ A/ui/screens/CopilotChatPanelOverlay.kt:172-193,976-983. |
| CH14 | LOW, settings destructive-confirm coverage is uneven | Container Restore and Reinstall have confirm dialogs, but "Clear Workspace Memory" and per-item "Clear Data" act immediately on tap of the row's Clear (the confirm dialog exists only for `showClearDialog` items — verify which items route through it); MCP server DELETE (`removeServer`) has no confirm and silently kills a live session. READ A/ui/screens/SettingsScreen.kt:176-199,527-560; A/agent/McpClientManager.kt:142-152. AT36. |

**Boundary:** Editor owns buffer refresh and G01's write path; SCM owns the v2 dir mechanics and SG02's restore surfaces (CH01 must be fixed WITH them — same swallow pattern, same design); Terminal owns execOnce/proot spawn semantics; Problems/SCM attach rows are data producers into chat context. Strengths recorded: the provider abstraction is genuinely registry-driven (a new provider touches zero panel code), MK v2 shipped the full VS Code Language-Models-editor shape, SecureTokenStore is the real EncryptedSharedPreferences+Keystore pattern with a storage contract, R6 apply is fail-closed with typed outcomes, key failover distinguishes 401/429/WAF correctly, and quota errors mirror into the notification system. OWNER RULINGS 2026-09-24: CH01 is escalated INTO the top tier (G01/TB01/TB03/SG02/CH01) as SG02's sibling on the recovery chain. CH02 carries an explicit PLAN A coverage ruling: PLAN A as scoped does NOT cover the agent tool boundary — recommended fix is its own translation choke point in AgentTools sharing ScmState's converter; never assume a PLAN A ticket covers it. CH05+CH01 are flagged as a COMPOUND RISK (ungated fallback write with no checkpoint, then a misreported undo = an edit with no recovery path at all) and must land together. CH03 stays SG01-family BELOW the EX/SG04 security tier (reasoning in MASTER-GAPS.md: it degrades a human confirmation instead of escaping containment unassisted) with the SG04 bridge recorded — its display fix ships with the SG04 argv fix. All other Chat gaps remain below the top tier. Do not mark any AT case passed or implement a fix based on this audit alone.
