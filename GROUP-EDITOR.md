# Editor: source-only group audit

**Evidence convention.** `READ A/path:line` means that line was read in the current Kotlin tree, with `A = android/app/src/main/java/com/codespace/ide/`. `READ V/path:line` means that line was read in the sibling VS Code clone, with `V = vscode-src/src/vs/` (the clone is outside this repository). Line numbers are 1-based. The tests below are **source-derived instructions, not executed results**. Other groups will own their broader implementations; their editor-facing entry points are still recorded here. No historical device result is used.

## FEATURES: live entry points

| ID | Feature and entry point |
|---|---|
| E01 | The editor is `EditorPane` containing a `key(activeId)`-mounted `CodeEditor`; `BasicTextField` consumes and emits `TextFieldValue`, with `onContentChange` updating the matching `EditorTab`, setting dirty, writing absolute paths to disk, invalidating `FileCache` and publishing lint. READ A/ui/panes/EditorPane.kt:147,2053,2081-2092; A/editor/CodeEditor.kt:513,2303-2306. |
| E02 | File loading and explicit save: `loadFileContent` reads through `FileCache`; `saveCurrentFile` handles Ctrl+S; external file reload and AI Apply have tab-refresh paths; a history restore emits a tick but does not pass its file path to the tab-refresh filter (see G02). READ A/ui/panes/EditorPane.kt:114-121,210-249,305-316; A/editor/CodeEditor.kt:1029-1064. |
| E03 | Syntax highlighting and large-file background precomputation, line numbers, active line, indentation guides, word and bracket matching, and diagnostic/semantic decorations: `CodeEditor` and rendering helpers. READ A/editor/CodeEditor.kt:2288-2306,3097-3107,3597-3605; A/editor/CursorBehaviors.kt:41,112; A/editor/MinimapSection.kt:46. |
| E04 | Typing helpers: language-aware bracket/quote pairs, Tab and Shift+Tab multiline indentation, snippet tab stops, hardware shortcuts and the mobile accessory-key row (characters, Tab, Esc, MC, Undo `↩`, Redo `↪`). READ A/editor/BracketPairConfig.kt:35,137-210; A/editor/CodeEditor.kt:2760-2889; A/ui/screens/EditorExtraKeysRow.kt:34-64; A/ui/screens/ProjectShellScreen.kt:4720-4724. |
| E05 | Undo/redo snapshots, keyboard/editor-toolbar actions, line duplication and comment toggling: per-mounted-editor `SnapshotUndoManager` and key handlers. READ A/editor/CodeEditor.kt:994,2943-3084; A/editor/undo/SnapshotUndoManager.kt:19,41-97; A/editor/SnapshotUndoInit.kt:15. |
| E06 | Selection and multi-cursor: MC accessory toggle, canvas double-tap, contextual Select Next/All Occurrences, Add Cursor Above/Below/All Lines Above/Below, synchronized edits and external-edit cursor remapping. READ A/ui/screens/EditorExtraKeysRow.kt:47-65; A/editor/CodeEditor.kt:2603-2623,3131-3144,4040-4085,4245-4353,1009-1031; A/editor/McEditTransaction.kt:89,124; A/editor/MultiCursorEngine.kt:122. |
| E07 | In-file find/replace: `Find & Replace` strip button opens the bottom `FindReplaceBar`, with Find/Replace fields, `.*`, `Aa`, `W`, `AB`, up/down navigation, `Replace`, `Replace All`, and close. State also accepts shell find controls and persists through `EditorFindState`. READ A/ui/panes/EditorPane.kt:1139-1148,2126-2130; A/editor/CodeEditor.kt:4744-4771; A/editor/FindReplaceBar.kt:93-130,134-346; A/editor/EditorViewStateEffects.kt:31,107-127. |
| E08 | In-file navigation: `Go to Line` strip icon, bounded numeric input and `Go` button, 1-based visual target; navigation `←`/`→` in the quick strip; incoming cross-file jump resolves target tabs, then converts 0-based caller line to 1-based editor scroll line. READ A/ui/panes/EditorPane.kt:1161-1168,2157-2209; A/editor/GotoLineBar.kt:30,78-125; A/ui/panes/EditorStripQuickActions.kt:90-114; A/ui/screens/ProjectShellScreen.kt:1010-1027. |
| E09 | Formatting: `regex`-labeled Format Document button calls `DocumentFormatter`; `{}` LSP format button uses server edits when running; `[|] sel` formats selection; format-on-save is settings-gated. These are distinct actions with distinct writers. READ A/ui/panes/EditorPane.kt:263-300,1170-1282; A/editor/DocumentFormatter.kt:144,234; A/editor/CodeEditor.kt:1064-1083. |
| E10 | Fold ranges and bookmarks: editor gutter toggles folds and `◆` bookmarks; bookmark strip button opens cross-open-file list; folded starts are path-keyed and persisted, while bookmark map is held by `EditorPane`. READ A/editor/CodeEditor.kt:820-824,2225-2262; A/editor/VisualLineMapper.kt:72,239-254; A/ui/panes/EditorPane.kt:476-477,567-569,1284-1350,2118-2122,2153-2154. |
| E11 | Visual viewport: word wrap, font zoom (`−`, size, `+`), minimap toggle/navigation, sticky scope header, per-view scroll lock (IME autoscroll only), cursor/viewport capture and restore. READ A/ui/panes/EditorStripQuickActions.kt:45-115,129-141; A/editor/CodeEditor.kt:800-815,1875-1888,2022-2058,3597-3605; A/editor/MinimapSection.kt:46,79-109,206-215; A/editor/EditorViewStateEffects.kt:31,64-100; A/ui/panes/EditorPane.kt:564-569,2031-2053. |
| E12 | Split view: fixed shell `Split Editor` button creates another view for the active file; its own cursor, scroll and lock are keyed by view ID; both instances share the tab's content. Each split has a close button. READ A/ui/screens/ProjectShellScreen.kt:4577-4601; A/editor/SplitViewStore.kt:39-72; A/ui/panes/EditorPane.kt:1059-1115,2051-2053,2103-2122. |
| E13 | Git-facing editor overlays: `Git Blame` strip toggle displays per-line author data; conflict markers offer Ours/Theirs/Both resolution; modified-line/minimap indicators read diff data. READ A/ui/panes/EditorPane.kt:1152-1158,1979-2007,2055-2059,2213-2227; A/editor/EditorOverlays.kt:238-294; A/editor/MinimapSection.kt:46-67. |
| E14 | Problems/LSP-facing editor overlays: lint and server diagnostics, squiggles, diagnostic tooltip/error lens, lightbulb actions, semantic tokens, inlay hints, hover, CodeLens, document links, folding ranges, and gutter breakpoints/current debug line. READ A/ui/panes/EditorPane.kt:1360-1382,1710-1803,2090-2093,2134-2153,2254-2670; A/editor/CodeEditor.kt:575-605,610-640,3237,3630-3803,4780-4803; A/editor/ErrorLensOverlay.kt:40; A/editor/ui/DiagnosticTooltip.kt:37. Feature-level protocol tests also belong to LSP, IntelliSense, Problems, and Debugger groups. |
| E15 | Symbol/navigation context menu: long-press editor or tap `Code actions`, then Expand Selection, Go/Peek Definition or Declaration, Type Definition, References, Implementations, Call/Type Hierarchy, Rename, occurrence selection, source actions and generation/refactoring. READ A/editor/CodeEditor.kt:3630-3658,3807-4455; A/ui/panes/EditorPane.kt:2431-2670. Full server-response coverage belongs to LSP/IntelliSense. |
| E16 | Completions, snippets and ghost text: local/language and LSP suggestions share popup; snippet placeholders advance with Tab/Shift+Tab and choices popup; AI continuation uses ghost overlay and Tab acceptance. READ A/editor/CodeEditor.kt:285-378,2758-2839,4817-4890; A/editor/CompletionPopupOverlay.kt:110; A/editor/GhostTextOverlay.kt:17. Detailed providers belong to IntelliSense and AI. |
| E17 | AI review crossing into editor: staged changes display affected lines in `AiReviewStrip`/gutter; Apply increments `appliedTick`, records its path for a tab reload and enters one undo snapshot where eligible; history restore increments the same tick without recording its path; active selection is published for chat attachment. READ A/ui/panes/EditorPane.kt:237-249,2062-2070; A/chat/PendingChangesStore.kt:179-193,302-308; A/editor/CodeEditor.kt:1052-1063,2339-2342; A/ui/screens/ChatAttachPicker.kt:227; A/chat/PendingChangesStore.kt:70,308,316. |
| E18 | Testing/performance-facing rendering: test CodeLens is merged into code lenses, large content has background highlighting and viewport mapping/minimap, and editor performance probes instrument frame behavior. READ A/ui/panes/EditorPane.kt:1811-1858; A/editor/CodeEditor.kt:2288-2301; A/editor/VisualLineMapper.kt:72,156-178; A/editor/PerfProbe.kt:1. Their runners/metrics belong to Testing and Performance. |

## TESTS: source-derived, run only on disposable project files

**Fixture.** In a scratch project, create `EditorProbe.kt` and paste this exactly, then create `Peer.kt` with `fun peer() = 1`. Keep backups before testing format, conflict resolution, external restore, or staged edits. The editor input/write path is READ A/ui/panes/EditorPane.kt:2081-2092. These tests predict UI behavior from source; a failure means the observed behavior differs from that prediction.

```kotlin
package audit
fun alpha() {
    val needle = "needle"
    println(needle)
}
fun beta() {
    println("needle")
}
```

| Case | Tap-by-tap procedure, expected result, failure check | Source |
|---|---|---|
| T01 E01/E02 | Tap `EditorProbe.kt` in Explorer, tap end of line 3, type ` // edit`, tap save (or Ctrl+S), close/reopen file. Expect edit retained and canvas matches disk; fail if edit vanishes, appears in `Peer.kt`, or an explicit save reports success with unchanged disk. | READ A/ui/panes/EditorPane.kt:114-121,305-316,2081-2092. |
| T02 E03 | Tap `EditorProbe.kt`, tap `needle` on line 4, then tap a bracket on line 2. Expect line numbers, caret/selection, token colors and word/bracket marking attached to the tapped tokens; fail if colored marks follow the wrong line after scroll. | READ A/editor/CodeEditor.kt:2288-2306,3097-3107; A/editor/CursorBehaviors.kt:41,112. |
| T03 E04 | With caret in a scratch line, type `(`, `{`, `[` and a quote one at a time, move inside each empty pair and press Backspace; expect configured matching closers and paired deletion where configured. Fail if pair handling duplicates or deletes neighboring text. | READ A/editor/BracketPairConfig.kt:35,137-210; A/editor/CodeEditor.kt:2305-2561. |
| T04 E04 | Focus editor to show accessory row, tap `Tab`, `Esc`, `{`, `↩`, `↪` separately in scratch content. Expect their dispatched insertion/escape/newline/indent effect rather than characters appearing in the terminal; fail if active editor gets no event or wrong punctuation. | READ A/ui/screens/EditorExtraKeysRow.kt:34-65; A/ui/screens/ProjectShellScreen.kt:4720-4724; A/editor/KeyInsertDispatcher.kt:24-41. |
| T05 E04 | Select lines 3-4 by dragging handles, press hardware Tab, then Shift+Tab. Expect both selected lines indented then unindented without losing code; fail if only one line changes or text is dropped. | READ A/editor/CodeEditor.kt:2844-2896; A/editor/EditorActions.kt:15-46. |
| T06 E05 | Append `hello`, tap accessory `↩` (or Ctrl+Z), then `↪` (or Ctrl+Y). Expect old text then `hello`; fail if either action edits a different file or skips unrelated edits. | READ A/editor/CodeEditor.kt:2943-2974; A/editor/undo/SnapshotUndoManager.kt:41-97. |
| T07 E05 | Put caret on line 3, press hardware Ctrl+Shift+D to duplicate the line, then Ctrl+/ to toggle a comment; press Ctrl+Shift+K to delete that line; tap `↩` to undo. Expect a duplicated, commented, then deleted line, with undo recovering it; fail if another line changes or undo loses content. | READ A/editor/KeyBindingRegistry.kt:106-121; A/editor/CodeEditor.kt:2975-3025; A/editor/LanguageConfigStore.kt:76. |
| T08 E06 | Tap `MC` above keyboard; tap two distinct positions on lines 3 and 4; type `X`; tap `MC` again. Expect highlighted MC chip and simultaneous edits/cursors, then single-cursor mode; fail if typing only edits one position or mode color disagrees with mode. | READ A/ui/screens/EditorExtraKeysRow.kt:47-65; A/editor/CodeEditor.kt:2603-2623,3131-3144; A/editor/MultiCursorEngine.kt:122. |
| T09 E06/E15 | Long-press `needle`, tap `Select Next Occurrence` and type `Z`; repeat with `Select All Occurrences` on restored fixture. Expect selection/additional cursors only at matching `needle` words; fail if a different word or another file changes. | READ A/editor/CodeEditor.kt:4040-4085; A/editor/MultiCursorEngine.kt:122. |
| T10 E06/E15 | Put caret on line 4, open `Code actions`, tap `Add Cursor Above`, then `Add Cursor Below`; repeat with `Cursors on All Lines Above/Below` in the scratch file. Expect same-column carets in requested directions; fail if counts/columns are wrong. | READ A/editor/CodeEditor.kt:3630-3658,4245-4353. |
| T11 E07 | Tap strip `Find & Replace`, type `needle` into Find; tap down then up arrows. Expect `1/3` counter initially (three occurrences in fixture) and selected next/previous matches; fail if counter differs or movement leaves this file. | READ A/ui/panes/EditorPane.kt:1139-1148; A/editor/FindReplaceBar.kt:93-130,177-221. |
| T12 E07 | In Find type `needle`, toggle `Aa`, `W`, then `.*`; separately try regex `nee.le`, and a missing string `absent_543`; expect toggles change matching and missing query reads `No results`; fail if match count/options don't reflect current input. | READ A/editor/FindReplaceBar.kt:177-286; A/editor/EditorFindState.kt:1. |
| T13 E07 | Find `needle`, Replace `token`, tap `Replace` once, inspect 2 remaining, then `Replace All` and inspect no remaining matches; restore fixture. Toggle `AB` and replace mixed-case words in a separate scratch line. Fail if only display changes while file content remains unchanged. | READ A/editor/FindReplaceBar.kt:271-346,348; A/editor/CodeEditor.kt:4744-4771. |
| T14 E08 | Tap strip `Go to Line`, type `7`, tap `Go`. Expect caret and highlight on line 7 (`println("needle")`); then type `9999` and tap `Go`, expect clamp to last line. Fail if line numbering is off or invalid input crashes. | READ A/ui/panes/EditorPane.kt:1161-1168; A/editor/GotoLineBar.kt:30,78-125; A/editor/CodeEditor.kt:4688-4711. |
| T15 E08 | Open `Peer.kt` from Explorer, open Search, search `needle`, tap the result in `EditorProbe.kt`; tap strip `←`, then `→`. Expect back/forward restore the two target files and locations; fail if it changes to the wrong file. | READ A/ui/panes/EditorStripQuickActions.kt:90-114; A/ui/screens/ProjectShellScreen.kt:1010-1027. |
| T16 E09 | Change scratch line 2 to `fun alpha(){`, tap `regex` Format Document icon; expect either changed formatted content with a `Formatted` notification or an explicit `Format result` reason. Fail if action silently mutates other file or gives no observable outcome. | READ A/ui/panes/EditorPane.kt:1170-1216; A/editor/DocumentFormatter.kt:144,234. |
| T17 E09 | Select lines 2-4, tap `[|] sel`; expect only selected range formatted or explicit no-op, with outside range unchanged. On a language with running LSP, tap `{}` `LSP` separately and inspect server edits. Fail if an unselected region is modified by selection formatting. | READ A/ui/panes/EditorPane.kt:1226-1282; A/editor/CodeEditor.kt:1064-1083. |
| T18 E09 | Enable Format on Save in Settings, put unformatted text in scratch file, tap File > Save in the menu (not Ctrl+S, which uses a different callback), then disable and repeat. Expect formatter path only when enabled and both branches save; fail if disabled still reformats or either save silently discards edits. | READ A/ui/screens/ProjectShellScreen.kt:1176,3054-3057; A/ui/panes/EditorPane.kt:263-300; A/ui/screens/InProjectSettingsDialog.kt:1116. |
| T19 E10 | Tap gutter `◆` at line 3, tap strip `◆`, tap the `EditorProbe.kt : 3` row; expect bookmark and navigation to that line. Toggle marker off; fail if it remains or jumps into another file. | READ A/editor/CodeEditor.kt:2235-2243; A/ui/panes/EditorPane.kt:1284-1350. |
| T20 E10 | Tap fold chevron beside `fun alpha` block, confirm body collapses; tap it again and confirm body returns. Switch to `Peer.kt` and back; expect saved fold state tied to `EditorProbe.kt`, not peer. Fail if hidden text is deleted or folded content appears under wrong file. | READ A/editor/CodeEditor.kt:2225-2262; A/editor/VisualLineMapper.kt:156-178,239-254; A/ui/panes/EditorPane.kt:567-569,2118-2122. |
| T21 E11 | Tap `+`, `+`, `−` in strip and read numeric font size; tap `↵` word wrap using a long line in scratch file. Expect net +1 size, and wrapping changes line display without inserting newline; fail if file content changes when merely wrapping. | READ A/ui/panes/EditorStripQuickActions.kt:45-78; A/editor/VisualLineMapper.kt:320. |
| T22 E11 | Open a file longer than one screen, tap the minimap near bottom then drag; expect main viewport follows, viewport indicator remains aligned. Tap minimap toggle to hide/show. Fail if minimap gesture edits text or cannot reach lower lines. | READ A/editor/MinimapSection.kt:79-109,206-215; A/editor/CodeEditor.kt:3597-3605. |
| T23 E11/E12 | Tap `Split Editor`, focus one view, scroll and position caret, tap its padlock, switch other view and return. Expect lock only gates IME-aware autoscroll, not content sync; unlock, switch tabs, return, expect per-view cursor/scroll restored. Fail if typing in one view fails to appear in peer view. | READ A/ui/screens/ProjectShellScreen.kt:4577-4601; A/ui/panes/EditorStripQuickActions.kt:129-139; A/editor/EditorViewStateEffects.kt:64-100; A/editor/CodeEditor.kt:732,1009-1031. |
| T24 E11 | In a long file with nested functions, scroll past enclosing declaration and observe sticky scope header; toggle word wrap and observe its conditional hiding. Fail if header names another file or obscures the first line. | READ A/ui/panes/EditorPane.kt:1959-2047; A/editor/CodeEditor.kt:2022-2058. |
| T25 E12 | Tap fixed `Split Editor` button, tap new split's close `✕`; expect only split closes, primary tab remains. Fail if closing split closes the file itself. | READ A/ui/screens/ProjectShellScreen.kt:4577-4601; A/ui/panes/EditorPane.kt:1059-1115. |
| T26 E13 | In a disposable Git-tracked file, tap strip `Git Blame`; expect line annotation if blame data exists. Separately paste a three-part conflict (`<<<<<<< HEAD`, `=======`, `>>>>>>> branch`), tap Ours/Theirs/Both independently on restored fixture; expect only selected hunk resolution. Fail if unrelated text changes. | READ A/ui/panes/EditorPane.kt:1152-1158,1979-2007,2055-2059,2213-2227; A/editor/EditorOverlays.kt:238-294. |
| T27 E14 | In scratch code type `val x =`, observe lint/squiggle/error lens; tap affected line for tooltip and available bulb for action. Switch to `Peer.kt` and verify markers remain tied to original file. Fail if diagnostic from first file appears on peer. | READ A/ui/panes/EditorPane.kt:1360-1382,2090-2093; A/editor/CodeEditor.kt:3630-3803; A/editor/ui/DiagnosticTooltip.kt:37. |
| T28 E14 | With language server running, tap `⊕` inlay toggle, move caret over symbol to obtain hover, tap CodeLens and document link if present, fold LSP region if present. Expect each overlay belongs to current file and toggles as indicated; fail if stale data stays on a different tab. | READ A/ui/panes/EditorStripQuickActions.kt:75-84; A/ui/panes/EditorPane.kt:1710-1803,2254-2670. |
| T29 E14 | Tap line-number gutter to add breakpoint, long-press for condition editor, then remove breakpoint; if debugging stops there, expect current-line indicator at that line. Fail if toggling one file adds same line to peer file. | READ A/ui/panes/EditorPane.kt:2069-2079,2134-2153; A/editor/CodeEditor.kt:575-585. |
| T30 E15 | Long-press symbol `alpha` in file, open `Code actions`, use `Go to Definition`, then `Peek Definition`; expect target file/line or visible non-result, and peek closes without corrupting buffer. Repeat for Declaration, References, Implementation, Type Definition and hierarchies only when server supports them. Fail if jump lands in a different file or line. | READ A/editor/CodeEditor.kt:3630-3658,3807-4241; A/ui/panes/EditorPane.kt:2431-2670. |
| T31 E15 | On the same symbol, invoke `Rename Symbol` and enter `alphaRenamed`; review scratch usage and cancel or apply as UI provides. Tap `Expand Selection` repeatedly and verify nested range growth. Fail if unrelated file changed after cancel. | READ A/editor/CodeEditor.kt:3807-3819,4015-4045; A/editor/RenameDialogOverlay.kt:1. |
| T32 E15 | Open `Code actions`, invoke `Organize Imports`, `Remove Unused Imports`, `Remove Unused Code`, `Fix All`, then source generation actions on disposable file with corresponding constructs. Expect applied edit or explicit no-op depending language/server; fail if a command silently edits a different file. | READ A/editor/CodeEditor.kt:4352-4455; A/editor/BuiltinSourceActions.kt:1. |
| T33 E16 | Type `fun` in Kotlin scratch file, tap a visible snippet/completion item, use `Tab`, `Shift+Tab`, `Esc` through placeholders. Expect replacement at caret and ordered placeholder navigation; fail if tab inserts unrelated whitespace while snippet active. | READ A/editor/CodeEditor.kt:285-378,2758-2839; A/editor/CompletionPopupOverlay.kt:110. |
| T34 E16 | In a file with AI suggestions configured, pause after a partial line until ghost text appears, tap `Tab`; expect proposed text inserted once. Dismiss a new proposal by typing a normal character. Fail if proposal silently enters file before acceptance. | READ A/editor/CodeEditor.kt:4817-4890,2305; A/editor/GhostTextOverlay.kt:17. |
| T35 E17 | In chat stage one edit to `EditorProbe.kt` without applying, return to editor, observe review mark; tap Apply in review card and then editor Undo. Expect staged mark, applied text and undo restores pre-apply buffer. Use only disposable file. Fail if stage writes disk before Apply or undo affects another tab. | READ A/ui/panes/EditorPane.kt:2062-2070,237-249; A/editor/CodeEditor.kt:1052-1063; A/chat/PendingChangesStore.kt:90,147,316. |
| T36 E18 | In disposable source with a detectable test, inspect test CodeLens and tap its run control; for a long file scroll quickly during typing and check correct visible token/line alignment. Fail if the test lens targets another method or highlighted text lags onto changed content. | READ A/ui/panes/EditorPane.kt:1811-1858; A/editor/CodeEditor.kt:2288-2301; A/editor/VisualLineMapper.kt:156-178. |

**End of Editor feature-level tests; cross-boundary cases follow in T37-T43.**

## CONNECTIONS: shared state and cross-group calls

These are concrete read/write edges, not a dependency inferred from a similarly named class. `owner` below means the place that currently holds or mutates state, not a proposed refactor.

| State / path | Editor read/write and other group reader/writer | Failure propagation / test |
|---|---|---|
| C01 File identity and project root | Editor resolves root using `ProjectPathResolver` and matches open tabs with a canonical/suffix fallback; Explorer and shell use the same root resolver, while the rootfs translator maps guest paths to host paths. READ A/ui/panes/EditorPane.kt:214,2157-2209; A/ui/panes/ExplorerPane.kt:1485; A/ui/screens/ProjectShellScreen.kt:695; A/terminal/ProotInstaller.kt:85,121. | Wrong or ambiguous path can open a new tab rather than target the existing file. Test same-basename files in separate folders and a guest-path navigation; assert path shown and bytes actually loaded. |
| C02 Tabs, active file and viewport | Shell provides `editorTabs`, `activeEditorTab`, jump target and back/forward stacks to EditorPane; EditorPane uses path as tab/view key, maintains local scroll target. READ A/ui/screens/ProjectShellScreen.kt:1002-1027,4633-4653; A/ui/panes/EditorPane.kt:484-490,2051-2053,2157-2209. | Caller line convention is 0-based, jump UI target is 1-based; check source paths via Problems, Search, output links and definition without an extra line. READ A/ui/panes/ExplorerPane.kt:3175-3177; A/ui/screens/ProjectShellScreen.kt:4642-4653. |
| C03 Open buffer vs disk | EditorPane publishes every open tab to `EditorBufferStore.sync`; chat `PendingChangesStore.stage` reads `contentOf(path)` first, then disk. Editor `onContentChange` writes absolute paths to disk on each edit. READ A/ui/panes/EditorPane.kt:754-763,2081-2092; A/editor/EditorBufferStore.kt:14-34; A/chat/PendingChangesStore.kt:90-116. | A raw path mismatch falls back to disk; a dirty buffer is not safely distinct from disk while typing. Test staged diff against unsaved text and alias paths, but only in disposable files. |
| C04 Staged Apply and undo | Chat Apply atomically-ish replaces disk, records path, increments `appliedTick` and gates an editor undo snapshot; EditorPane reloads recorded paths; CodeEditor consumes undo gate on content-param change. READ A/chat/PendingChangesStore.kt:147-193,308-321; A/ui/panes/EditorPane.kt:235-250; A/editor/CodeEditor.kt:1045-1064. | If target is not active or path spellings differ, apply/undo synchronization is conditional. Check both active and background tab. |
| C05 History restore and file cache | Explorer Local History and Timeline restore copy snapshots and emit `bumpExternalRestore()`; EditorPane's refresh observer reads `lastAppliedPaths()` rather than the restore path, and FileCache is keyed by raw pathname/mtime. READ A/ui/panes/ExplorerPane.kt:2360-2373; A/ui/panes/TimelinePanel.kt:256-265; A/chat/PendingChangesStore.kt:302-308; A/ui/panes/EditorPane.kt:235-250; A/editor/FileCache.kt:27-58. | G02: restoring a file with no previous matching Apply does not select that tab for refresh. Inspect open buffer, tab close/reopen, and disk separately. |
| C06 View persistence | `EditorViewStateEffects` captures cursor/scroll on disposal and fold changes; EditorPane persists view-keyed cursor/scroll/locks and path-keyed folds via `SessionStateStore`; `SessionHandoffManager` uses the same store. READ A/editor/EditorViewStateEffects.kt:31-100; A/ui/panes/EditorPane.kt:754-789,2103-2123; A/data/SessionStateStore.kt:87-111,135-190; A/data/SessionHandoffManager.kt:66-94. | Alias paths and split IDs affect restore identity. Test switching two files, returning to a split, and relaunching the same project. |
| C07 Diagnostics and file badges | EditorPane publishes lint on open/change, merges URI-filtered LSP squiggles; TaskRunner and BuildPanel publish build errors into `DiagnosticPublisher`; Explorer badges and ChatAttachPicker read `DiagnosticManager`. READ A/ui/panes/EditorPane.kt:723,1360-1382,1550-1563,2090-2093; A/project/TaskRunner.kt:90-92; A/ui/panes/ExplorerPane.kt:266; A/ui/screens/ChatAttachPicker.kt:213. | Diagnostic origin/file identity can diverge among gutter, Problems, Explorer badge and chat attachment. Test same error line across all four, then switch file. |
| C08 LSP transport and editor actions | EditorPane requests hover, definition, references, rename, formatting, code actions, lenses and diagnostics via LSP handlers, passing results to CodeEditor overlays. READ A/ui/panes/EditorPane.kt:1710-1803,2254-2670; A/editor/CodeEditor.kt:3630-3803,3807-4455. | Server availability and document URI decide whether an action appears or applies. Test editor overlay belongs to active URI; protocol coverage is assigned to LSP group. |
| C09 Settings and language grammar | `FeatureToggleStore` controls word wrap/minimap/inlay/completion; settings dialog writes toggles; `ProjectSettingsStore` gates format-on-save; `LanguageConfigStore` overrides brackets and line comments by file. READ A/editor/CodeEditor.kt:812-815; A/ui/panes/EditorStripQuickActions.kt:69-84; A/ui/screens/InProjectSettingsDialog.kt:767-769,1116; A/ui/panes/EditorPane.kt:263-300; A/editor/BracketPairConfig.kt:137-152; A/editor/CodeEditor.kt:2995-3013. | Project/global setting scopes differ; check UI and effect after file/project switch, not just icon tint. |
| C10 Debugger state | CodeEditor gutter renders breakpoint/conditional/current execution state; EditorPane forwards click/long-press to breakpoint store/condition dialog. READ A/editor/CodeEditor.kt:2245-2263; A/ui/panes/EditorPane.kt:2069-2079,2134-2153. | 0-based gutter and 1-based debugger line must agree; debugger group owns runtime condition tests. |
| C11 Git state | EditorPane fetches blame for active path and overlays conflict resolutions; Source Control diff and history use the same file/disk path but have distinct update signals. READ A/ui/panes/EditorPane.kt:1979-2007,2055-2059,2213-2227; A/editor/MergeConflictParser.kt:34,95; A/ui/panes/TimelinePanel.kt:256-265. | Restore/change in one surface should not leave stale editor bytes or blame annotation in the other. |
| C12 Selection to AI | CodeEditor records selected text/file offsets in `EditorSelectionStore`; chat attachment picker reads the snapshot, and SkillsCatalog reads it too. READ A/editor/CodeEditor.kt:2339-2342; A/editor/EditorSelectionStore.kt:16-43; A/ui/screens/ChatAttachPicker.kt:227; A/chat/SkillsCatalog.kt:146. | A stale selection after changing files must not silently attach as if current; compare file name and text in picker. |
| C13 Extra keys and split focus | Shell's accessory row sends text to `KeyInsertDispatcher`; CodeEditor registers the mounted instance's handler and unregisters on disposal. READ A/ui/screens/ProjectShellScreen.kt:4720-4724; A/editor/KeyInsertDispatcher.kt:24-42; A/editor/CodeEditor.kt:1677-1708,1835-1850. | In split views, target must be the focused editor. Tap key after alternating split focus and inspect which view's buffer changed. |
| C14 Tests and performance | EditorPane merges test code lenses with LSP code lenses; CodeEditor sends edit, layout and frame events to `PerfProbe`. READ A/ui/panes/EditorPane.kt:1811-1858; A/editor/CodeEditor.kt:1673-1677,2318-2321,2595; A/editor/PerfProbe.kt:6-25. | Test runner owns execution, editor owns placement; performance group owns thresholds/measurement. |

## COMPARE: direct VS Code clone references

These comparisons are **architecture and behavior**, not a demand to copy desktop UI into a phone. Our host/guest Proot translation is an additional boundary that VS Code's normal single-URI workspace does not have. No cloned feature is treated as evidence that our Android app works on-device.

| Topic | VS Code read | Android read and contrast |
|---|---|---|
| Document vs widget | READ V/editor/browser/widget/codeEditor/codeEditorWidget.ts:506; V/editor/common/services/modelService.ts:84,440-457. `setModel` attaches a resource model to the widget; model service resolves models by resource. | READ A/editor/CodeEditor.kt:513,994; A/ui/panes/EditorPane.kt:2051-2053,2081-2092. `CodeEditor` remembers local text/undo and receives `EditorTab.content`; split views share tab content via parent rather than one model with multiple view widgets. |
| Dirty/save lifecycle | READ V/workbench/services/textfile/common/textFileEditorModel.ts:51-80,592-642; V/workbench/contrib/files/browser/editors/textFileEditor.ts:129. The text-file model emits content and dirty events separately. | READ A/ui/panes/EditorPane.kt:2081-2092,305-316. `onContentChange` marks dirty and writes disk in the same callback; see G01. |
| View and restoration | READ V/editor/browser/widget/codeEditor/codeEditorWidget.ts:1038-1052; V/workbench/browser/parts/editor/editorWithViewState.ts:167-188; V/workbench/browser/parts/editor/editorPane.ts:210-268. Widget saves/restores per-resource view state. | READ A/editor/EditorViewStateEffects.kt:64-100; A/data/SessionStateStore.kt:87-111,179-190. Captures offset/line/folds via Compose callbacks and preferences using string keys, including split IDs. |
| Diagnostics and decorations | READ V/platform/markers/common/markerService.ts:150-179; V/editor/common/services/markerDecorationsService.ts:24-57; V/editor/common/model/textModel.ts:1669. Marker changes are keyed by URI and drive model decorations. | READ A/diagnostics/DiagnosticPublisher.kt:22-46; A/editor/EditorDecorations.kt:15-42; A/ui/panes/EditorPane.kt:1360-1382,1550-1563. `DecorationStore` is **per mounted CodeEditor**, not a global singleton; LSP squiggles also live in EditorPane state and are URI-filtered with a basename fallback. |
| Undo scope | READ V/platform/undoRedo/common/undoRedoService.ts:505; V/editor/browser/widget/codeEditor/codeEditorWidget.ts:506. VS Code service owns edit history by resource. | READ A/editor/CodeEditor.kt:994,2943-2974; A/editor/undo/SnapshotUndoManager.kt:19-97. Whole-buffer snapshot undo is remembered per mounted view; switching views can change history ownership. |
| Find state | READ V/editor/contrib/find/browser/findController.ts:384,462; V/editor/common/model/textModel.ts:1669. Find controller operates on model. | READ A/editor/CodeEditor.kt:1869-1875,4725-4771; A/editor/EditorFindState.kt:12-52. EditorFindState stores only query/toggles; regex/match/replace run in CodeEditor and FindReplaceBar. It is **not** the search engine. |
| Multi-cursor | READ V/editor/contrib/multicursor/browser/multicursor.ts:455,664. Editor contribution exposes multicursor actions over editor selection state. | READ A/editor/CodeEditor.kt:2603-2623,4040-4085; A/editor/MultiCursorEngine.kt:29,122; A/editor/EditorSelectionStore.kt:16-43. Actual extra cursors are CodeEditor state, whereas EditorSelectionStore snapshots only selection for chat, not multicursor ownership. |
| Folding and visual lines | READ V/editor/contrib/folding/browser/folding.ts:68,191,295. Folding contribution has a restorable folding state/model. | READ A/editor/VisualLineMapper.kt:72,156-178,239-254; A/editor/CodeEditor.kt:2205-2234; A/data/SessionStateStore.kt:179-190. Android visual/doc line mapping, gutter toggle and persisted folded starts are separate concerns. |
| Format document | READ V/editor/contrib/format/browser/format.ts:290. Selected provider returns document edits. | READ A/editor/DocumentFormatter.kt:35-53,144-234; A/ui/panes/EditorPane.kt:1170-1282. Android provides an external formatter/fallback and a distinct LSP format action. Tool availability and Proot/host paths matter. |
| Local history | READ V/workbench/services/workingCopy/common/workingCopyHistoryService.ts:122. Working-copy history has resource-based entry API. | READ A/util/VersionHistoryV2.kt:22; A/ui/panes/ExplorerPane.kt:2359-2373; A/ui/panes/TimelinePanel.kt:256-265. Android's snapshot files are restored by copy; refresh across open editor and cache is a separate integration edge. |
| File invalidation | READ V/platform/files/common/fileService.ts:1175; V/workbench/services/textfile/common/textFileEditorModel.ts:57-80. File change events and model lifecycle are explicit. | READ A/editor/FileCache.kt:11-58; A/ui/panes/EditorPane.kt:2081-2092. The 20-entry path/mtime cache is manually invalidated by some writers; external copies may not invalidate it. |
| Brackets and highlights | READ V/editor/common/model/bracketPairsTextModelPart/bracketPairsImpl.ts:103; V/editor/common/model/textModelTokens.ts:105. VS Code maintains model-scoped bracket/token state. | READ A/editor/BracketPairConfig.kt:35,137-210; A/editor/IncrementalTmHighlighter.kt:32; A/editor/EditorDecorations.kt:15-42. Android uses language pair config and Compose-mounted decoration/highlight pipeline. |

## GAPS: source-confirmed integration risks, not device results

| ID / risk | Evidence and bounded conclusion | Required check / future owner |
|---|---|---|
| G01 critical, save/dirty divergence | `onContentChange` sets `isDirty=true` and `File(path).writeText(newText)` for absolute paths; catches write exceptions silently. Thus disk may already contain edits before Save, or disk may differ while UI is dirty; the Save menu announces success at shell entry before an observable disk result. READ A/ui/panes/EditorPane.kt:2081-2092,305-316; A/ui/screens/ProjectShellScreen.kt:1176. | T01/T18, compare editor buffer, disk and dirty indicator; a safe buffered working-copy policy would require separate design approval. |
| G02 high, restore refresh path mismatch | History restorers discard staged edits, copy a snapshot and call `bumpExternalRestore()`; that function increments `appliedTick` but does not add restored file to `lastAppliedPaths()`. EditorPane refreshes only `lastAppliedPaths()` and swallows read failures. Hence the tick alone does not identify the restored file. READ A/ui/panes/ExplorerPane.kt:1501-1528,1692,2359-2373; A/ui/panes/TimelinePanel.kt:256-265; A/chat/PendingChangesStore.kt:302-308; A/ui/panes/EditorPane.kt:235-250. | T37 below, compare disk/open tab after restore, including when no AI Apply has run. History/Editor integration owner. |
| G03 high, alias-key split | `EditorBufferStore` and FileCache are keyed by input path string; a navigation helper canonicalizes candidate paths and falls back to suffix matching, while view IDs and saved state use path strings. A guest/host or `/sdcard` alias can therefore refer to one file under distinct keys, and identical suffixes can resolve ambiguously. READ A/editor/EditorBufferStore.kt:14-34; A/editor/FileCache.kt:27-58; A/ui/panes/EditorPane.kt:2157-2209,754-789; A/terminal/ProotInstaller.kt:85,121. | T38 with two duplicate basenames and alternate host/guest path; canonical path per file and model-state store is the planned architectural direction, not implemented by this document. |
| G04 medium, server decoration scope | EditorPane holds one `lspSquiggles` list and clears it on active path changes; diagnostic matching also permits `diagFile == ourFile` basename. `DecorationStore` is created with `remember` inside each mounted CodeEditor, not globally or canonically file-keyed. READ A/ui/panes/EditorPane.kt:419,1360-1382,1550-1563; A/editor/EditorDecorations.kt:15-42; A/ui/panes/EditorPane.kt:2051-2053. | T27/T28 with two same-name files and tab switches; planned per-file canonical store. No claim of observed stale squiggles. |
| G05 medium, viewport/navigation identity | Local `scrollToLine` and `lspSquiggles` are remembered in EditorPane and reset/consume on specific effects; incoming shell jump target is separate state. Multiple writers convert lines. READ A/ui/panes/EditorPane.kt:419,484-490,2157-2209; A/ui/screens/ProjectShellScreen.kt:1518-1522,1622-1630,4642-4653. | T14/T15 and Problems/Search/Output link cases; record actual file and displayed line, not just whether a jump occurred. |
| G06 medium, view-specific undo | `SnapshotUndoManager` is `remember` in CodeEditor, which is keyed on `activeId`; each split gets a new view ID despite shared tab content. READ A/editor/CodeEditor.kt:994; A/ui/panes/EditorPane.kt:2051-2053; A/editor/SplitViewStore.kt:39-72. | T39: edit in view A, undo in view B and after tab switch; define expected semantics from buffer ownership before changing code. |
| G07 medium, cache/external write | FileCache checks `lastModified()` and raw path keys; EditorPane invalidates on typing/format but restore uses `copyTo` and does not call `FileCache.invalidate`. READ A/editor/FileCache.kt:27-58; A/ui/panes/EditorPane.kt:2081-2092; A/ui/panes/ExplorerPane.kt:2368-2370; A/ui/panes/TimelinePanel.kt:260-263. | T37: compare re-open content against disk when mtime resolution is coarse or paths are aliased. |
| G08 medium, find-state scope | `EditorFindState` stores the last query and toggles in a global preferences file, not keyed by file or project. READ A/editor/EditorFindState.kt:12-52; A/editor/CodeEditor.kt:1869-1875; A/editor/EditorViewStateEffects.kt:107-115. | T40 across files/projects: ensure the shared last-query behavior is acceptable; do not describe it as file-specific persistence. |
| G09 medium, formatter outcome | Format-on-save/menu, direct formatter, selection formatter and LSP action are separate callbacks with different write paths; direct disk writes in EditorPane catch and discard exceptions. READ A/ui/panes/EditorPane.kt:263-308,1170-1282. | T16-T18 compare chosen action to result and disk; report a failure instead of interpreting a notification as proof of a write. |

### Cross-boundary tap tests to add to T01-T36

| Case | Exact procedure and expected criterion | Source |
|---|---|---|
| T37 G02/G07 | In a scratch project append `// version one` to `EditorProbe.kt`; wait at least one 20-second capture cycle, long-press the file in Explorer, tap `Local History`, and verify the latest snapshot shows version one. Close the dialog, change the line to `// version two`, long-press the file and open `Local History` again, then tap `Restore` on the previously verified version-one snapshot. Check the open editor text **and** disk content, then close/reopen and compare again. Repeat using Timeline restore. Pass only if both display `version one` immediately and after reopen; log both values if different. | READ A/ui/panes/ExplorerPane.kt:1501-1528,1692,2359-2373; A/ui/panes/TimelinePanel.kt:256-265; A/chat/PendingChangesStore.kt:302-308; A/ui/panes/EditorPane.kt:235-250. |
| T38 G03/G04 | Create `left/Twin.kt` and `right/Twin.kt` with visibly different first lines (`// LEFT`, `// RIGHT`). Open both; trigger a Search result for each, and if Proot maps a second spelling of the same path, open by guest path too. Pass only if each jump lands in the intended buffer and any squiggle/scroll/bookmark stays with its exact file. | READ A/ui/panes/ExplorerPane.kt:3175-3177; A/ui/panes/EditorPane.kt:1360-1382,2157-2209; A/terminal/ProotInstaller.kt:85,121. |
| T39 G06 | Open scratch file, tap `Split Editor`, add `X` in first view, switch to the second view and tap `↩` Undo; then switch away to `Peer.kt` and back and tap `↩` again. Record whether both views show the same buffer and which edits each undo removes. Pass only if undo is coherent and no unrelated file changes; this is a behavior-discovery case, not a claim of model-level undo parity. | READ A/ui/screens/ProjectShellScreen.kt:4577-4601; A/ui/panes/EditorPane.kt:2051-2053; A/editor/CodeEditor.kt:994,1690-1708. |
| T40 G08 | In `EditorProbe.kt` open Find and type `needle`, turn on `Aa`; close Find, switch to `Peer.kt`, reopen Find, then relaunch project and reopen Find. Record whether query/toggle persist globally; expected by current source is last query and toggle in both files after persistence. Fail if toggle display and match behavior disagree. | READ A/editor/EditorFindState.kt:12-52; A/editor/EditorViewStateEffects.kt:107-115; A/editor/CodeEditor.kt:1869-1875. |
| T41 C07 | In scratch file introduce a parser error, open Problems, tap its entry, check selected file/line and editor squiggle. Switch to `Peer.kt` and inspect Explorer problem badge. Pass only if all surfaces agree on file/line and marker does not follow the active tab. | READ A/ui/panes/EditorPane.kt:1360-1382,2090-2093; A/ui/panes/ExplorerPane.kt:266; A/project/TaskRunner.kt:90-92. |
| T42 C12/C13 | Select only `needle` in `EditorProbe.kt`, open chat attachment picker and choose `Attach current editor selection`; confirm displayed file and selected text. Then alternate split focus, tap `{` in accessory row and confirm insertion goes to focused view's shared buffer once. | READ A/editor/CodeEditor.kt:2339-2342; A/ui/screens/ChatAttachPicker.kt:227; A/ui/screens/ProjectShellScreen.kt:4720-4724; A/editor/KeyInsertDispatcher.kt:24-42. |
| T43 E05 | Put caret on line 4 in scratch file, press hardware Alt+Up and Alt+Down in order. Expect moving the same line up one row then back with content intact; fail if selection/caret attaches to an unrelated line. | READ A/editor/KeyBindingRegistry.kt:135-139; A/editor/CodeEditor.kt:3044-3095. |

## Scope ledger

The Editor group is the mounted editing surface, model/view state, direct editing controls, and crossings above. Full protocol/backend verification remains for **Explorer and file lifecycle** (READ A/ui/panes/ExplorerPane.kt:224), **Tabs and split/window shell** (READ A/ui/screens/ProjectShellScreen.kt:4633), **Search/Navigation** (READ A/ui/panes/ExplorerPane.kt:2903), **Problems/Build/Tasks** (READ A/project/TaskRunner.kt:90-92), **LSP/IntelliSense/Testing**, **Debugger**, **Source Control/History/Trash**, **Chat/AI and settings**, **Terminal/Proot**, **Performance**, plus **Project/workspace restore** (READ A/data/SessionHandoffManager.kt:25,66). That last group is explicit because it owns root and persisted session identity; it must not be silently folded into a tab UI test. This ledger allocates work without reporting any of those groups as audited.
