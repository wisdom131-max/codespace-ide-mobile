# Debugger: fresh source-only group audit

**State:** app and checked-out VS Code clone read; no device test run. `READ A/path:N` is 1-based in `android/app/src/main/java/com/codespace/ide/`, and `READ V/path:N` is 1-based in `vscode-src/src/vs/`. This group owns the debug pipeline end to end: `UniversalDebugManager` (sessions, state machine, breakpoint store, threads/watch models), `DAPClient` wire protocol, the two DAP adapters (PythonDAPAdapter/debugpy, NodeDAPAdapter/js-debug), `LegacyDebugAdapter` + the seven legacy providers, `DebugConfiguration`, `DebuggerDependencies`, and the debug UI surfaces (ExplorerPane Run & Debug view, gutter dots/rings, debug band, hover evaluate, DebugConsolePanel, DebugToolbarOverlay, AttachDebugDialog, VariableInspectorPanel, DebugEditDialogs, BreakpointConditionDialog, DebugConsoleSection). Editor squiggle/selection internals, LSP server lifecycle, the Problems store, and Terminal process management are boundaries. Nothing here is a device outcome or a code-fix authorization.

## Feature inventory

| ID | Source-traced feature |
|---|---|
| DB01 | Session core: `DebugSession` model, 10-state `DebugState` machine with a validated transition table (P27-3 `isValidTransition`/`transitionState` logging illegal moves), ConcurrentHashMap sessions/breakpoints/sessionAdapters, active-session tracking (P27-7), multi-listener callback lists (P26-1) with backward-compat var setters. READ A/debug/UniversalDebugManager.kt:30-75,295-310,603-614. |
| DB02 | Adapter resolution: DAP adapters first (PythonDAPAdapter, NodeDAPAdapter registered in init), legacy `DebugProvider` fallback wrapped in `LegacyDebugAdapter`, last-resort no-op adapter (P26-2d). READ A/debug/UniversalDebugManager.kt:212-232,317-331; A/debug/DebugAdapter.kt:96-132. |
| DB03 | Launch handshake: async front-door `startDebugAsync` (DEBUG-ANR FIX, IO dispatcher + main-thread result), initialize/launch/initialized-latch ordering (P32-DAP-ORDER: setBreakpoints strictly AFTER the `initialized` event, then configurationDone), caps negotiation via `toDAPCapabilities`, per-adapter install self-heal (debugpy/js-debug) with surfaced output tails (P54-DEBUGPY). READ A/debug/UniversalDebugManager.kt:352-371; A/debug/PythonDAPAdapter.kt:139-360; A/debug/NodeDAPAdapter.kt:185-445. |
| DB04 | PythonDAPAdapter: debugpy presence check + install, guest path mapping (`hostToGuestPath` + filesDir `/host-files/` fallback), spawn `python3 -m debugpy.adapter` via `IdeEnvironment.forSubprocess` with P32 profile-noise suppression, stopped/terminated/exited event wiring with exit-code capture (P27-10), stack+variables fetch on an IO thread per pause. READ A/debug/PythonDAPAdapter.kt:139-230,431-517. |
| DB05 | NodeDAPAdapter: js-debug install + `findDapServerPath` (require-resolve with find fallback), spawn `dapDebugServer.js --stdio`, attach mode (port 9229 default or PID, `localRoot`/`remoteRoot`), TS launch via `ts-node/register` runtimeArgs + sourceMaps, thread event logging. READ A/debug/NodeDAPAdapter.kt:117-262,444-476. |
| DB06 | DAPClient wire protocol: Content-Length framing in both directions (UTF-8 byte-counted headers), per-request-seq pending queues with timeout, event handler map, `sendRequest` fire-and-forget, daemon reader + stderr-drain threads (P26-2a). READ A/debug/DAPClient.kt:26-135. |
| DB07 | Breakpoint store: per-file list of `DebugBreakpoint` (0-based line, condition, logMessage, hitCondition, enabled, verified, message), add/remove/toggle/enable/setCondition/edit with listener notify, `sendBreakpointsToActiveSession` live push on add/remove/toggle/setCondition (P32-BREAKPOINT-FIX), long-press gutter editor (CW3) via `BreakpointConditionDialog`. READ A/debug/UniversalDebugManager.kt:889-963; A/ui/panes/EditorPane.kt:2071,2148-2153; A/ui/panels/BreakpointConditionDialog.kt:42-118. |
| DB08 | Verification: `markBreakpointsVerified` from setBreakpoints responses (P27-11, DAP 1-based line mapping), function-breakpoint verification via `markFunctionBreakpointsVerified` (P2-FUNCBP). READ A/debug/UniversalDebugManager.kt:903-920,843-860; A/debug/PythonDAPAdapter.kt:328-342. |
| DB09 | Function breakpoints: in-memory `DebugFunctionBreakpoint` list, add/remove/toggle + push to live sessions, `AddFunctionBreakpointDialog`, verified/message rendering (tint + inline message). READ A/debug/UniversalDebugManager.kt:781-842; A/ui/panes/DebugEditDialogs.kt:125-168; A/ui/panes/ExplorerPane.kt:3730-3745. |
| DB10 | Threads/paging/restartFrame (P2): `getThreads`/`switchThread` (returns refreshed frames), `loadMoreFrames` (20-frame pages, totalFrames tracking), `restartFrame` gated on `supportsRestartFrame`; UI threads section + Load More in the debug view. READ A/debug/UniversalDebugManager.kt:748-780; A/debug/PythonDAPAdapter.kt:364-404; A/ui/panes/ExplorerPane.kt:3481-3530. |
| DB11 | Variables: scope fetch (3-scope cap, `expensive` scopes skipped at pause per VS Code lazy-load parity, P1-D5), child expansion by `variablesReference` (`getChildVariables` + ExplorerPane `expandedVarChildren`), `setVariable` (P1-D3, capability-gated) with `EditVariableDialog`, containerRef bookkeeping for nested sets. READ A/debug/PythonDAPAdapter.kt:498-579; A/debug/UniversalDebugManager.kt:686-728; A/ui/panes/ExplorerPane.kt:3487-3525; A/ui/panes/DebugEditDialogs.kt:90-124. |
| DB12 | Exception filters (P1-D4): capabilities expose `exceptionBreakpointFilters`, defaults applied lazily per session and pushed after configurationDone, `setExceptionBreakpoint` toggles push `setExceptionBreakpoints`; filter UI in the debug view. READ A/debug/DAPClient.kt:178-205; A/debug/UniversalDebugManager.kt:730-747,825-842; A/debug/PythonDAPAdapter.kt:351-357. |
| DB13 | Debug consoles/REPL: PSS `DebugConsolePanel` (multi-session switcher P26-4c, attach entry for JS/TS, colour-coded transcript, input routed via `sendInput` → DAP evaluate or legacy stdin), ExplorerPane `consoleLines` REPL through the extracted `DebugConsoleSection` (P1-D1, 64KB-rule file). READ A/ui/screens/ProjectShellScreen.kt:3939-4115,3566-3606; A/ui/panes/DebugConsoleSection.kt:38-96. |
| DB14 | UI surfaces: ExplorerPane Run & Debug view (start/continue/pause/step/restart/stop controls, VARIABLES/WATCH/CALL STACK/BREAKPOINTS sections, per-row jump/edit/remove, enable-toggle via dot tap), floating `DebugToolbarOverlay` (resume/pause/step gated on paused, stop), `AttachDebugDialog` (IO-thread attach, port/PID), `DebugConfiguration` dropdown (P27-9), `DebuggerDependencies` batch install with the LSP server start (P54-DEBUG-DEPS, per-process memo on success OR failure). READ A/ui/panes/ExplorerPane.kt:3199-3530,3360-3455; A/ui/screens/DebugToolbarOverlay.kt:44-110; A/ui/panes/AttachDebugDialog.kt:37-230; A/debug/DebuggerDependencies.kt:22-86; A/debug/DebugConfiguration.kt:11-52. |
| DB15 | Debug band + hover: `debugCurrentLine` paused-band with the DEBUG-CRASH FIX guard (only when the paused frame's file matches the open file; cleared on session end) and the [BAND-DIAG] off-by-one evidence chain (toggle line, DAP raw line, band decision); hover-evaluate on the word under the cursor (350ms debounce, `evaluateOnActiveSession` gated on PAUSED + `supportsEvaluateForHovers`, IO thread). READ A/ui/panes/EditorPane.kt:491-517,1625-1642; A/editor/EditorBreakpointDot.kt:32-42. |
| DB16 | Lifecycle tracking: exit-code capture → CRASHED vs STOPPED (P27-10), `notifyDebugEvent` on session end, `ProcessTracker` (P27-6 track/untrack/cleanupOrphanedProcesses), `restartSession` (stop → poll → restart with preserved breakpoints), legacy providers for pdb/node-inspect/shell/php/android/apk. READ A/debug/UniversalDebugManager.kt:178-206,466-498,569-597,1012-1485. |

## Cross-group connection ledger

| Edge | Producer -> consumer and seam |
|---|---|
| C-DB01 | LSP group: `DebuggerDependencies.ensure` is called from LspManager.startServer in the same install batch; both DAP servers and language servers spawn through `IdeEnvironment.forSubprocess` with the shared P32 profile-noise suppression — any change to that path affects both pipelines. READ A/debug/DebuggerDependencies.kt:22-45. |
| C-DB02 | Proot group: guest path mapping (`ProotInstaller.hostToGuestPath`, filesDir `/host-files/` prefix) is duplicated per adapter at launch (converted) and per live-update (NOT converted — DG02); install self-heal runs pip/npm inside the rootfs. READ A/debug/PythonDAPAdapter.kt:163-177,316-322; A/debug/NodeDAPAdapter.kt:213-247. |
| C-DB03 | Editor group: gutter taps/long-press feed `onBreakpointToggle`/`onBreakpointLongPress` (EditorPane:2137-2153); the band and hover-evaluate read the paused event stream; the band file-match compares basenames only (DG11) while the group's BUG-A fix normalized paths elsewhere. READ A/ui/panes/EditorPane.kt:491-517,2133-2153. |
| C-DB04 | Tabs group: the active tab path drives language detection for the Debug button; debug call-stack jumps land through `scrollToLine`/`onOpenFileAtLine` — the VARIABLES-tab variant of that chain drops the line and mismaps guest paths (DG09). READ A/ui/screens/ProjectShellScreen.kt:1141-1147,3713-3717. |
| C-DB05 | Problems group: PB14's canonical jump chain (guest→host, canonical tab matching) is the correct pattern the debugger's own jump wiring should reuse; [BAND-DIAG] and [DAP] lines flow through the shared AppOutputLog/Output tab. READ A/ui/screens/ProjectShellScreen.kt:3537-3553; A/debug/PythonDAPAdapter.kt:99-101. |
| C-DB06 | Notifications group: `NotificationStore.notifyDebugEvent` on session end and the debug start/stop notifications; the DEBUG-ANR fix route exists only for two callers (DG05). READ A/debug/UniversalDebugManager.kt:488-494; A/ui/screens/ProjectShellScreen.kt:1141-1152. |
| C-DB07 | Build group: startDebug's synchronous proot checks are the same class as BuildRunner's — but BuildRunner at least keeps its runner on a coroutine; the sidebar debug button and both restart entry points block the main thread (DG05). READ A/build/BuildRunner.kt:108-160; A/ui/panes/ExplorerPane.kt:3375,3430. |
| C-DB08 | PLAN A per-file canonical store: the debugger's breakpoint state exists in TWO representations (UDM map + EditorPane local `fileBreakpoints`) that diverge (DG08); any canonical-store design must make UDM the single breakpoint source for the gutter. READ A/ui/panes/EditorPane.kt:479,2135-2140. |
| C-DB09 | LSP protocol-family: DAPClient's dispatch has no "request" branch — the same protocol-completeness gap as LSP LS01 (server-initiated requests dropped without even an error response). READ A/debug/DAPClient.kt:118-130; A/lsp/JsonRpcClient.kt:244-285. |
| C-DB10 | Proot zombie family: stop paths destroyForcibly the proot process without a DAP disconnect or processTracker cleanup (DG13) — same orphan risk class as PR13's zombie gradle; trackProcess/untrackProcess have no adapter callers. READ A/debug/DAPClient.kt:68-71; A/debug/UniversalDebugManager.kt:188-206. |

## VS Code clone comparison

| Area | Verified VS Code owner | Android contrast |
|---|---|---|
| Breakpoint persistence | READ V/workbench/contrib/debug/common/debugStorage.ts:76-95. Breakpoints + function breakpoints are JSON-serialized to WORKSPACE storage, revived on load with URI revival; per-workspace and automatic. | READ A/debug/UniversalDebugManager.kt:943-993. App has the full save/load code (P23-8) but `saveBreakpoints` is NEVER called — MainActivity only calls `loadBreakpoints`, so the store is always empty at load. Persistence is a dead feature (DG01). |
| Enabled filter | READ V/workbench/contrib/debug/browser/debugService.ts:1287. `getBreakpoints({ originalUri, enabledOnly: true })` — only enabled breakpoints are ever sent to the adapter. | READ A/debug/PythonDAPAdapter.kt:58-96,320-330. App sends the whole per-file list on both the launch and live paths — disabled breakpoints included — and `setBreakpointEnabled` never pushes to the session (DG03). |
| Source path normalization | READ V/workbench/contrib/debug/browser/debugSession.ts:488-509. `sendBreakpoints` resolves the raw source (adapterData), normalizes drive letters going out, and only sends after `readyForBreakpoints`. | READ A/debug/PythonDAPAdapter.kt:58-96. App's live path sends raw HOST paths while the launch path sends GUEST paths — the adapter receives two different source identities for the same file (DG02). |
| Reverse requests | VS Code's debug session handles server→client requests (runInTerminal etc.) through the adapter manager and replies; READ V/workbench/contrib/debug/browser/debugSession.ts:1465 (session-driven resend) and debugProtocol in common/debugProtocol.d.ts. | READ A/debug/DAPClient.kt:118-130. App's dispatch has only "response" and "event" branches — a DAP reverse request is silently dropped with no response (DG07), the LS01 pattern on the debug channel. |
| Breakpoints view | READ V/workbench/contrib/debug/browser/breakpointsView.ts:2210 (Remove All Breakpoints context item) and :432-442 (unverified-breakpoint warnings tied to debugger strings). | READ A/debug/UniversalDebugManager.kt:1000-1004. App has per-row remove and enable-toggle, but `clearAllBreakpoints` has no caller (no Remove All UI), and the line-bp `verified`/`message` fields are parsed and stored but never rendered — only function breakpoints show verification (DG14). |
| Call stack view | READ V/workbench/contrib/debug/browser/callStackView.ts (session/threads grouping, frame paging with "Show More"). | READ A/ui/panes/ExplorerPane.kt:3481-3530. App matches the parity shape: threads section, per-thread frames, loadMoreFrames paging, restartFrame — a genuine parity point. |
| Watch expressions | READ V/workbench/contrib/debug/browser/debugCommands.ts watch command set + watch view — one service-owned watch store with add/remove/re-evaluate on pause. | READ A/ui/panes/VariableInspectorPanel.kt:62-189 and A/ui/panes/ExplorerPane.kt:3229-3246. App has TWO independent remember-scoped watch stores that don't share state or survive panel switching; UDM's `DebugWatch` model is used by neither (DG10). |
| REPL | READ V/workbench/contrib/debug/common/replModel.ts — one repl model with expression evaluation per frame, filter/zones. | READ A/ui/screens/ProjectShellScreen.kt:4066-4115 and A/ui/panes/DebugConsoleSection.kt:38-96. App has two consoles with separate transcripts and input state (debugMessages vs consoleLines); the PSS one routes through `sendInput` (evaluate/stdin), the Explorer one through its own echo+evaluate — divergent behavior over one session (DG10). |

## Fresh scratch-only device checks, NONE RUN

Keep scratch scripts: a python file with a loop/function, a multi-threaded python file, a node script, a TS file, a deep-recursion script, two same-basename files in different folders, and a .kt file. Record PASS/FAIL/NOT RUN with reason; BAND-DIAG and [DAP] Output-tab lines are the diagnostic channel.

| Test | Action -> pass criterion and diagnostic |
|---|---|
| DT01 DB03/DB15 | Set a bp on a loop line BEFORE Debug (python). Pass: stops there; band on the exact line (check [BAND-DIAG] paused line for the 0-based/1-based chain); stack + locals render. |
| DT02 DG02 | While that session is running (not yet stopped), add ANOTHER bp from the gutter. Record: does the debuggee stop at the new line? (predicted NO — host path sent on the live path). Capture the [DAP] setBreakpoints line. |
| DT03 DG02 | Remove the original bp while paused, resume. Pass: the debuggee does NOT stop at the removed line; record if it still does. |
| DT04 DG03 | Disable the bp via the Explorer dot-tap while paused, resume. Record: does it still stop at the disabled bp? (predicted YES). The Explorer row shows it dimmed/off while the session honors it. |
| DT05 DB07 | Edit the bp CONDITION while paused (gutter long-press → condition), resume with input that should skip it. Record whether the new condition is honored live (setBreakpointCondition DOES push, unlike enable). |
| DT06 DG01 | Set 3 bps, kill the app completely, relaunch, open the file. Record: gutter dots, Explorer BREAKPOINTS count (predicted all gone). |
| DT07 DG01 | Same after merely backgrounding/resuming the app (process alive). Record whether the in-memory store survives (predicted yes — persistence is memory-only). |
| DT08 DG04 | Open a .kt file, press the sidebar Debug button. Record: does a "session" appear RUNNING with controls, with only a "[terminal] Ready to run" line? (predicted phantom session). Check the VARIABLES panel state text. |
| DT09 DG05 | While a python session is paused, tap the Restart button in the Run & Debug view. Record: main-thread freeze duration, ANR dialog, and whether the new session comes up. |
| DT10 DG05 | Fresh install state (debugpy absent), press the sidebar Debug button on a .py. Record: UI freeze during the install window (predicted ANR-class, unlike the Run/Debug button path). |
| DT11 DG12 | Start two python debug sessions back-to-back (different files). Record: which session's client survives; do step controls act on the second file's session regardless of switcher choice? |
| DT12 DG06 | Force a launch failure (rename the file mid-debug or bad path), then open the multi-session switcher. Record: is the FAILED session listed and selectable? |
| DT13 DB05 | `node --inspect script.js` in the terminal, then Attach (port mode) from the Debug console. Pass: paused on bp, stack/variables render, stop ends cleanly. |
| DT14 DB05 | Attach by PID (`node --inspect` then Attach with PID). Record success/failure and error surfacing. |
| DT15 DB07/CW3 | Add a conditional bp (gutter long-press, fill condition). Pass: hollow ring renders in the gutter; re-open the dialog shows the stored values; edit clears with blank fields. |
| DT16 DB07 | Set a hitCondition ">=3" (edit dialog) BEFORE launch. Record: stops only on the 3rd hit or never (launch path omits hitCondition — predicted never until edited live). |
| DT17 DB07 | Log-only bp (logMessage, no condition). Pass: prints the interpolated message and does NOT stop; check the console transcript. |
| DT18 DB09 | Add a function bp on a known function, launch. Pass: stops at entry; verified tint/message appears; remove clears it live. |
| DT19 DB10 | Debug the multi-threaded python file. Pass: THREADS section lists both; switching threads changes the call stack; stepping follows the selected thread. |
| DT20 DB10 | Debug deep recursion (>20 frames). Pass: 20 frames render; Load More appends the next page; totalFrames is honest at the end. |
| DT21 DB10 | While paused mid-function, trigger restartFrame on a parent frame (js-debug). Record: re-run behavior and the refreshed stack. |
| DT22 DB11 | Pause with a dict/list local. Pass: expansion arrow fetches children (values correct), a second level expands too; non-expandable values show no arrow. |
| DT23 DB11 | Edit a variable's value via EditVariableDialog, then evaluate/print it in the console. Pass: the debuggee's state reflects the new value. |
| DT24 DB12 | Toggle the "caught" exception filter on, then raise a caught exception. Pass: stops at the raise; toggling off resumes normal flow. |
| DT25 DG09 | Open the VARIABLES bottom tab, tap a call-stack row. Record: does it open ANY file, the right file, at the right line? (predicted: line dropped + guest path joined to host root → wrong/no file). |
| DT26 C-DB04 | From the Run & Debug view, tap a call-stack row for a different file. Pass: correct file + line via the debug view's onJumpToSource wiring; compare with DT25. |
| DT27 DG11 | Create a.py and dir/b.py, bp in b.py only, open a.py as active tab, trigger the bp. Record: does the band render in a.py (basename match) — predicted wrong-file render. |
| DT28 DB15 | While paused, hover a variable word in the editor. Pass: `name = value` tooltip; while merely RUNNING, no evaluate calls occur (check Output tab [DAP] lines). |
| DT29 DB13 | While paused, evaluate expressions in BOTH consoles (bottom DEBUG tab and Run & Debug REPL). Record: identical results; input echo behavior differences. |
| DT30 DB13 | Debug via a legacy pdb session (context null path) and type stdin commands in the console. Pass: pdb responds (InteractiveDebugProvider stdin). |
| DT31 DB16 | Debug a script that exits with code 3. Pass: state CRASHED, "[debug] Process crashed (exit code 3)" in console, notification fires, session cleans up. |
| DT32 DG13 | While the debuggee sleeps (time.sleep loop), press Stop, then check the proot terminal (`ps aux | grep python`) for an orphaned debuggee. Record findings. |
| DT33 DB03 | Break the pip environment, fresh-start Debug on .py. Pass: install output tail shown in console, launch aborted with FAILED (no silent pdb fallback); retry works after fixing. |
| DT34 DB14 | Attach dialog: enter a dead port. Pass: onAttachFailed surfaces the reason in the console; no phantom session (check the switcher). |
| DT35 DG14 | Set a bp on a blank line and launch. Record the [DAP] response and whether ANY UI element distinguishes verified vs unverified for line bps (predicted none). |
| DT36 DB14 | Fresh install: open a .py (no debug tap). Record: debugpy installs in the LSP batch ([DEBUG-DEPS] lines), and a later Debug tap starts without an install wait. |

## Source-confirmed gaps for master fix-plan input

| Gap | Priority below owner's established co-top tier | Bounded source finding |
|---|---|---|
| DG01 | MEDIUM-HIGH, breakpoint persistence is dead | `saveBreakpoints` (P23-8) has NO callers anywhere — MainActivity:72 only calls `loadBreakpoints`, so the store is always empty at load and every breakpoint dies with the process. The save schema also omits `hitCondition` even if it were called, and `loadBreakpoints` swallows parse exceptions silently. VS Code persists per-workspace automatically. Misreports a feature (persistence) that never happens — S01 family. READ A/debug/UniversalDebugManager.kt:943-993; A/MainActivity.kt:72; V/workbench/contrib/debug/common/debugStorage.ts:76-95. DT06/DT07. |
| DG02 | MEDIUM-HIGH, live breakpoint updates can't bind | `PythonDAPAdapter.sendBreakpoints` sends `session.filePath`/`bp.filePath` RAW (host paths) as DAP `source.path` (lines 58-96), while `launch` converts via `hostToGuestPath`/`/host-files` (lines 316-322) — the adapter receives two different source identities for the same file, so live-added/removed/edited breakpoints cannot bind (removals equally ineffective). `NodeDAPAdapter.sendBreakpoints` carries a comment claiming the live path is "already a guest path (mapped at launch time)" — false: UDM keys the store by host paths. Launch also omits `hitCondition` while the live path sends it, and the live path never refreshes verification. READ A/debug/PythonDAPAdapter.kt:58-96,316-330; A/debug/NodeDAPAdapter.kt:74-110; A/debug/UniversalDebugManager.kt:280-293. DT02/DT03/DT16. |
| DG03 | MEDIUM, disabled breakpoints still fire | Neither `launch` nor `sendBreakpoints` filters `enabled == false`, and `setBreakpointEnabled` (UDM:931-938) never pushes to a live session at all — a disabled breakpoint keeps stopping the debuggee while the Explorer list shows it dimmed as off. VS Code sends `enabledOnly: true`. A placebo control in the misreport family. READ A/debug/PythonDAPAdapter.kt:58-96,320-330; A/debug/UniversalDebugManager.kt:931-938; V/workbench/contrib/debug/browser/debugService.ts:1287. DT04. |
| DG04 | MEDIUM, phantom debug sessions | `TerminalDebugProvider.launch` returns TRUE having done nothing ("[terminal] Ready to run X") — for any language without a real DAP adapter or provider (Kotlin, C, Go...), `resolveAdapter` falls back to it, UDM marks the session RUNNING, and the UI shows live-looking pause/step controls over a session that never existed. False-session pattern; the "No debugger" branch is only reached when even the fallback refuses. READ A/debug/UniversalDebugManager.kt:1012-1053,218-232; A/ui/screens/ProjectShellScreen.kt:1141-1152. DT08. |
| DG05 | MEDIUM, main-thread ANR paths remain | The DEBUG-ANR fix (startDebugAsync) covers only the PSS Run/Debug button and console Run. `restartSession` (stop + Thread.sleep poll up to 2s + synchronous startDebug with 10s proot check / 5-min install) is called directly in onClick at PSS:1217 and ExplorerPane:3430, and the sidebar Debug button calls synchronous `startDebug` at ExplorerPane:3375 — all on the main thread. Same ANR class the fix was made for, on unfixed callers. READ A/debug/UniversalDebugManager.kt:569-597,373-463; A/ui/panes/ExplorerPane.kt:3375,3430; A/ui/screens/ProjectShellScreen.kt:1217. DT09/DT10. |
| DG06 | MEDIUM, dead sessions pollute the active list | `getActiveSessions` filters only IDLE/STOPPED (UDM:557-558), and the launch-failure path leaves the FAILED session in the map forever (no removal) — the DebugConsolePanel multi-session switcher lists and selects FAILED/CRASHED/ERROR sessions, and state listeners keep processing them. Also the legacy launch=false path leaves sessionAdapters cleared but the session leaked in `sessions`. READ A/debug/UniversalDebugManager.kt:373-463,557-561; A/ui/screens/ProjectShellScreen.kt:3959-3990. DT12. |
| DG07 | MEDIUM, DAP reverse requests dropped | `DAPClient.dispatch` has no "request" branch — any server→client request (runInTerminal, launchBrowser, future debugpy/js-debug requests) is silently ignored with no response, hanging the server side. Declaring `supportsRunInTerminalRequest=false` narrows but does not eliminate this; no error response is ever sent. Same protocol-completeness family as LS01. READ A/debug/DAPClient.kt:118-130,135-163. DT13/DT14 (attach paths exercise js-debug's request behavior). |
| DG08 | LOW-MEDIUM, dual breakpoint representations diverge | EditorPane keeps its own `fileBreakpoints` remember map written ONLY by that instance's gutter taps (479,2135-2140), while UDM's store drives the Explorer list and the conditional rings (2146). Gutter dots don't reflect Explorer removals or any other source of truth, and a gutter tap on a line where only UDM has a bp toggles UDM OFF while the local map turns it ON. Split-editor instances each have their own map. PLAN A should make UDM the single source the gutter renders from. READ A/ui/panes/EditorPane.kt:479,2133-2153. DT06/DT08 adjacent. |
| DG09 | LOW-MEDIUM, variables-panel jump drops line and mismaps paths | The VARIABLES bottom tab's `onJumpToSource` ignores the `line` parameter entirely and joins the GUEST frame path onto the HOST project root (`File(projectRoot, file)`) — call-stack and breakpoint rows in that panel open a wrong/nonexistent file with no line. Should reuse PB14's canonical chain. READ A/ui/screens/ProjectShellScreen.kt:3713-3717. DT25. |
| DG10 | LOW-MEDIUM, two consoles and two watch stores | PSS `debugMessages` console and ExplorerPane `consoleLines` REPL are separate transcripts with separate inputs over the SAME session (different echo/evaluate behavior); `VariableInspectorPanel.watchExprs` and the Explorer debug view's watch are independent remember stores — neither survives panel switching, and UDM's `DebugWatch` model is used by neither. VS Code has one replModel and one watch service. READ A/ui/screens/ProjectShellScreen.kt:3939-4115; A/ui/panes/DebugConsoleSection.kt:38-96; A/ui/panes/VariableInspectorPanel.kt:62-189; A/ui/panes/ExplorerPane.kt:3229-3246. DT29. |
| DG11 | LOW-MEDIUM, basename-matched debug band | The paused-band guard compares the frame file's BASENAME to the active tab's BASENAME (EditorPane:491-517) — two same-named files in different folders make the band render on the wrong file, and guest/host path differences are papered over instead of resolved. Same basename-fallback family as LS06. READ A/ui/panes/EditorPane.kt:491-517. DT27. |
| DG12 | LOW, singleton adapter client field | PythonDAPAdapter and NodeDAPAdapter each hold ONE mutable `client` field on a single registered instance; a second session of the same language overwrites it — stop(A) kills B's client, events route to whichever callbacks registered last. The multi-session switcher UI (P26-4c) exists but the adapters cannot back it. READ A/debug/PythonDAPAdapter.kt:36,213-217,425-431; A/debug/NodeDAPAdapter.kt:50; A/debug/UniversalDebugManager.kt:317-331. DT11. |
| DG13 | LOW, stop path orphans debuggee + dead tracker | `stop()` sends terminate then `destroyForcibly()`s the proot process — the debuggee (a proot child) can be reparented inside the rootfs and keep running; no `disconnect` request is used, and `ProcessTracker` (P27-6) is DEAD: `trackProcess`/`untrackProcess`/`cleanupOrphanedProcesses` have no callers outside UDM (no adapter registers anything), and `stopSession` never untracks. Same zombie family as PR13. READ A/debug/DAPClient.kt:68-71; A/debug/PythonDAPAdapter.kt:425-431; A/debug/UniversalDebugManager.kt:178-206,466-498. DT32. |
| DG14 | LOW, remove-all dead + verification invisible + broadcast inconsistency | `clearAllBreakpoints` has no callers (VS Code ships Remove All Breakpoints in the breakpoints view context menu); line-bp `verified`/`message` are parsed and stored but rendered nowhere (only function bps show it); and `editBreakpoint` pushes ALL breakpoints for ALL files to EVERY live session (UDM:714-718) while `sendBreakpointsToActiveSession` pushes one file to the first active session — two different broadcast semantics in one store. READ A/debug/UniversalDebugManager.kt:700-718,1000-1004; A/ui/panes/ExplorerPane.kt:3730-3745; V/workbench/contrib/debug/browser/breakpointsView.ts:2210. DT35. |

**Boundary:** Editor owns gutter rendering internals, LSP owns server lifecycle and its install trigger, Problems owns the canonical jump chain, Terminal owns proot process semantics, Notifications owns action handling. The strongest rows are DG01 (a persistence feature that never runs — every breakpoint silently lost per restart), DG02 (live breakpoint updates structurally cannot bind after a session starts), and DG03+DG04 (placebo controls: disabled bps still fire; phantom sessions show live controls over nothing). All remain below the owner's co-top tier G01/TB01/TB03/EX04/EX05/EX07. Do not mark any DT case passed or implement a fix based on this audit alone.
