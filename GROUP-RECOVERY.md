# Recovery: fresh source-only group audit

**State:** app source read; no device test run — but ONE backend-side verification WAS possible and was performed: the app's crash telemetry posts to THIS Superagent's own `reportCrash` function, and the `CrashLog` store it writes was read directly (see RC06/RG01 — verified EMPTY). `READ A/path:N` is 1-based in `android/app/src/main/java/com/codespace/ide/`, `READ V/path:N` is 1-based in `vscode-src/` (repo root `src/vs/`). Scope per COVERAGE-CHECK.md (~1.8k lines): cloud project backup/restore + session handoff, the local rootfs container backup (the uninstall-survival system), workspace session state, the crash/safe-mode pipeline, app-shell startup, and the notification drawer. **Boundaries owned elsewhere:** FlowGate/AgentTools (Chat), ProotInstaller download path (Terminal TP04), Tabs' own close/dirty logic (TB), Debugger's breakpoint model (DG — recovery only wires `loadBreakpoints` at startup, MainActivity.kt:72), NotificationStore's settings persistence (Settings). Nothing here is a device outcome or a code-fix authorization.

## Feature inventory

| ID | Source-traced feature |
|---|---|
| RC01 | Crash-loop protection: `WorkspaceManager` recordLaunch heuristic (previous start <60s = crash, else counter resets to 0), recordStable after 60s uptime, threshold 3 → Safe Mode dialog that skips project auto-open + terminal auto-start, "Normal Mode" resets. READ A/util/WorkspaceManager.kt:35-58,63-76; A/MainActivity.kt:69-78,125-150. |
| RC02 | Startup routing: `startDest` computed ONCE in `remember` (signed-out → AUTH, lastProjectId → project route, else HOME) — NavHost graph-stability guard; accessToken kept in memory only; PIN lock shown once per process launch. READ A/ui/CodeSpaceApp.kt:92-108,111-112. |
| RC03 | Startup store-init ordering: JsonSettingsStore first (other stores delegate), feature toggles, project settings, chat stores, find state, key pool, plan store, image prune, TextMate, NotificationStore + action-handler registration (view_logs/restart), usage tracker, keybindings. READ A/CodeSpaceApplication.kt:30-66. |
| RC04 | JVM crash logger: default UncaughtExceptionHandler writes timestamped file to filesDir/crash_logs BEFORE letting the crash proceed; upload off-thread with 4s timeouts; P32 COMPOSE_CONCURRENT_CHANGE enrichment logs state-object sizes (AppOutputLog, NotificationStore). READ A/CodeSpaceApplication.kt:150-203. |
| RC05 | Native crash handler: signal crashes (SIGSEGV/SIGABRT/SIGBUS/SIGILL/SIGFPE) — invisible to JVM handlers — written to filesDir/crash_logs via async-signal-safe writes (see pty_native.c native_crash_handler); MainActivity reads ALL crash_logs files (JVM + native) and uploads. READ A/CodeSpaceApplication.kt:96-101; A/MainActivity.kt:197-206. |
| RC06 | Crash upload endpoint: POST to `https://superagent-4a7af576.base44.app/functions/reportCrash` — UNAUTHENTICATED, 4s timeouts, response ignored; payload = package/model/version/git_hash/thread/stack/timestamp. **Backend verified (2026-09-24): the CrashLog store this function writes is THIS Superagent's own entity table and contains ZERO records.** READ A/CodeSpaceApplication.kt:205-248. |
| RC07 | SessionStateStore: per-project workspace memory — shell state (open/pinned/active/split files, panels, font size), per-file cursors + scrolls, splits, locks, folds, blame toggle, terminal cwd + last 50 commands; decode paths try/catch → null = fresh start; global restore toggle; per-project and global clears. READ A/data/SessionStateStore.kt:17-133. |
| RC08 | Workspace restore wiring: loadShellState(projectId) seeds ProjectShell; saveShellState on change; EditorPane restores cursor offsets, scroll, splits from the store and saves them back on change. READ A/ui/screens/ProjectShellScreen.kt:730,917; A/ui/panes/EditorPane.kt:594-612,771-777. |
| RC09 | Session handoff: exportSession/importSession full-session JSON blob (shell/cursors/scrolls/terminal history); push/pull to backend `/api/session/:projectId` (Bearer auth). READ A/data/SessionHandoffManager.kt:24-60,110-157; A/ui/panels/CloudBackupPanel.kt:123. |
| RC10 | Cloud project backup: tar.gz the project dir → multipart POST to backend `/api/backup/upload`; retryNetwork = 3 attempts, 1/3/7s backoff, onAttempt progress surfaced to UI; temp archive in cacheDir deleted in finally; resolveProjectDir 3-step resolution (direct id → SharedPreferences name lookup → single-subdir fallback). READ A/project/CloudBackupManager.kt:36-77,86-146,204-224. |
| RC11 | Cloud project restore: download tar.gz → HAND-ROLLED extractTarGz into filesDir/projects — header name (100 bytes) + octal size read raw, `File(destDir, name)`, no checksum, no entry-name validation, size → `ByteArray(size.toInt())`. READ A/project/CloudBackupManager.kt:148-190,246-290. |
| RC12 | Hand-rolled tar writer: ustar subset — 512-byte headers, octal size field, checksum computed over zeros then written; names truncated to 100 bytes; end-of-archive 2 zero blocks. READ A/project/CloudBackupManager.kt:226-244. |
| RC13 | CloudBackupPanel UI: list backups, backup with retry progress, restore, session push/pull; authToken falls back to empty string (`?: ""`). READ A/ui/panels/CloudBackupPanel.kt:44-123; A/ui/screens/ProjectShellScreen.kt:3652-3663. |
| RC14 | Rootfs container backup (the uninstall-survival system): commons-compress tar.gz of the whole rootfs — symlink entries with targets preserved, exec bits, GNU long-file + star big-number modes, proc/sys/dev skipped, tmp file + atomic rename, progress every 500 files, NotificationStore record; triggered ONLY from a Settings button. READ A/terminal/BackupManager.kt:46-119; A/ui/screens/SettingsScreen.kt:379-380. |
| RC15 | Rootfs restore: on first-time install, if a shared-storage backup exists, restore INSTEAD of downloading a fresh rootfs; restore wipes rootfs FIRST (deleteRecursively), per-file failures swallowed with Log.w and the loop continues, returns true unless the file is missing; TerminalPane prints "✓ Restored from backup!" WITHOUT checking the return value. READ A/terminal/BackupManager.kt:124-170; A/ui/panes/TerminalPane.kt:895-906. |
| RC16 | Prefs backup/restore alongside container backup: copies 3 prefs XMLs (projects, copilot_chat, main) + agent memory.json to/from /sdcard/CodespaceIDE/prefs-backup/; restore runs during first-time install. READ A/terminal/BackupManager.kt:173-203; A/ui/panes/TerminalPane.kt:901. |
| RC17 | Notification drawer overlay: bell (unread count, DND state), toast banner (activeToast), drawer with severity filter, all items rendered from the bounded store (max 50), action buttons (view_logs/restart). READ A/ui/screens/NotificationDrawerOverlay.kt:77-125,241-255; A/CodeSpaceApplication.kt:61-65. |
| RC18 | Workspace snapshots: createSnapshot zips the project dir to a timestamped .zip in Downloads (create-only — no restore path exists). READ A/util/WorkspaceManager.kt:83-100; A/ui/screens/ProjectShellScreen.kt:1116. |

## Cross-group connection ledger

| Edge | Producer -> consumer and seam |
|---|---|
| C-RC01 | TB03 family: restoreBackup wipes the rootfs BEFORE extraction begins and then reports completion — the same delete-then-restore-with-false-success shape as the Tabs backup deletion chain; RG02 is Recovery's member. READ A/terminal/BackupManager.kt:135-137,167. |
| C-RC02 | Tabs: openFilePaths/pinned/split restore feeds the Tabs group directly; the known rebind requirement (ProjectPathResolver must be manually triggered after session restore) lives at this seam. READ A/ui/panes/EditorPane.kt:594-612. |
| C-RC03 | Integration IG03: prefs-backup (RC16) does NOT include agent_scheduler/tasks.json — even if restoreAll were wired, scheduled tasks still die with an uninstall; ssh-profiles.json, session_state, and JsonSettingsStore-backed settings are also outside the 4-store list. READ A/terminal/BackupManager.kt:181-183. |
| C-RC04 | Settings SK01/Integration IG16: createBackup's tmp+rename (RC14) is the gold-standard atomic write the whole SK01 batch should copy; restorePrefs' raw XML file-copy under a live process is the anti-pattern (RG06) — VS Code never copies raw store files while the process holds them. READ A/terminal/BackupManager.kt:57,68,119. |
| C-RC05 | EX05/IG01 path-escape family: extractTarGz trusts archive entry names (`File(destDir, name)`) and resolveProjectDir trusts a SharedPreferences-sourced project name (`File(projectsBase, name)`) — two more unsanitized File(parent, controlledString) sinks (RG03). READ A/project/CloudBackupManager.kt:271-272,216. |
| C-RC06 | Terminal TP04: the auto-restore path (RC15) bypasses ProotInstaller's rootfs download entirely — no checksum/integrity validation exists on the restored archive; combined with RG07's public-storage location, the strongest trust surface in the app (the container) accepts an unvalidated input. READ A/ui/panes/TerminalPane.kt:895-906. |
| C-RC07 | Chat/Settings: copilot_chat.xml survives uninstall (RC16), but JsonSettingsStore (editor settings, keybindings, AI keys) is NOT in the prefs-backup list — Keystore-backed SecureTokenStore values are unrecoverable by design (Keystore wipes on uninstall), but the plain-store settings loss is a coverage gap (RG05). |
| C-RC08 | Debugger: startup restores persisted breakpoints (P23-8) via UniversalDebugManager.loadBreakpoints before any project loads — the one debugger-owned state Recovery wires. READ A/MainActivity.kt:72. |
| C-RC09 | Terminal TP01 posture: the crash endpoint is an UNAUTHENTICATED data-writing backend function (world-postable — anyone can forge CrashLog records); low-value target, but it is the same "no auth on a mutating endpoint" shape TP01/TP02 documented, now on the Superagent side. READ A/CodeSpaceApplication.kt:205. |
| C-RC10 | Notifications: BackupManager writes BACKUP notifications into NotificationStore which the drawer renders; P32's crash-context enrichment reads NotificationStore size — the drawer is both the recovery UX and a crash-diagnostic readout. READ A/terminal/BackupManager.kt:117,167; A/CodeSpaceApplication.kt:226-232. |

## VS Code clone comparison

| Area | Verified VS Code owner | Android contrast |
|---|---|---|
| Unsaved-work backup timing | READ V/src/vs/workbench/services/workingCopy/common/workingCopyBackupService.ts:119,188 — WorkingCopyBackupService writes hash-keyed backup files continuously during editing, so a crash loses nothing. | READ A — no between-edit content backup exists; dirty content survives only in memory until a save, and the whole-container backup (RC14) fires only when the user taps a Settings button. |
| Backup integrity/manifest | READ V/src/vs/workbench/services/workingCopy/common/workingCopyBackupService.ts:119 — backups are keyed by resource + version, with hasBackups/discard APIs; restore knows exactly what it owns. | READ A/project/CloudBackupManager.kt:226-290 — unmanifested hand-rolled tar, no checksum on restore, per-file failures invisible (RG02/RG03/RG04). |
| Shutdown ordering | READ V/src/vs/workbench/services/lifecycle/electron-browser/lifecycleService.ts:41,89,120 — onBeforeShutdown fires, vetoes are COLLECTED, shutdown WAITS for pending saves and warns on stall. | READ A/terminal/BackupManager.kt:135-137 — the destructive step (rootfs deleteRecursively) runs FIRST, before the first byte is extracted: the exact inverse of the veto pattern. |
| Editor session restore | READ V/src/vs/workbench/browser/parts/editor/editorPart.ts:100 (IEditorPartMemento) + V/src/vs/workbench/common/editor/editorGroupModel.ts:185,227,1163 — per-group serialize/deserialize memento restored on reopen. | READ A/data/SessionStateStore.kt:17-133 — the same shape done compactly: open files, cursors, scrolls, splits, locks, folds per project, decode-defensive (null = fresh). An honest parity point (RC07/RC08). |
| Crash-loop recovery UX | READ V/src/vs/workbench/services/extensionManagement/browser/extensionBisect.ts:67 — ExtensionBisectService binary-search-disables extensions to recover an unstable window. | READ A/util/WorkspaceManager.kt:35-58,63-76 — the app's Safe Mode is the same idea implemented natively: crash heuristic + threshold-3 dialog that skips auto-open/auto-start. Real and wired (RC01), a strength. |
| Notifications triad | READ V/src/vs/workbench/browser/parts/notifications/notificationsCenter.ts:38 + V/src/vs/workbench/browser/parts/notifications/notificationsToasts.ts:47 — a persistent center AND transient toasts on one store. | READ A/ui/screens/NotificationDrawerOverlay.kt:77,122,241 — the same triad (bell, toast banner, drawer) on one bounded store (max 50) with severity filters and action buttons. Parity point (RC17). |
| Storage durability under a live process | READ V/src/vs/workbench/services/storage/browser/storageService.ts:22 + V/src/vs/platform/storage/common/storage.ts:326 — BrowserStorageService flushes on a controlled interval through ONE owned API; raw store files are never swapped under the process. | READ A/terminal/BackupManager.kt:186-203 — restorePrefs copies raw XML into shared_prefs while the process may already hold those SharedPreferences: the loaded instance neither sees nor keeps the restore (RG06); SessionStateStore itself is fine. |
| Terminal revival | READ V/src/vs/workbench/contrib/terminal/browser/terminalService.ts:486-523 — detached background terminal instances are REVIVED from the pty host across restarts. | READ A/data/SessionStateStore.kt:115-133 — TerminalMemory persists only cwd + last 50 commands: honest cosmetic continuity, no revival claim (RG11, additive). |

## Device checks (all pending)

| Check | What to verify on device |
|---|---|
| RT01 | Crash counter: force-kill the app <60s after launch 3 times → Safe Mode dialog appears with count "3 times in a row". |
| RT02 | Safe mode behavior: dialog dismissed → project auto-open and terminal auto-start skipped; "Normal Mode" resets and the next launch auto-opens. |
| RT03 | recordStable: run the app >60s, background-kill (not crash), relaunch → counter at 0 (stable launch heuristic resets). |
| RT04 | Startup routing: signed out → AUTH; last project → project route ("[NAV] startDest" log); no last project → HOME; rotation does NOT re-trigger startDest recompute. |
| RT05 | Session restore: open 3 files, move cursors, split editor, restart → tabs, cursor offsets, scroll lines, split all back (workspaceRestoreEnabled on). |
| RT06 | Restore toggle off → restart → fresh state; re-enable → state resumes saving. |
| RT07 | clearProjectState on project delete → relaunch: deleted project's keys gone, other project's state intact. |
| RT08 | Corrupt session blob (hand-edit prefs via debug route): launch → decode catch → fresh start, NO crash. |
| RT09 | Session handoff push → pull on same device: shell/cursors/terminal history round-trip via backend. |
| RT10 | Pull corrupt session JSON → importSession decode catch → fresh start, no crash. |
| RT11 | Cloud backup: project with nested files → backup completes, entry appears in list with size/date; retry progress "Retrying (2/3)" visible under airplane-mode toggling. |
| RT12 | Cloud restore: restores files under filesDir/projects; project opens normally afterward. |
| RT13 | **RG03 probe:** craft an archive with a `../` entry name (or restore one with long paths) → observe where files land / whether names truncate at 100 chars (deep path). |
| RT14 | Cloud restore over an EXISTING project → existing files silently overwritten (no conflict prompt). |
| RT15 | CloudBackupPanel signed out (authToken "") → backend 401 message quality (opaque auth error vs a sign-in prompt). |
| RT16 | Container backup: Settings button → progress messages every 500 files, completion notification appears, file at /sdcard/CodespaceIDE/container-backup.tar.gz, size plausible. |
| RT17 | Symlink-heavy rootfs backup → restore: symlinks preserved with targets (e.g., /usr/bin symlinks), exec bits preserved (proot binary runs after). |
| RT18 | **RG02 probe:** corrupt the container backup copy (truncate it), trigger first-time restore → rootfs wiped, extraction fails/half-finishes — observe what the terminal claims ("✓ Restored"?). |
| RT19 | Per-file restore failure: pre-make one path unwritable → Log.w skip → terminal still prints "✓ Restore complete" (false success). |
| RT20 | Uninstall/reinstall cycle WITHOUT a prior backup → fresh-install path prints the explicit "No container backup found" message before the ~250MB download. |
| RT21 | Uninstall/reinstall WITH a backup: auto-restore path runs, container intact (node -v, piper, Claude Code all still installed), prefs-backup restored. |
| RT22 | **RG06 probe:** restorePrefs during first install, then immediately change a project setting → restart → does the restored value survive (in-memory instance overwrote the file?) or was the restore visible at all without a restart? |
| RT23 | Prefs-backup coverage: settings/keybindings/SSH profiles/session state after a restore → which of them actually survived (RG05 list). |
| RT24 | Crash upload (RG01): force a JVM crash → check crash_logs file written locally, then check whether a CrashLog record EVER appears on the backend. |
| RT25 | Native crash: force a SIGSEGV (pty native path) → native handler writes its minimal file; MainActivity upload attempt visible in logs. |
| RT26 | Notification drawer: bell unread count, DND silences toasts, severity filter, view_logs action routes to Output; store caps at 50 (flood 60 → oldest dropped). |
| RT27 | Toast banner: appears for new notification while drawer closed, auto-dismiss, tap-through. |
| RT28 | Workspace snapshot: createSnapshot → .zip lands in Downloads with the project inside (create-only; no in-app restore path — confirm). |
| RT29 | Backup over flaky network: airplane-mode mid-upload → backoff retries fire, typed failure surfaces with "failed after 3 attempts" wording. |
| RT30 | First-install restore + immediate project open: restored projects list works (projects.xml restored) AND session state for the project loads. |

## Gaps

| Gap | Priority | Source-confirmed behavior / risk | Evidence and check |
|---|---|---|---|
| **RG01** | **HIGH in group — VERIFIED DEAD. STANDING NOTE (owner): all earlier "confirmed device crash cause" claims audit-wide are UNVERIFIED until RG01 is fixed and a real crash produces a record — the audit has been missing crash-cause ground truth the whole time** | Crash telemetry delivers ZERO records end-to-end: the backend CrashLog store this pipeline writes is THIS Superagent's own table, read directly on 2026-09-24 — count = 0, despite months of confirmed device crashes (signal-31, native pty crashes, Compose concurrent-change) that the uploader was built to capture. Failure surface: upload thread with 4s timeouts racing process death on a crashing process; unauthenticated POST to a function that may not exist; response ignored (`conn.responseCode` "we don't care"). The app's own comment predicted "empty CrashLog on the backend after a confirmed device crash". Every crash-debug loop in this project has been running blind. | `CodeSpaceApplication.kt:150-248`; `MainActivity.kt:197-206`; **backend verified: CrashLog count=0, 2026-09-24**; RT24/RT25. |
| **RG02** | **HIGH in group, TB03/S01 family** | Rootfs restore wipes FIRST, then extracts, then lies: `rootfs.deleteRecursively()` runs before the first archive byte is read; per-file failures inside the extract loop are swallowed (`onFailure { Log.w }`, loop continues); the function returns true unless the FILE was missing; and TerminalPane prints "✓ Restored from backup!" without reading the return value. Mid-restore failure = container destroyed + half-extracted rootfs + a success message. Fix class: extract to a sibling temp dir + atomic swap, count failures, type the result, check it at the call site. | `terminal/BackupManager.kt:135-137,140-167`; `ui/panes/TerminalPane.kt:895-906`; RT18/RT19. |
| **RG03** | **TOP SECURITY TIER (2026-09-24 owner re-rank) — same zip-slip family as EX05; SHARED-FIX with EX05 confirmed in MASTER-GAPS.md** | Cloud restore extracts an archive from the network with `File(destDir, name)` where `name` is read raw from the tar header — `../` or absolute names write outside filesDir/projects on restore; `size.toInt()` allocates unbounded memory from a header field (OOM on a 3GB device, silent truncation >2GB); no checksum validation of the downloaded archive. Fix class: canonical-path validation + size caps — or replace with the commons-compress extraction BackupManager already uses (with the same name validation added there, see RG07). | `project/CloudBackupManager.kt:271-272,283-286`; RT13. |
| **RG04** | HIGH in group, silent data loss in the backup format itself | Hand-rolled tar writer truncates entry names to 100 bytes (`toByteArray(US_ASCII).take(100)`) — deeper paths silently become wrong filenames on restore with NO error; checksum written per the zero-field convention while the app's own reader IGNORES checksums entirely, so round-trip works but interop with real tar is unverified. The fix for RG03 (use commons-compress both ways) deletes this gap too. | `project/CloudBackupManager.kt:246-259,290-302`; RT13. |
| **RG05** | MEDIUM-HIGH in group — highest-real-world-impact for THIS device | Backup is manual-only AND selective: createBackup fires ONLY from the Settings button (no schedule, no pre-uninstall prompt), and prefs-backup covers just 3 XMLs + memory.json — JsonSettingsStore settings, keybindings, ssh-profiles.json, session_state, tasks.json are all outside the list. This user's workflow (every CI APK = forced uninstall, per the file's own header) means one forgotten Settings tap loses the container AND most persisted state. Fix class: prompt/auto-backup on app-update detection + extend the store list. | `terminal/BackupManager.kt:46,173-183`; `ui/screens/SettingsScreen.kt:379-380`; RT21/RT23. |
| **RG06** | MEDIUM in group, false-restoration risk (IG03 family) | restorePrefs copies raw XML into shared_prefs while the app process may already hold those SharedPreferences loaded: the in-memory instance can overwrite the restored file on its next commit (restore silently lost) and won't see restored values without a restart. Restore must either happen before any load or go through the prefs API. | `terminal/BackupManager.kt:186-203`; `ui/panes/TerminalPane.kt:901`; RT22. |
| **RG07** | MEDIUM in group, security (tamper surface) | The container backup lives in WORLD-READABLE/WRITABLE public storage (/sdcard/CodespaceIDE/) — any app or adb session can read the entire rootfs (home-dir tokens, git config, installed credentials) or REPLACE the archive; on next reinstall the tampered archive is extracted into the rootfs with `File(rootfs, entry.name)` unvalidated at the sink (commons-compress does not sanitize names by default). Fix class: validate entry names + record and verify an archive checksum on restore; public location stays (uninstall survival is the point), integrity does not have to be lost with it. | `terminal/BackupManager.kt:26-28,141`; RT18. |
| **RG08** | MEDIUM in group, S01 | TerminalPane:895-906 ignores restoreBackup's Boolean return and prints "✓ Restored from backup!" unconditionally — a restore that returned false or half-failed reads as success in the terminal; pairs with RG02's per-file swallow (fix together at the call site). | `ui/panes/TerminalPane.kt:902-904`; RT19. |
| **RG09** | LOW in group | CloudBackupPanel falls back to `authToken ?: ""` — a signed-out user gets opaque backend 401s instead of a sign-in prompt (the ConnectorsApiClient pattern handles this correctly with a clear message; same fix shape). | `ui/panels/CloudBackupPanel.kt:50`; RT15. |
| **RG10** | LOW in group, forward-compat | SessionHandoffManager has no schema version field in the session blob — a future format change makes old blobs decode to garbage or empty via the opt*-fallbacks, silently, instead of a versioned rejection. | `data/SessionHandoffManager.kt:31-60`. |
| **RG11** | LOW in group, additive | TerminalMemory persists cwd + last 50 commands only — cosmetic continuity (honest, no revival claim); actual terminal revival (VS Code pty-host revive) is a distant additive feature. | `data/SessionStateStore.kt:126-133`; V terminalService.ts:486-523. |
| **RG12** | LOW in group, create-only affordance | Workspace snapshots zip a project to Downloads with NO restore path — an export with no import; the file exists only if the user finds it in Downloads manually. Either wire a restore (or point the project-open flow at it) or label it export-only in the UI. | `util/WorkspaceManager.kt:83-100`; RT28. |

**Strengths (recorded for the fix plan):** createBackup is the codebase's gold-standard atomic write (tmp + rename) with real tar (symlinks, exec bits, long-file modes, virtual-mount skips) and honest progress + notification records; the Safe Mode crash-loop breaker is genuinely wired end-to-end (heuristic, threshold, dialog, reset); SessionStateStore is decode-defensive with a restore toggle and bounded terminal history; the notification triad (bell/toast/drawer) mirrors VS Code's center/toasts shape on one bounded store; P32's crash-context enrichment was smart engineering aimed at a real recurring crash; retryNetwork's onAttempt progress is the honest-retry pattern SK-family fixes should copy.

**Cross-links:** RG01 → S01/TP01 posture (unauthenticated mutating endpoint on the Superagent side) + needs a device-crash re-verify after any endpoint fix. RG02 → TB03 (fix design shared: never delete before the replacement is verified) + S01 typed result. RG03 → EX05/IG01 sinks + RG07 (one shared canonical-path validation utility). RG04 → RG03 fix (commons-compress both ways) subsumes it. RG05 → owner's forced-uninstall workflow — the highest-urgency MEDIUM in the audit so far. RG06 → IG03/restoreAll + SK01 batch. RG07 → TP04 rootfs trust (checksum on restore = TP04's own recommendation). RG08 → RG02 call-site fix. RG09 → ConnectorsApiClient requireAccessToken pattern.
