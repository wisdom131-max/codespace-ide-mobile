<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Storage permissions for reading/writing project files -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- Floating terminal overlay -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Battery optimization exemption — prevents Android from killing terminal sessions.
         Critical on TECNO/Infinix/Samsung devices with aggressive power managers. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Vibrate for haptic key feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Install APKs from within the terminal -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Auto-restart terminal service after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:extractNativeLibs="true"
        android:name=".CodeSpaceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.CodeSpace"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.CodeSpace.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- GitHub OAuth callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="codespace" android:host="oauth" />
            </intent-filter>
        </activity>

        <!-- Foreground service for terminal & background sync keep-alive -->
        <service
            android:name=".terminal.TerminalService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <!-- specialUse required: terminal emulator forks pty/shell processes,
                 which doesn't map to any standard Android foreground service type.
                 This is the correct type for Termux-style terminal emulators. -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="terminal_emulator" />
        </service>

        <!-- OEM keepalive signals — read by Samsung, TECNO, Infinix, and other OEMs
             that share Samsung's power management framework. Without these, the OEM
             power manager treats the app as a regular background app and sends SIGRTMIN.
             Termux ships these exact values and survives on all these OEMs. -->
        <meta-data
            android:name="com.samsung.android.keepalive.density"
            android:value="true" />
        <meta-data
            android:name="com.samsung.android.multidisplay.keep_process_alive"
            android:value="true" />
        <meta-data
            android:name="com.sec.android.support.multiwindow"
            android:value="true" />

        <!-- TECNO/Transsion specific — prevent phantom process killing -->
        <meta-data
            android:name="com.hihonor.android.support.keepalive"
            android:value="true" />

    </application>
</manifest>


