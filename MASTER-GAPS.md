# Master gap register (source-confirmed, device unverified)

A living fix-plan input, not a record of passed or failed device tests. Priority describes the order for *eventual* fix planning, not authority to change code. Each group audit adds its gaps here and links its detailed evidence. Source paths below are relative to `android/app/src/main/java/com/codespace/ide/`; line numbers are 1-based.

## Fix-plan ordering and shared failure pattern

**TOP TIER (co-priority, above every other listed gap): G01, TB01, TB03, EX04, EX05, EX07.** G01/TB03/TB01 form one coupled data-loss chain: an edit can fail to persist silently (G01), a tab/root close can discard its only live buffer without a dirty warning (TB03), and crash recovery can delete the only backup while falsely reporting restored edits (TB01). **Plan and verify these together**: adding a TB03 dialogue alone cannot recover a TB01 backup already deleted. EX04 is file-creation/rename path escape, EX05 is archive extraction escape (zip-slip boundary), and EX07 is a destructive remove-root scope risk. Treat the latter three as security-relevant containment issues, not mere UI correctness. These rankings are for a later user-authorized fix plan, not permission to implement now. Do not use a real project to reproduce them.

**SYSTEMIC GAP S01: unchecked operation outcome and false success.** G01 (file write), EX01 (trash move), EX02 (nested restore) and EX06 (folder create) all permit a success-looking outcome without proving the requested file result. EX02's mechanism is specifically *lost original-path metadata plus the restore UI ignoring the Boolean result*, not an unchecked `mkdirs()` or write call. When planning fixes, prefer a shared typed result/verification contract for file operations and user-facing success notifications, preserving original paths for restore; do not assume one helper alone fixes EX02's metadata loss. Keep the four IDs as traceable manifestations and test them individually, but design the fix as one systemic workstream where feasible. **SR03 extends the same false-success pattern to Search Replace All**: it reports matched-line counts without verifying that the displayed hits were actually replaced. Any shared verification/typed-result contract for S01 must specifically cover SR03 and SR04's write paths too — Search has its own separate Replace All implementations (sidebar and modal) that write disk directly rather than reusing Explorer's file-write logic, so a fix confined to Explorer's helpers would leave them unchecked. SR03 stays below top tier: it misreports but does not destroy data. **IC04 extends the same pattern to IntelliSense auto-import insertion**: the tap-path import apply wraps the whole operation in a swallowed exception, so a failed import insertion disappears without any failure signal — the S01 shared verification design must cover this write path too. **PR01 extends the same pattern to build cancellation**: BuildRunner.currentProcess is never assigned, so cancelBuild() destroys null and sets CANCELLED while the gradle build keeps running, and the completing coroutine then overwrites the status to SUCCESS/FAILED — a person can act on a build status that never happened, a sharper consequence than a miscount. PR01 ranks above PR03 within the Problems group, but the owner directs they be fixed together: PR03's 2000-line output cap swallowing the BUILD SUCCESSFUL/FAILED marker is a second way the same status field ends up wrong — one status-integrity workstream covers both (true process ownership via execOnceWithProcess + marker-preserving output capture + a single authoritative terminal status write).

## Editor: accepted audit ([GROUP-EDITOR.md](GROUP-EDITOR.md))

| Gap | Priority | Source-confirmed behavior / risk | Evidence and check |
|---|---|---|---|
| **G01** | **TOP TIER, co-priority with TB01/TB03/EX04/EX05/EX07** | On each edit of an absolute-path file, EditorPane marks the tab dirty, attempts a disk write, and silently catches a write failure. The open buffer may show the new text while the disk does not, and Save can still clear dirty state. Treat this in the top fix-plan tier, ahead of all non-top-tier gaps; do not confuse a success notification with verified persistence. | `ui/panes/EditorPane.kt:2081-2092,305-316`; Editor T01/T18. |
| **G10** | **HIGH, distinct Save-route divergence** | **File > Save and Ctrl+S are different callbacks, not just different write timings.** File menu calls `formatOnSaveTrigger++`, immediately announces success, then an EditorPane effect consults `ProjectSettingsStore.formatOnSaveEnabled`; Ctrl+S routes through `CodeEditor.onSave` to `saveCurrentFile`, writes without that formatting branch. Thus when Format on Save is enabled, menu Save *may change the text* while Ctrl+S will not; both routes swallow write errors and clear dirty state, and both issue LSP `didSave`. A separate shell command/palette `Save` handler only announces success (`ProjectShellScreen.kt:1070`). Confirm the behavioral divergence in a disposable file, including actual disk bytes, not merely timestamps. | `ui/screens/ProjectShellScreen.kt:412,1070,1176`; `ui/panes/EditorPane.kt:263-316,2132`; `editor/CodeEditor.kt:3029-3037`; Editor T18. |
| G02 | HIGH | External history restore ticks editor refresh without recording the restored file among `lastAppliedPaths`; the refresh loop filters on those paths. An open tab need not refresh when restoring a snapshot. | `chat/PendingChangesStore.kt:302-308`; `ui/panes/EditorPane.kt:235-250`; `ui/panes/ExplorerPane.kt:2359-2373`; Editor T37. |
| G03 | HIGH | Path spellings split buffer/cache/view identities; suffix fallback may ambiguously choose one of two files. | `editor/EditorBufferStore.kt:14-34`; `editor/FileCache.kt:27-58`; `ui/panes/EditorPane.kt:2157-2209`; Editor T38. |
| G04 | MEDIUM | Per-mounted-view `DecorationStore` and pane-level LSP squiggles have distinct lifetimes; diagnostic matching permits basename fallback. | `editor/EditorDecorations.kt:15-42`; `ui/panes/EditorPane.kt:1360-1382,1550-1563`; Editor T27/T28. |
| G05 | MEDIUM | Shell and pane jump/viewport state have multiple writers and line conventions. | `ui/screens/ProjectShellScreen.kt:1518-1522,4642-4653`; `ui/panes/EditorPane.kt:484-490,2157-2209`; Editor T14/T15. |
| G06 | MEDIUM | Whole-buffer undo snapshots are per mounted editor view while split views share tab content. | `editor/CodeEditor.kt:994`; `ui/panes/EditorPane.kt:2051-2053`; Editor T39. |
| G07 | MEDIUM | FileCache uses raw path and last-modified time, and external history copies do not explicitly invalidate it. | `editor/FileCache.kt:27-58`; `ui/panes/ExplorerPane.kt:2368-2370`; Editor T37. |
| G08 | MEDIUM | Find query/toggle persistence is global rather than resource- or project-keyed. | `editor/EditorFindState.kt:12-52`; Editor T40. |
| G09 | MEDIUM | Direct, selected-range, LSP and save-time formatting take distinct paths; some write failures are swallowed. | `ui/panes/EditorPane.kt:263-308,1170-1282`; Editor T16-T18. Distinct G10 isolates Save-route behavior. |

## Explorer

Explorer source audit: [GROUP-EXPLORER.md](GROUP-EXPLORER.md). DATA-RISK checks are confined to disposable roots; no device result is asserted.

| Gap | Priority | Source-confirmed behavior / risk | Evidence and check |
|---|---|---|---|
| **EX01** | HIGH | `WorkspaceManager.moveToTrash` returns an entry without checking `renameTo` success. Single delete and bulk-delete counting can claim a move that did not occur. | `util/WorkspaceManager.kt:202-213`; `ui/panes/ExplorerPane.kt:2450-2470,2487-2500`; XT33. |
| **EX02** | HIGH | Trash listing reconstructs only the basename, so nested files restore to the root rather than their original folder; `_restored` collision fallback is only one level. | `util/WorkspaceManager.kt:215-244`; XT16/XT34. |
| **EX03** | MEDIUM | Cut/Paste drops clipboard even if `renameTo` fails; directory copy calls nonrecursive `copyTo`, and shell rename callback is not used. | `ui/panes/ExplorerPane.kt:1631-1653`; XT14/XT35. |
| **EX04** | **TOP TIER, security-relevant path containment** | New File, New Folder and Rename accept slash/`..` inputs without canonical containment checks. User input can specify paths outside the chosen directory; device impact not tested. | `ui/panes/ExplorerPane.kt:2146-2334`; XT36 only in isolated scratch root. |
| **EX05** | **TOP TIER, security-relevant zip-slip containment** | ZIP extraction compares canonical paths with `startsWith(dirPath)` lacking a path-separator boundary, so a sibling `out2` can pass for output root `out`. Avoid untrusted archives in real workspaces until fixed. | `ui/panes/ExplorerPane.kt:1737-1778`; XT36 only in isolated scratch root. |
| **EX06** | MEDIUM | New Folder reports success without checking `mkdirs()` result. | `ui/panes/ExplorerPane.kt:2260-2296`; XT37. |
| **EX07** | **TOP TIER, security-relevant delete scope** | Cloned-root `Remove & delete files` uses `deleteRecursively()` after only a minimum path-depth check; it does not assert canonical containment within an allowed project root. | `ui/panes/ExplorerPane.kt:1162-1211`; XT18 disposable clone only. |
| **EX08** | MEDIUM | Multi-select All can append duplicate paths; it only walks displayed root and intentionally excludes internal files even if shown. | `ui/panes/ExplorerPane.kt:833-854`; XT11/XT12. |


## Tabs

Tabs source audit: [GROUP-TABS.md](GROUP-TABS.md). All BT01-BT36 checks are pending on disposable projects; **G01/TB01/TB03/EX04/EX05/EX07 are co-top; the other Tabs gaps rank below them**.

| Gap | Priority | Source-confirmed behavior / risk | Evidence and check |
|---|---|---|---|
| **TB01** | **TOP TIER, coupled data-loss chain G01/TB03/TB01** | Scratch autosave files are named by URL-encoded *full path*, but Restore compares the encoded filename to `File(tab.path).name` and deletes the backup regardless of whether a tab matched, then reports success. Requires correct full-path decode, verified restore outcome and safe backup retention. Related to S01 false-success pattern but not fixed by a generic write-result check alone. | `ui/panes/EditorPane.kt:808-821,831-904`; BT25/BT26. |
| **TB02** | HIGH, **race condition below top tier** | Pane and shell replace the same per-project `ShellState` JSON using different subsets and default-filled fields. Rapid/repeated independent state updates can race; whichever partial writer runs last erases fields owned by the other. A single happy-path launch test cannot establish safety: repeatedly alternate tab/selection, panel and font changes, then relaunch and inspect all fields. | `ui/panes/EditorPane.kt:749-773`; `ui/screens/ProjectShellScreen.kt:900-918`; `data/SessionStateStore.kt:47-65,214-259`; BT22. |
| **TB03** | **TOP TIER, coupled data-loss chain G01/TB03/TB01** | Strip Close/Close Others/Close All and root-removal close bypass the only dirty warning, which belongs to Android BackHandler. If G01 disk write failed, closing can discard the sole live buffer. Related to S01's unverified persistence, but requires explicit close policy. | `ui/panes/EditorTabClose.kt:34-81`; `ui/panes/EditorPane.kt:458-478,650-688,982-1039`; BT23/BT27. |
| **TB04** | HIGH, split ID collision | After creating split views 1..4, removing #2 then adding uses `existing.size+1` and recreates #4. Same ID can refer to two strip views; verified by applying code's ID formula, not a device result. | `editor/SplitViewStore.kt:60-106`; BT31. |
| **TB05** | MEDIUM, cross-project state | Process-global split views are not pruned on entering a project whose saved split list is empty, because the restore/prune call is conditional on a nonempty list. | `editor/SplitViewStore.kt:41-47,106-122`; `ui/panes/EditorPane.kt:636-647`; BT30/BT34. |
| **TB06** | HIGH, rename identity | Explorer rename updates shell mirror and LSP but does not rekey authoritative EditorPane tabs; old path can reappear or new path open as an extra tab. | `ui/screens/ProjectShellScreen.kt:1422-1450`; `ui/panes/EditorPane.kt:157-210,690-747`; BT28. |
| **TB07** | MEDIUM, mirrored close | Explorer OPEN EDITORS X removes a shell mirror path, not EditorPane's real tab, and pane sync can resurrect it. | `ui/screens/ProjectShellScreen.kt:1558-1568`; `ui/panes/EditorPane.kt:730-747`; BT10/BT11. |
| **TB08** | MEDIUM, linked to G03 | Raw string equality can duplicate one canonical file as separate tabs and split identities through host/guest aliases. Treat as a tab-level manifestation of G03, not a separate fix. | `ui/panes/EditorPane.kt:690-729`; `editor/SplitViewStore.kt:41-122`; BT35. |


## Search

Search source audit: [GROUP-SEARCH.md](GROUP-SEARCH.md). All ST01-ST36 checks are pending, with replacements confined to disposable backed-up files. The owner-ranked co-top tier **G01/TB01/TB03/EX04/EX05/EX07** remains above every Search gap.

| Gap | Priority | Source-confirmed behavior / risk | Evidence and check |
|---|---|---|---|
| **SR01** | HIGH, regex correctness | Sidebar regex hit is accepted by Regex but then located by *literal* `indexOf(query)`; valid pattern hits with metacharacters can disappear. | `ui/panes/ExplorerPane.kt:2964-2989`; ST04. |
| **SR02** | MEDIUM, stale/UI latency | Sidebar debounce launches an independent main-scope scan, so canceling an old effect need not cancel that scan; older results can overwrite newer, and file reads run on UI scope. | `ui/panes/ExplorerPane.kt:2922-3009`; ST32/ST33. |
| **SR03** | HIGH, replace mismatch / S01 manifestation | Modal Aa OFF searches case-insensitively but Replace All uses case-sensitive escaped Regex; it reports matched-line counts even when a visible hit remains unchanged — the same reports-success-without-verifying-result pattern as S01, in Search's own Replace All rather than a file operation. Keep below top tier (misreport, not data loss); any S01 shared verification design must cover this and SR04's write paths. | `ui/panes/ProjectFileSearchPanel.kt:176-205,368-390`; ST17/ST18. |
| **SR04** | HIGH, working-copy/data integrity | Sidebar and modal Replace All write disk directly from previously collected results with no shared editor-buffer/drift/operation-result contract; sidebar swallows per-file failures. Coordinate with G01/S01, while leaving it below the established top tier pending owner review. | `ui/panes/ExplorerPane.kt:3066-3104`; `ui/panes/ProjectFileSearchPanel.kt:368-390`; ST08-ST10/ST19/ST36. |
| **SR05** | MEDIUM, symbol jump | Workspace symbol overlay drops line before shell navigation. Command palette @ preserves line, so routes diverge. | `ui/screens/ProjectShellScreen.kt:1831-1841,1967-1976,4211-4238`; ST25. |
| **SR06** | MEDIUM, LSP readiness | Symbol panel remembers LSP initialized status only by active language, leaving readiness stale if server starts while overlay remains open. | `ui/panes/SymbolSearchPanel.kt:74-94`; ST27. |
| **SR07** | MEDIUM, stale symbols | Fallback `remember(query)` omits indexer completion from its key; same query may remain empty after indexing finishes. | `ui/panes/SymbolSearchPanel.kt:59-69`; `editor/FileIndexer.kt:86-125`; ST28. |
| **SR08** | MEDIUM, external changes | Incremental FileIndexer watcher exists but no call site starts it; only project-open indexing is found. New symbols may remain absent until reindex/reopen. | `editor/FileIndexer.kt:86-125,272-334`; `ui/screens/ProjectShellScreen.kt:896-906`; ST29. |
| **SR09** | MEDIUM, palette routing | >Go to File sets the already-open palette true and click then closes it; >Save File label has no exact Save handler. Listed commands are not necessarily operational. | `ui/screens/ProjectShellScreen.kt:1070-1078,2283-2308`; ST23/ST24. |
| **SR10** | MEDIUM, search responsiveness | Modal indexed walk and palette scan run in Compose on UI thread; sidebar file reads run on main scope, modal Text reads unrestricted file lines. UI stalls/large allocations need device measurement. | `ui/panes/ProjectFileSearchPanel.kt:136-153,176-205`; `ui/screens/ProjectShellScreen.kt:2327-2345`; ST33. |
| **SR11** | MEDIUM, path normalization | LSP workspace-symbol URI uses URLDecoder (literal `+` becomes space) without an overlay guest-to-host handoff; file navigation can target a different/unreadable path. | `ui/panes/SymbolSearchPanel.kt:106-118`; `ui/screens/ProjectShellScreen.kt:1831-1841,4211-4238`; ST35. |
| **SR12** | MEDIUM, scope fragmentation | Sidebar, modal/palette and staged chat search use different roots, datasets and query persistence; compare scope before treating differing hits as search failure. | `ui/panes/ExplorerPane.kt:131-138,2935-3003`; `ui/panes/ProjectFileSearchPanel.kt:106-153`; `chat/SearchResultsAttach.kt:20-49`; ST30/ST34. |

## IntelliSense

IntelliSense source audit: [GROUP-INTELLISENSE.md](GROUP-INTELLISENSE.md). All IT01-IT36 checks are pending. The owner-ranked co-top tier **G01/TB01/TB03/EX04/EX05/EX07** remains above every IntelliSense gap, but the owner has flagged **IC01 as high-visibility, ranked above the rest of this group**: Tab-accepting a completion in a dot-context can replace more of the buffer than intended — silent code corruption during editing, not a missing feature. Note C-IS03: every completion accept path funnels through the same disk write G01 shows can fail silently, so accept-contract fixes should be sequenced with that tier's fix, not before it.

| Gap | Priority | Source-confirmed behavior / risk | Evidence and check |
|---|---|---|---|
| **IC01** | **HIGH-VISIBILITY (owner-flagged), ranked above the rest of this group** | Tap applies snippets/auto-imports, Tab accepts with a plain insert and treats `.` as a word char — Tab-accepting a completion in a dot-context can replace more of the buffer than the user intended (e.g. the whole `obj.m` receiver+member), i.e. silent code corruption during editing rather than a missing feature. Commit characters do another plain insert; snippets accepted via Tab leave raw `$1` text. One item, three semantics. | `editor/CodeEditor.kt:2355-2374,2693-2722`; `editor/CompletionPopupOverlay.kt:322-478`; IT09/IT24/IT25. |
| **IC02** | HIGH, LSP textEdit ignored | `textEditJson` is carried but never applied; all accept paths replace only the current word scan, approximating server replacement spans. | `editor/CodeEditor.kt:191,1410,1466`; `editor/CompletionPopupOverlay.kt:322-328`; IT21. |
| **IC03** | MEDIUM, LSP command ignored | The LSP `command` field passes through ranking but no accept path executes it. | `editor/CodeEditor.kt:1414,1471`. |
| **IC04** | MEDIUM, auto-import shift assumption / S01 manifestation | Tap-path auto-import adds a blanket length delta to the word position (assumes the edit is above the cursor); an edit below it mislocates insertion, and any exception is silently swallowed so the auto-import can fail with no feedback. Joins G01/EX01/EX02/EX06/SR03 in the S01 unchecked-outcome pattern — any shared typed-result/verification design must cover the auto-import insertion path. | `editor/CompletionPopupOverlay.kt:329-346,362-366`; IT26. |
| **IC05** | MEDIUM, no list navigation | `selectedLabel` is only ever reset, never moved; highlight is always index 0 and Tab always accepts the first item. Hardware Up/Down only cycle signature overloads. | `editor/CodeEditor.kt:1203-1204,2698-2704,2921-2936`; `editor/CompletionPopupOverlay.kt:283-286`; IT35. |
| **IC06** | MEDIUM, Enter does not accept | IME Enter inserts a newline while the popup is visible; only Tab or tap accepts. | `editor/CodeEditor.kt:2432-2467,2693-2695`. |
| **IC07** | MEDIUM, ranking divergence | sortText is claimed PRIMARY but the code sorts by fuzzy score first with a small sortText tiebreak; comment's "+50f boost" does not match the computed 0..-50 range. | `lsp/CompletionEngine.kt:250-276`; IT34. |
| **IC08** | LOW-MEDIUM, dead AI source | AI source has boost/chip/allowance and an onAiFixRequest path, but no producer creates AI-source completion items. | `lsp/CompletionEngine.kt:261`; `editor/CompletionPopupOverlay.kt:244,545,556-578`; IT30. |
| **IC09** | MEDIUM, signature help visibility | Signature help hides whenever completions show, and overload cycling needs hardware arrows only. | `editor/CodeEditor.kt:4805,2921-2936`; IT14/IT15. |
| **IC10** | MEDIUM, hover requires LSP | Hover renders only LSP content or debug evaluate; the curated doc table feeds completion details, not hover. | `editor/ui/HoverPopup.kt:56`; `editor/CodeEditor.kt:283,369-372`; IT17. |
| **IC11** | LOW, resolve keyed by label | resolveCache and resolver lookup use label only; same-label items with different kind/detail can resolve the wrong docs. | `editor/CodeEditor.kt:1197-1206,1497-1523,1430-1436`. |
| **IC12** | LOW, history scope | MRU/usage maps are global by label (last language wins), not per model/position like VS Code suggest memory; save failures are silently swallowed. | `lsp/CompletionHistoryStore.kt:73-110,113-130`; IT28. |
| **IC13** | LOW, no document-word provider | No provider derives suggestions from words already in the document; only curated keyword/type/snippet/stdlib tables. | `editor/CodeEditor.kt:283-392,1187`. |
| **IC14** | LOW, context scanner vs literals | `isInsideCallParens` counts parentheses inside strings and comments, so quoted `(` can wrongly trigger call-argument context. | `lsp/CompletionContextDetector.kt:206-215`; IT33. |

## LSP

LSP source audit: [GROUP-LSP.md](GROUP-LSP.md). All LT01-LT36 checks are pending. The owner-ranked co-top tier **G01/TB01/TB03/EX04/EX05/EX07** remains above every LSP gap. This group owns the language-server client machinery (configs, install, spawn, initialize, JSON-RPC, sync, lifecycle, diagnostics push, ~35-method request surface, URI mapping, ctags fallback); accept semantics are the IntelliSense group's IC gaps.

| Gap | Priority | Source-confirmed behavior / risk | Evidence and check |
|---|---|---|---|
| **LS01** | HIGH, protocol completeness | Server-initiated requests (workspace/applyEdit, workspace/configuration, client/registerCapability, window/workDoneProgress/create, showMessageRequest) get no response at all — not even a spec-permitted error; servers relying on them degrade silently. | `lsp/JsonRpcClient.kt:244-285`; LT10. |
| **LS02** | HIGH, silent sync loss | `isServerRunning` checks only process.isAlive, not initialized; a file opened mid-initialize gets didOpen rejected but `lspOpenedFiles` set true anyway, so that tab never gets didOpen on the running server instance — no completions/diagnostics until a restart or tab re-key. GEN-WATCH only fires on generation bumps, POLL-CLEANUP only on death. | `lsp/LspManager.kt:858-861`; `ui/panes/EditorPane.kt:1460-1461,1398-1399`; LT05. |
| **LS03** | HIGH, idle strand | Idle auto-close stops the server while the active tab's keys are unchanged, so Effect A never re-fires and nothing restarts on typing — active editor silently degrades to local-only IntelliSense until a tab switch. | `lsp/LspManager.kt:903-930`; `ui/panes/EditorPane.kt:1390-1395,1466-1477`; LT06. |
| **LS04** | MEDIUM, full-text sync cost | didChange always resends the whole buffer (no incremental sync declared), so every 150ms typing window costs O(file size) on-device. | `lsp/LspDocumentSync.kt:33-45`; `lsp/LspServerLifecycle.kt:69-73`; LT20. |
| **LS05** | MEDIUM, inconsistent capability gating | Hover/signature/codeAction/semanticTokens and friends gate on hasCapability, but completion, definition, references, documentSymbol, rename, prepareRename, formatting do not; the code's own comment warns unrequested methods can crash servers like pylsp. | `lsp/LspManager.kt:2498-2576,2619-2667,2767-2807,2888-2910,962-979`; LT31. |
| **LS06** | MEDIUM, diagnostics cross-match | Squiggle handler falls back to matching by basename, so two open tabs with the same filename can receive each other's diagnostics (same misdirection class as SR03); normalizeFileUri decode cannot round-trip literal `%` names. PLAN A canonical-path keying should replace the fallback. | `ui/panes/EditorPane.kt:1554-1558`; `lsp/LspManager.kt:3388-3390`; LT13/LT15. |
| **LS07** | MEDIUM, server messages dropped | Only publishDiagnostics and window/logMessage have handlers; showMessage, showMessageRequest and $/progress never surface — server user-messages and long-indexing progress are invisible. | `lsp/JsonRpcClient.kt:229-253`; `lsp/LspManager.kt:1600-1615`. |
| **LS08** | MEDIUM, failure info collapse | request() maps timeout, server error and connection loss all to null; error codes are logged but not returned, so call sites cannot distinguish unsupported from busy from dead. | `lsp/JsonRpcClient.kt:127-187`; LT32. |
| **LS09** | LOW-MEDIUM, restart/start race | handleAutoRestart runs on a raw thread concurrent with Effect A / master-toggle startServer; servers map and generation counters have no lock. Healthy-reuse check mitigates; stress-test item (TB02 class), not a confirmed defect. | `lsp/LspManager.kt:2088-2153,1390-1401`. |
| **LS10** | LOW-MEDIUM, unchecked sync outcomes | didOpen/didChange/didClose/didSave/didRenameFiles are fire-and-forget: notify swallows write exceptions and Boolean returns are ignored — S01's unchecked-outcome shape on the LSP channel; LS02 is the worst instance. | `lsp/JsonRpcClient.kt:186-219`; `lsp/LspManager.kt:2359-2497`. |
| **LS11** | LOW, poll cost | Four permanent background polls (2s GEN-WATCH, 1s idle, 60s health, 10s memory) on a battery-constrained device. | `ui/panes/EditorPane.kt:321-397`; `lsp/LspManager.kt:903-930,2189-2282`. |
| **LS12** | LOW, comment/config drift | Effect A's info note claims JSON/XML/Markdown/Shell have no server, but configs include JSON, HTML and CSS servers — doc drift misleads triage. | `ui/panes/EditorPane.kt:1407-1411`; `lsp/LspManager.kt:480-549`. |
| **LS13** | LOW, settings switch needs restart | diagnosticsSource (pyright/pylsp) and typescriptVersion only apply on the next startServer; a running server keeps its identity until it dies or idles. | `lsp/LspManager.kt:1291-1303,1122-1178`; LT03. |
| **LS14** | LOW, inlay range/budget | getInlayHints requests a range ending at line Int.MAX_VALUE with a 5s timeout; some servers clamp or reject, and whole-file hints may exceed the budget. | `lsp/LspManager.kt:3477-3495`; LT27. |

## Problems / Build / Tasks

Problems/Build/Tasks source audit: [GROUP-PROBLEMS.md](GROUP-PROBLEMS.md). All PT01-PT36 checks are pending. The owner-ranked co-top tier **G01/TB01/TB03/EX04/EX05/EX07** remains above every Problems gap. This group owns the diagnostics pipeline (central DiagnosticManager store, LSP/lint/matcher/gradle bridges, Advanced Problems panel, jump chain, explorer badges) and the build/task surface (BuildRunner, GradleErrorParser, TaskRunner, both panels).

| Gap | Priority | Source-confirmed behavior / risk | Evidence and check |
|---|---|---|---|
| **PR01** | MEDIUM-HIGH (above PR03, fix WITH PR03 per owner), cancel no-op + state clobber / S01 manifestation | `BuildRunner.currentProcess` is never assigned (runBuild uses execOnce, which keeps the Process private); `cancelBuild()` destroys null and sets CANCELLED while the gradle build keeps running in proot, and the still-completing coroutine then overwrites the status to SUCCESS/FAILED after the CANCELLED state — a "cancelled" build can end up labeled SUCCESSFUL. `execOnceWithProcess` (P25-3) exists for exactly this and is unused. S01-family unchecked operation. | `build/BuildRunner.kt:47,108-160,212-231`; `terminal/ProotInstaller.kt:1438-1443`; PT12/PT13. |
| **PR02** | MEDIUM, badge source divergence | The RUN side-panel badge counts only LintChecker errors in a 3s disk poll, ignoring the central store — it disagrees with the Explorer badge and the panel over the same file, and re-lints outside the store. | `ui/screens/ProjectShellScreen.kt:3172-3184,3206,3287`; PT19/PT20. |
| **PR03** | MEDIUM, build output truncated + not streamed | execOnce caps output at 2000 lines — a verbose gradle build can lose the BUILD SUCCESSFUL/FAILED marker (mislabel via the else-FAILED branch) and all late compile errors; `_buildOutput` is written only at start and completion so the "live output" panel shows nothing during a multi-minute build; timeout returns a plain string indistinguishable from real output. | `terminal/ProotInstaller.kt:1396-1425`; `build/BuildRunner.kt:108-160`; PT14/PT15/PT33. |
| **PR04** | MEDIUM, quick-fix dead end | `Diagnostic.quickFixes` is never populated by any producer and never rendered — dead model field; VS Code's markers view has a Quick Fix entry per row. | `diagnostics/DiagnosticManager.kt:57`; `diagnostics/DiagnosticConverter.kt:20-90`; PT17. |
| **PR05** | MEDIUM, relatedInformation invisible | fromLsp parses relatedInformation into every row but no UI renders related locations — actionable context for duplicate-class/unused-import chains is dropped. | `diagnostics/DiagnosticConverter.kt:52-66`; PT16. |
| **PR06** | LOW-MEDIUM, matcher ingestion breadth | Any build-ish run command (node/npm/python...) gets compiler-format regexes applied to arbitrary output — false BUILD rows from non-compiler tools; VS Code matchers are per-task opt-in. | `diagnostics/ProblemMatcher.kt:33-36,47-50,159-170`; PT09/PT10. |
| **PR07** | LOW-MEDIUM, placebo menu commands | Problems tab commands "Filter" and "Show Errors Only" only fire notifications and change nothing — "Error-only filter toggled" is a false report (SR03 misreport family). | `ui/screens/ProjectShellScreen.kt:5039-5041`; PT29. |
| **PR08** | LOW-MEDIUM, empty-file build rows | Task-FAILED and "What went wrong" rows publish with file="" line 0 under an empty-path group; tapping sends "" into the jump chain (canonicalPathOrSelf finds no tab → new tab with empty path) — same family as the problems-jump BUG-A. | `build/GradleErrorParser.kt:41-52,102-116`; `diagnostics/DiagnosticConverter.kt:182-210`; `ui/screens/ProjectShellScreen.kt:4777-4786`; PT18. |
| **PR09** | LOW-MEDIUM, preset filter is substring | Explorer badge preset drops a bare filename into substring search — `Main.kt` also matches `MainViewModel.kt` and messages containing "main". | `ui/panes/AdvancedProblemsPanel.kt:71-76,100-112`; PT26. |
| **PR10** | LOW, dead panel file | `ui/panes/ProblemsPanel.kt` (399 lines) has no callers — superseded by AdvancedProblemsPanel; dead code. | `ui/panes/ProblemsPanel.kt:46`. |
| **PR11** | LOW, triple lint cost | Lint runs synchronously on open, tab switch, and save (main-thread string scan on large files) plus the 3s badge poll re-reads from disk — three passes over the same content. | `ui/panes/EditorPane.kt:723,1713,2092`; PT36. |
| **PR12** | LOW, task catalogue drift | BuildPanel dropdown (6 tasks) and TaskRunner catalogue (8 tasks) are separate hardcoded lists that have drifted; no single source. | `ui/panes/BuildPanel.kt:72-79`; `project/TaskRunner.kt:50-58`. |
| **PR13** | LOW, run state not durable | TaskRunner runs map and BuildRunner status are in-memory — an app kill mid-build leaves a RUNNING tile forever while a zombie gradle may still hold proot. | `project/TaskRunner.kt:63-66`; `build/BuildRunner.kt:29-50`; PT31. |
| **PR14** | LOW, central rows unshifted | Squiggles shift-correct through EditShiftHelper while typing but central store rows keep last-published ranges until the next republish — panel line numbers go stale mid-edit; PLAN A canonical store should own both. | `editor/EditShiftHelper.kt:1-76`; PT35. |

## Debugger

Debugger source audit: [GROUP-DEBUGGER.md](GROUP-DEBUGGER.md). All DT01-DT36 checks are pending. The owner-ranked co-top tier **G01/TB01/TB03/EX04/EX05/EX07** remains above every Debugger gap. Scope: debug/ (UniversalDebugManager, DAPClient, PythonDAPAdapter, NodeDAPAdapter, DebugAdapter/LegacyDebugAdapter, DebugConfiguration, DebuggerDependencies, legacy providers) and the debug UI (ExplorerPane Run & Debug view, gutter dots/rings, debug band, hover evaluate, DebugConsolePanel, DebugToolbarOverlay, AttachDebugDialog, VariableInspectorPanel, edit dialogs).

| Gap | Priority | Source-confirmed behavior / risk | Evidence and check |
|---|---|---|---|
| **DG01** | MEDIUM-HIGH, breakpoint persistence is dead (S01 family: feature that never happens) | `saveBreakpoints` (P23-8) has NO callers — MainActivity only calls `loadBreakpoints`, so the store is always empty at load and every breakpoint dies with the process; the save schema also omits `hitCondition` and load swallows parse exceptions silently. VS Code persists per-workspace automatically. | `debug/UniversalDebugManager.kt:943-993`; `MainActivity.kt:72`; VS `workbench/contrib/debug/common/debugStorage.ts:76-95`; DT06/DT07. |
| **DG02** | MEDIUM-HIGH, live breakpoint updates can't bind — **SEPARATE root cause from G03/PLAN A** | PythonDAPAdapter.sendBreakpoints sends raw HOST paths as DAP source.path while launch converts to GUEST paths — two different source identities for one file, so live-added/removed/edited breakpoints cannot bind; NodeDAPAdapter carries a false "already a guest path" comment. Launch omits hitCondition while the live path sends it; live path never refreshes verification. **Root-cause ruling (owner question, 2026-09-24): NOT a manifestation of G03.** G03/PLAN A is the intra-app identity split — one file's editor buffer/cache/tab identities keyed by different raw path spellings, fixed by one canonical-path store. DG02 lives entirely at the proot DAP boundary: the breakpoint store itself is consistently host-keyed; the bug is that the host→guest translation exists but is applied on the launch send path and omitted on the live send path. PLAN A alone would not fix it (a canonical store still needs the boundary translation), though it helps by making canonical-path → DAP-source one well-defined conversion point. The Debugger group's PLAN A members are DG08/DG11, not DG02; DG02's fix is local: apply the same launch-path mapping in sendBreakpoints. | `debug/PythonDAPAdapter.kt:58-96,316-330`; `debug/NodeDAPAdapter.kt:74-110`; DT02/DT03/DT16. |
| **DG03** | MEDIUM, disabled breakpoints still fire (placebo control) | Neither launch nor sendBreakpoints filters `enabled == false`, and setBreakpointEnabled never pushes to a live session — a disabled breakpoint keeps stopping the debuggee while the Explorer list dims it as off. VS Code sends enabledOnly: true. | `debug/PythonDAPAdapter.kt:58-96,320-330`; `debug/UniversalDebugManager.kt:931-938`; VS `browser/debugService.ts:1287`; DT04. |
| **DG04** | **TOP-TIER-ADJACENT, fabricated live session — flagged above the rest of the Debugger group** | TerminalDebugProvider.launch returns TRUE having done nothing — for languages without a real adapter (Kotlin/C/Go...) the fallback produces a session object in RUNNING state with live-looking pause/step controls over zero real execution. **Distinct from the placebo-control pattern it was linked to (PR07):** PR07's members are no-op/stalled UI that leaves the visible state honestly unchanged; DG04 FABRICATES a live status a person can burn real debugging time trusting before realizing nothing executes. Keep the PR07 link only as a shared honesty-of-controls theme; rank and fix DG04 as a false-session defect alongside the S01 false-success family (a session claimed that never existed). | `debug/UniversalDebugManager.kt:1012-1053,218-232`; DT08. |
| **DG05** | MEDIUM, main-thread ANR paths remain | The DEBUG-ANR fix covers only the PSS Run/Debug button and console Run; restartSession (stop + sleep poll + synchronous startDebug, 10s proot check / 5-min install) is called directly in onClick at PSS:1217 and ExplorerPane:3430, and the sidebar Debug button calls synchronous startDebug at ExplorerPane:3375. | `debug/UniversalDebugManager.kt:569-597`; `ui/panes/ExplorerPane.kt:3375,3430`; `ui/screens/ProjectShellScreen.kt:1217`; DT09/DT10. |
| **DG06** | MEDIUM, dead sessions pollute the active list | getActiveSessions filters only IDLE/STOPPED and the launch-failure path leaves the FAILED session in the map forever — the multi-session switcher lists and selects FAILED/CRASHED/ERROR sessions; legacy launch=false leaks the session. | `debug/UniversalDebugManager.kt:373-463,557-561`; DT12. |
| **DG07** | MEDIUM, DAP reverse requests dropped (LS01 protocol-completeness family) | DAPClient.dispatch has no "request" branch — any server→client request (runInTerminal, launchBrowser) is silently ignored with no response, hanging the server side. | `debug/DAPClient.kt:118-130,135-163`; DT13/DT14. |
| **DG08** | LOW-MEDIUM, dual breakpoint representations diverge (PLAN A concern) | EditorPane keeps its own fileBreakpoints remember map written only by that instance's gutter taps, while UDM's store drives the Explorer list and conditional rings — dots don't reflect Explorer removals, and a gutter tap where only UDM has a bp toggles UDM OFF while the local map turns it ON. | `ui/panes/EditorPane.kt:479,2133-2153`; DT06/DT08. |
| **DG09** | LOW-MEDIUM, variables-panel jump drops line and mismaps paths | The VARIABLES bottom tab onJumpToSource ignores the line parameter and joins the GUEST frame path onto the HOST project root — opens a wrong/nonexistent file with no line; should reuse PB14's canonical chain. | `ui/screens/ProjectShellScreen.kt:3713-3717`; DT25. |
| **DG10** | LOW-MEDIUM, two consoles and two watch stores | PSS debugMessages console and ExplorerPane consoleLines REPL are separate transcripts over one session; VariableInspectorPanel watchExprs and the Explorer watch are independent remember stores — neither survives switching; UDM's DebugWatch model used by neither. | `ui/screens/ProjectShellScreen.kt:3939-4115`; `ui/panes/VariableInspectorPanel.kt:62-189`; DT29. |
| **DG11** | LOW-MEDIUM, basename-matched debug band (LS06 family) | The paused-band guard compares frame file BASENAME to active tab BASENAME — same-named files in different folders make the band render on the wrong file; guest/host differences papered over instead of resolved. | `ui/panes/EditorPane.kt:491-517`; DT27. |
| **DG12** | LOW, singleton adapter client field | Python/NodeDAPAdapter each hold ONE mutable client field on a single registered instance; a second same-language session overwrites it — stop(A) kills B's client and events route to the last-registered callbacks; the multi-session switcher UI cannot be backed by it. | `debug/PythonDAPAdapter.kt:36,213-217`; `debug/NodeDAPAdapter.kt:50`; DT11. |
| **DG13** | LOW, stop path orphans debuggee + dead tracker (PR13 zombie family) | stop() destroys the proot process with no DAP disconnect — the debuggee can be reparented inside the rootfs and keep running; ProcessTracker (P27-6) is dead code: trackProcess/untrackProcess have no adapter callers and stopSession never untracks. | `debug/DAPClient.kt:68-71`; `debug/UniversalDebugManager.kt:178-206`; DT32. |
| **DG14** | LOW, remove-all dead + verification invisible + broadcast inconsistency | clearAllBreakpoints has no callers (VS Code ships Remove All in the breakpoints view); line-bp verified/message parsed but rendered nowhere; editBreakpoint pushes ALL breakpoints for ALL files to EVERY session while sendBreakpointsToActiveSession pushes one file to the first active session. | `debug/UniversalDebugManager.kt:700-718,1000-1004`; VS `browser/breakpointsView.ts:2210`; DT35. |

## Source Control / History / Trash

SCM source audit: [GROUP-SCM.md](GROUP-SCM.md). All CT01-CT36 checks are pending. The owner-ranked co-top tier **G01/TB01/TB03/EX04/EX05/EX07** remains above every SCM gap. Scope: the scm/ package (GitCommandExecutor, GitService, ScmState), SourceControlPane + dialogs, TimelinePanel, VersionHistoryV2 + the Explorer 20s capture loop, Local History and Trash dialogs, GitHubAuth device flow. Trash MECHANICS stay owned by the Explorer group (EX01/EX02).

| Gap | Priority | Source-confirmed behavior / risk | Evidence and check |
|---|---|---|---|
| **SG01** | HIGH in group, honesty-of-controls family (PR07/DG03) — staging UI is cosmetic for commits | The Commit button ALWAYS calls `stageAllAndCommit`: every commit sweeps all modified+untracked files even though the per-file stage/unstage UI and staged-count imply scoped commits; PublishDialog does the same. VS Code commits staged-only and asks before smart-committing. Scoped commit is impossible; unintended files enter history. | `ui/panes/SourceControlPane.kt:373-421,1892`; `scm/GitService.kt:212-222`; VS `extensions/git/src/commands.ts:2438-2450`; CT01/CT02. |
| **SG02** | HIGH in group, direct S01 manifestation on the DATA-RECOVERY path | Both restore surfaces (TimelinePanel, Local History dialog) run `PendingChangesStore.discard(file)` BEFORE `snap.copyTo(file, overwrite=true)`. If the copy fails, TimelinePanel swallows the exception (`catch (_: Exception) { }`) and the dialog lets it propagate unguarded — either way the staged overlay was already dropped, bumpExternalRestore never fires, disk keeps old content, open editor stays unrefreshed, NO user signal. These restores are the designated recovery for the top-tier G01/TB01/TB03 chain; the recovery itself can silently fail. Must join the S01 shared typed-result/verification design. | `ui/panes/TimelinePanel.kt:256-264`; `ui/panes/ExplorerPane.kt:2355-2372`; CT28. |
| **SG03** | MEDIUM-HIGH in group, TB03-adjacent silent loss | Restore immediately overwrites the working file with no diff preview and no confirmation (VS Code's default timeline action opens a DIFF; restore is a separate explicit command). Edits made since the last 20s capture are silently destroyed; no snapshot of the CURRENT content is taken before overwriting. | `ui/panes/TimelinePanel.kt:215-264`; `ui/panes/ExplorerPane.kt:2355-2372`; VS `contrib/localHistory/browser/localHistoryTimeline.ts:150-166`; CT29. |
| **SG04** | MEDIUM, security-adjacent (token exposure on the timeout path) | The GitHub token is injected as a Basic auth header INSIDE the shell command string, visible in the proot process list during push/pull/fetch, AND `ProotInstaller.execOnce` returns "Timed out after Ns running: $command" on timeout — the full command including the auth header becomes the result string flowing into the snackbar and NotificationStore verbatim. VS Code routes credentials through GIT_ASKPASS, never in argv or error text. Below the EX04/EX05/EX07 security tier (user's own token shown back to the same user) but should be reviewed with it. | `scm/GitCommandExecutor.kt:44-57`; `terminal/ProotInstaller.kt:1420`; VS `extensions/git/src/askpass.ts:17-34`; CT09. |
| **SG05** | MEDIUM, clone carries no auth | `GitService.clone` never passes the token (clone is not in needsAuth's set) — "Browse My Repos" can LIST the user's private repos via the API but cloning one FAILS with git's auth error; CloneDialog private URLs fail even while signed in. | `scm/GitService.kt:681-687`; `scm/GitCommandExecutor.kt:159-165`; CT10/CT11. |
| **SG06** | MEDIUM, stale SCM pane | No watcher or polling — refresh runs on pane open and after SCM's own operations only. Editor saves, Explorer deletes, chat apply, and terminal git all leave branch status, change counts, and ahead/behind stale until manual refresh. | `ui/panes/SourceControlPane.kt:125-136`; VS `extensions/git/src/model.ts:301-308`; CT06. |
| **SG07** | MEDIUM, status cost blocks freshness | One loadStatus = 7 proot process spawns (isRepo + branch + upstream + rev-list + HEAD + porcelain + rev-parse) at ~1s+ each on-device — multi-second refreshes, the likely reason auto-refresh was never built. VS Code's porcelain carries branch+upstream+ahead/behind+files in ONE call. Restructure to `git status --porcelain=v1 -z -b`. | `scm/GitService.kt:30-43,68-147`; CT07. |
| **SG08** | LOW-MEDIUM, timeline git rows inert + two path bugs | Git-history rows render with no click handling (no diff/revert/jump); relPath falls back to a BASENAME on exception (LS06 family, can show another file's history); and repo detection uses `File(projectDir, ".git").exists()` while GitService supports parent-dir repos via repoRoot — two repo detectors disagree, so a subdirectory-of-repo project shows "No timeline available" though git log works in the terminal. | `ui/panes/TimelinePanel.kt:89-113,155-175`; CT32/CT33. |
| **SG09** | LOW, branch misclassification | `isRemote = name.startsWith("origin/") || name.contains("/")` — any local branch containing a slash (`feature/x`) is labeled remote in the branches dialog. | `scm/GitService.kt:277-305`; CT16. |
| **SG10** | LOW-MEDIUM, publish partial-failure sequencing | PublishDialog's happy path assumes origin is absent: `addRemote("origin", ...)` throws if it exists, aborting the flow AFTER the GitHub repo was created (orphan remote repo, no cleanup/reuse). No pre-check, no set-url fallback, no name-collision handling beyond the raw exception. | `ui/panes/SourceControlPane.kt:1885-1900`; CT12/CT13. |
| **SG11** | LOW, diff parser old-path prefix bug | `oldPath = line.removePrefix("--- ").removePrefix("b/")` — the `--- ` header carries the `a/` prefix, so oldPath keeps a bogus `a/` for every renamed diff. | `scm/GitService.kt:569-648`; CT24. |
| **SG12** | LOW-MEDIUM, trash restore result discarded (EX02's UI manifestation) | The Trash dialog ignores `restoreFromTrash`'s Boolean result, relists unconditionally, ticks refresh — a failed restore produces NO failure message anywhere. EX02 owns the mechanics; this UI-side write-back must join the S01 verification design. | `ui/panes/ExplorerPane.kt:2388-2433`; CT30. |
| **SG13** | LOW, misc GitService correctness | `ScmCommit.isHead` hard-coded false (HistoryDialog's HEAD badge can never render); `pull` uses plain `git pull` with no `--ff-only` — surprise merge commits on diverged branches. | `scm/GitService.kt:333-361,249-260`; CT21. |
| **SG14** | LOW, silent fabricated identity | `ensureIdentity` silently writes `user.name=VN Code User` / `user.email=user@codespace.local` when unset — commits misattributed with no prompt, and the fabrication persists in git config. VS Code surfaces identity errors instead of inventing an author. | `scm/GitService.kt:527-540`; CT35. |
| **SG15** | LOW, unguarded stage/unstage concurrency | onStage/onUnstage/onStageAll launch with no operation pre-check and FileRow's inline +/− icons have no enabled gate — rapid double-taps spawn concurrent gits racing on .git/index.lock (classified as LockFailed, retried by nothing). TB02/LS09 rapid-action race family. | `ui/panes/SourceControlPane.kt:488-515,1119-1198`; CT05/CT14. |
