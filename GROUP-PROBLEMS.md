# Problems / Build / Tasks: fresh source-only group audit

**State:** app and checked-out VS Code clone read; no device test run. `READ A/path:N` is 1-based in `android/app/src/main/java/com/codespace/ide/`, and `READ V/path:N` is 1-based in `vscode-src/src/vs/`. This group owns the diagnostics pipeline end to end: the central `DiagnosticManager` store, all bridges (LSP, lint, problem matchers, gradle), the Advanced Problems panel and its jump chain, explorer problem badges, and the build/task surface (`BuildRunner`, `GradleErrorParser`, `TaskRunner`, `BuildPanel`, `TaskRunnerPanel`). Editor squiggle rendering, LSP server lifecycle, and the Notifications action handling are boundaries. Nothing here is a device outcome or a code-fix authorization.

## Feature inventory

| ID | Source-traced feature |
|---|---|
| PB01 | Central store: `DiagnosticManager` object with a 10-value `DiagnosticSource` enum, a normalized `Diagnostic` model (1-based `DiagnosticRange`, code, codeDescription, sourceName, relatedInformation, tags, quickFixes, documentVersion, isStale), a main-thread-posted `SnapshotStateList`, and a deterministic dedup `computeId` over source/sourceId/uri/range/severity/code/normalized-message. READ A/diagnostics/DiagnosticManager.kt:27-83,289-302. |
| PB02 | Ingest semantics: `publishDiagnostics` REPLACES the source+sourceId+uri group (LSP complete-set pattern) with a within-batch deduped add; `addDiagnostic`/`addDiagnostics` ingest incrementally deduped by id; every mutation is `Handler.post`ed to main so background producers (LSP reader, build thread) are safe. READ A/diagnostics/DiagnosticManager.kt:109-166. |
| PB03 | Clear/own operations: `clearDiagnostics` (per uri), `clearSource` (per sourceId, sets health UNAVAILABLE), `clearFile` (all sources, one uri), `renameFile` (repoints uri+filePath so diagnostics follow Explorer renames), `clearAll`; plus the source health map. READ A/diagnostics/DiagnosticManager.kt:169-230. |
| PB04 | Stale semantics: `markSourceStale`/`markSourceFailed` flag rows via copy(isStale=true) and set health STALE/FAILED; LSP stopServer marks its language's rows stale; `countBySeverity` skips stale rows; the panel dims stale rows to 40% alpha. READ A/diagnostics/DiagnosticManager.kt:185-197,234-246,260-272; A/lsp/LspManager.kt:2293-2300; A/ui/panes/AdvancedProblemsPanel.kt:272-277. |
| PB05 | LSP bridge: the LspManager `publishDiagnostics` handler converts the raw JSONArray via `DiagnosticConverter.fromLsp` (0-based LSP line/col +1 on both ends, severity map, code, relatedInformation, tags unnecessary/deprecated, codeDescription href) and publishes to the central store. READ A/lsp/LspManager.kt:1596-1600; A/diagnostics/DiagnosticConverter.kt:20-90,212-225. |
| PB06 | Lint pipeline: `LintChecker` bracket/quote balance with literal skipping, trailing whitespace, mixed indentation, 200-char lines, TODO/FIXME markers, markdown/plaintext excluded (B1 false-positive fix); `unified` merges `LintAnalyzer` results; published on file open, tab switch, and save from EditorPane via `DiagnosticPublisher.publishLintDiagnostics` (LINTER + STATIC_ANALYZER sources; empty result clears both). READ A/diagnostics/LintChecker.kt:31-114; A/diagnostics/DiagnosticPublisher.kt:26-51; A/ui/panes/EditorPane.kt:723,1713,2092. |
| PB07 | Problem matchers (CW1): `ProblemMatcher` ANSI-strips tool output and matches gcc/clang, javac, kotlinc in-process, tsc/MSBuild, and python traceback (File-line + Exception pairing) formats; dedup + 200-problem cap; gated to build-ish commands by regex; `publishFromCommand` REPLACES the previous BUILD/matcher batch (clean run clears); BUG-A resolves relative tool paths against the command workdir before publishing. READ A/diagnostics/ProblemMatcher.kt:21-196; A/terminal/TerminalAiBridge.kt:45-51. |
| PB08 | Gradle build runner: `isGradleProject`/`validateProject` pre-checks; `runBuild` maps host to guest path, prefers gradlew over system gradle (`--no-daemon --console=plain`), runs via `ProotInstaller.execOnce` with a 600s timeout, infers status by scanning output text for BUILD SUCCESSFUL/FAILED, locates the produced APK with a guest find, and fires NotificationStore start/success/fail events (fail carries a view_logs action). READ A/build/BuildRunner.kt:52-90,108-212. |
| PB09 | Gradle error parser: `extractErrors` captures Task :x FAILED, Kotlin `e: file:(line,col)` (with file:// strip), Java `path:line: error`, the "* What went wrong:" section, and "Build file 'x' line: N" refs; warning variant; `generateSummary`. READ A/build/GradleErrorParser.kt:32-244. |
| PB10 | Build problem publication: `DiagnosticPublisher.publishBuildDiagnostics` groups problems by file and publishes per-file rows under BUILD/gradle (sourceName = task or "gradle"); `clearBuildDiagnostics` on empty parses; wired from BOTH BuildPanel and TaskRunner.run (CW1 fixed the formerly dead publishBuildDiagnostics path). READ A/diagnostics/DiagnosticPublisher.kt:58-68; A/diagnostics/DiagnosticConverter.kt:182-210; A/ui/panes/BuildPanel.kt:89-106; A/project/TaskRunner.kt:82-105. |
| PB11 | Task runner: 8-task catalogue (assembleDebug, assembleRelease, clean, lint, test, installDebug, bundleRelease, assemble) mapped to gradle tasks, a `RunState` StateFlow map with a serial `isAnyRunning` gate, execution delegated to BuildRunner, problems published per run. READ A/project/TaskRunner.kt:17-122. |
| PB12 | Build and task panels: BuildPanel offers a 6-task dropdown, live output collected from the BuildRunner StateFlows, and an environment report; TaskRunnerPanel renders the 8 catalogue tasks as a 2-column grid with state colors and a per-run log pane. READ A/ui/panes/BuildPanel.kt:34-110; A/ui/panels/TaskRunnerPanel.kt:34-130. |
| PB13 | Advanced Problems panel: reads the central SnapshotStateList; severity count chips; filter chips for errors/warnings/info/hints, a source filter, a stale toggle, and a search across message/path/code/sourceName/sourceId; grouping by filePath (sorted) with per-file error/warning/stale counts; row click navigates with 1-based range.startLine/startColumn. READ A/ui/panes/AdvancedProblemsPanel.kt:48-120,202-277. |
| PB14 | Jump-to-source chain: panel callback converts guest paths to host (/host-files prefix, filesDir, IdeTerminalBridge.guestPathToHostFile, LspManager.hostPathFromFileUri fallback), then `onJumpToSourceWithPath` canonicalizes both sides against open tabs (reuses the tab or adds it), sets scrollTargetLine, and hides the bottom panel. READ A/ui/screens/ProjectShellScreen.kt:3537-3553,155-159,4777-4786. |
| PB15 | Line conventions (C3/A-9): DiagnosticManager ranges are 1-based and the panel passes them raw (per the A-9 finding, correct); the `onOpenFileAtLine` family is 0-based with the PSS wrapper owning the single +1 (OSC 7777, terminal taps, go-to-def); the old EditorPane double +1 is documented as fixed at the wrapper. READ A/ui/screens/ProjectShellScreen.kt:3534,3563,4642-4652. |
| PB16 | Explorer problem badges (CW5): per-file red/amber badges computed from the CENTRAL store (`buildProblemCounts`, re-keyed on list change) with a tap that sets the ProblemsPreset, opens the panel, and pre-filters the search to that file; chat attach picker offers the first 50 non-stale diagnostics as context rows (CW2/I4). READ A/ui/panes/ExplorerPane.kt:262-267,1378-1390; A/ui/screens/ProjectShellScreen.kt:1405-1409; A/ui/panes/AdvancedProblemsPanel.kt:71-76; A/ui/screens/ChatAttachPicker.kt:211-216. |

## Cross-group connection ledger

| Edge | Producer -> consumer and seam |
|---|---|
| C-PB01 | LSP group: the LspManager publishDiagnostics handler feeds PB05 (the LSP→store direction of C-LP06); LS06's basename fallback lives on the squiggle side, not the store side. READ A/lsp/LspManager.kt:1596-1600. |
| C-PB02 | Editor E14 squiggles and the central store are fed by the same publish event but through different handlers; the store keys exact URI, the squiggle path has the same-filename fallback. READ A/ui/panes/EditorPane.kt:1520-1571. |
| C-PB03 | Terminal: every run of a build-ish command publishes matcher problems via TerminalAiBridge; the jump chain reuses IdeTerminalBridge.guestPathToHostFile for path conversion. READ A/terminal/TerminalAiBridge.kt:45-51; A/ui/screens/ProjectShellScreen.kt:3544-3551. |
| C-PB04 | Proot: build commands and matcher workdirs live in guest space (execOnce, hostToGuestPath); guest→host conversion happens only at jump time (PB14) and at BUG-A resolve time (PB07). READ A/build/BuildRunner.kt:120-127; A/diagnostics/ProblemMatcher.kt:157-170. |
| C-PB05 | Tabs: onJumpToSourceWithPath reuses or adds tabs via the canonical compare; the B1 onTabsChanged stream keeps shell mirrors live so jumps see current tabs. READ A/ui/screens/ProjectShellScreen.kt:4777-4786. |
| C-PB06 | Notifications: build start/finish/cancel publish NotificationStore events; the view_logs action belongs to the Notifications surface. READ A/build/BuildRunner.kt:126-135,170-186,210-220. |
| C-PB07 | PLAN A per-file canonical store: central-store ranges are NOT shift-corrected between publishes; EditShiftHelper covers squiggles, so store rows can briefly disagree with editor positions — the canonical store must become the single keyed source. READ A/editor/EditShiftHelper.kt:1-76. |
| C-PB08 | Output panel shares the same jump route with the problems panel (CW4, 0-based in, +1 at shell). READ A/ui/screens/ProjectShellScreen.kt:3561-3563. |
| C-PB09 | Chat CW2: the attach picker's problems snapshot reads the store directly (first 50, non-stale); ProblemsPreset search matching is substring-based, so a preset fileName also matches other files with similar names. READ A/ui/screens/ChatAttachPicker.kt:211-216; A/ui/panes/AdvancedProblemsPanel.kt:100-112. |
| C-PB10 | LSP/Proot install path: BuildRunner and the LSP installer share ProotInstaller.execOnce and its P32 profile-source noise suppression; the 2000-line output cap documented in PR03 affects both surfaces. READ A/terminal/ProotInstaller.kt:1356-1436. |

## VS Code clone comparison

| Area | Verified VS Code owner | Android contrast |
|---|---|---|
| Marker store | READ V/platform/markers/common/markerService.ts:150,266. MarkerService keyed by owner+resource; `changeAll(owner, data)` is VS Code's exact analog of publishDiagnostics' replace-per-owner semantics. | READ A/diagnostics/DiagnosticManager.kt:109-137. App keys source+sourceId+uri — equivalent shape, plus per-source health VS Code keeps in extension state instead. |
| Marker model / related info | READ V/workbench/contrib/markers/browser/markersModel.ts:109-129,222. MarkersModel carries relatedInformation and renders it as child nodes in the panel. | READ A/diagnostics/DiagnosticConverter.kt:52-66. App PARSES relatedInformation into rows but renders it nowhere (PR05). |
| Quick fixes from markers | READ V/workbench/contrib/markers/browser/markersView.ts:293-296,844-845. Markers view renders a Quick Fix link per marker backed by code actions. | READ A/diagnostics/DiagnosticManager.kt:57. App's `quickFixes` field exists but is never populated or read anywhere (PR04) — the panel has no fix entry point. |
| Problems panel | READ V/workbench/contrib/markers/browser/markersView.ts (severity filters, grouping) and markersTreeViewer.ts:178,247 (filter data, template rendering). | READ A/ui/panes/AdvancedProblemsPanel.kt:48-120,202-277. App matches the shape: severity chips, source filter, search, file grouping, counts — plus a stale toggle VS Code does not need. |
| Problem matchers | READ V/workbench/contrib/tasks/common/problemMatcher.ts:148,923,1632,1880. Matchers are NAMED, configurable objects parsed from tasks.json and registered in a registry; tasks opt in per matcher. | READ A/diagnostics/ProblemMatcher.kt:21-96. App hardcodes five tool formats behind one build-ish regex gate (PR06) — no configuration surface, no per-task opt-in. |
| Tasks | READ V/workbench/contrib/tasks/common/tasks.ts:274-276,451,527. Tasks are contributed (tasks.json), grouped, and run through a TaskService with terminals, output, and problem binding. | READ A/project/TaskRunner.kt:17-105. App ships a fixed 8-task gradle catalogue — no tasks.json, no custom commands, no task-level problem binding (part of the B14 action-registry gap already in the parity backlog). |
| File decorations | READ V/workbench/contrib/markers/browser/markersFileDecorations.ts — VS Code decorates the EXPLORER with per-file marker counts. | READ A/ui/panes/ExplorerPane.kt:262-267,1378-1390. App implements exactly this via buildProblemCounts from the central store — a genuine parity point; but the separate RUN badge bypasses the store (PR02). |
| Build | VS Code has no built-in build system — tasks + matchers compose into one. | READ A/build/BuildRunner.kt:108-212. App has a dedicated Gradle-only runner with its own status inference and notifications — narrower but on-device appropriate; its cancel/timeout/streaming defects (PR01/PR03) have no VS Code analog to copy. |

## Fresh scratch-only device checks, NONE RUN

Keep a scratch gradle project (small Kotlin app) plus per-language files; note server/build state where relevant; destructive cases are buffer-only but the standard backup rules apply. Every case records PASS/FAIL/NOT RUN with reason.

| Test | Action -> pass criterion and diagnostic |
|---|---|
| PT01 PB05 | Break a line in a scratch .kt with the LSP server READY. Pass: the row appears in the Problems panel with the correct 1-based line, and the squiggle matches. |
| PT02 PB14 | Tap the row from PT01. Pass: lands on the exact line in the SAME tab (no duplicate tab); record the path form shown in the row. |
| PT03 PB05 | Fix the error and save. Pass: the row disappears within the LSP republish window; stale flag not involved. |
| PT04 PB04 | Stop the LSP server (kill from terminal). Pass: rows flip to dimmed stale, counts drop to 0 in the header, and a restart restores them. |
| PT05 PB06 | Open a scratch .md with parentheses and apostrophes. Pass: NO lint problems (B1 exclusion holds) — the file's badge and panel stay clean. |
| PT06 PB06 | Save a .py with an unmatched quote. Pass: lint ERROR row at the right line; then fix, save, row clears on the same save path. |
| PT07 PB07 | In the terminal run `python3 broken.py` (file with a traceback) from the project dir. Pass: BUILD/matcher rows appear with ABSOLUTE paths (BUG-A resolve) and jump works. |
| PT08 PB07 | Run the same command from a DIFFERENT working directory (relative path). Pass: rows still resolve to the correct absolute file, or clearly record the wrong-path behavior. |
| PT09 PB07 | Run a non-build command that prints `error: something at foo:12:1` (e.g. `echo`). Pass: NOTHING published (not build-ish) — the matcher gate holds. |
| PT10 PR06 | Run `node script.js` (a build-ish command) whose output contains lines in tsc/javac format. Record whether false rows appear in the panel (predicted: yes) and whether they survive the next clean build-ish run. |
| PT11 PB02 | Run two different build-ish commands with different failures. Pass: the second run REPLACES the first file's rows (no accumulation across runs), while LSP and lint rows for the same file persist untouched. |
| PT12 PR01 **cancel test** | Start a scratch gradle build (min 60s), press Cancel in the panel. Pass criteria to record: does the underlying execOnce process actually die (watch terminal/process), and does the final status STAY CANCELLED or get overwritten to SUCCESS/FAILED when the coroutine ends? |
| PT13 PR01 | After a real build finishes, tap a task again. Record whether CANCELLED state from PT12 ever blocks a new run or a stale status lingers. |
| PT14 PR03 **output cap test** | Run a scratch gradle build that emits >2000 lines (add a chatty task). Record: is the BUILD SUCCESSFUL/FAILED marker present in the panel output, or truncated? Do late compile errors still publish as rows? |
| PT15 PR03 **streaming test** | During a multi-minute gradle build, watch the Build panel. Record whether ANY output streams before completion (predicted: none — only "Building: task" until the end). |
| PT16 PR05 | Open a Java/Kotlin file with an error that has relatedInformation (e.g. duplicate class). Record whether the panel shows only the parent row (predicted) and where the related locations are visible, if anywhere. |
| PT17 PR04 | With a TS quick-fixable error in the panel, long-press/tap the row. Record whether any quick-fix entry point exists (predicted: none — field never populated). |
| PT18 PR08 | Trigger a gradle build that fails at a task ("Task :app FAILED" with no file). Record: the row's file group (predicted: empty-path group), the displayed line number, and what happens on tap (predicted: nothing sensible). |
| PT19 PR02 | With an LSP error present in the ACTIVE file, compare the RUN side-panel badge count with the Explorer badge and the panel header count. Record the three numbers (predicted: RUN badge counts only lint errors — divergence). |
| PT20 PR02 | Edit the file so an error appears; watch the RUN badge. Record the delay until it updates (poll is 3s) vs the Explorer badge (immediate on store change). |
| PT21 PR11 | During a gradle build, open the TaskRunnerPanel and tap another task. Pass: the serial gate blocks it (isAnyRunning); record the disabled UX. |
| PT22 PB11 | Run TaskRunner CLEAN then BUILD_DEBUG. Pass: state tiles show RUNNING→SUCCESS for both in sequence; problems from BUILD_DEBUG publish. |
| PT23 PB10 | Make a scratch Kotlin compile error in the gradle project and run BUILD_DEBUG from the TaskRunnerPanel. Pass: BUILD/gradle rows appear with file+line; then fix and rerun — rows clear (publish replaces). |
| PT24 PB03 | Rename a file with live LSP errors via the Explorer. Pass: problems rows follow to the new path (renameFile); the Explorer badge moves with the file. |
| PT25 PB16 | Tap a file's red explorer badge. Pass: panel opens pre-filtered to that file's rows (ProblemsPreset), and clearing the search restores the full list. |
| PT26 PR09 | Two files with the same basename in different folders, both with errors. Tap one badge. Record whether the preset filter shows BOTH files' rows (substring search) or only the tapped file. |
| PT27 PB02 | With rows from LSP + lint + gradle + matcher in the panel, use the source filter chip. Pass: only the chosen source's rows remain; counts update. |
| PT28 PB04 | Toggle "show stale" off with stale rows present. Pass: they hide; counts exclude them either way. |
| PT29 PR07 | Open the Problems bottom-tab command menu ("Filter", "Show Errors Only"). Record that both only show notifications (predicted placebos) and the panel does NOT actually change. |
| PT30 PR12 | A file with 300+ lint problems (generated whitespace noise). Record panel scrolling performance, badge rendering, and whether the 200-cap applies (lint has no cap — matcher does). |
| PT31 PR13 | Kill the app mid-build (swipe away) and reopen. Record the state tiles (predicted: stale RUNNING forever — runs map is in-memory) and whether a zombie gradle continues in proot. |
| PT32 PB08 | Gradle project WITHOUT gradlew (system gradle fallback). Record whether the build runs and validateProject's "will use system gradle" warning shows. |
| PT33 PR03 | Run a build long enough to hit the 600s timeout (chatty sleep task). Record the status shown (predicted: FAILED with the "Timed out after 600s" string as output and 0 errors parsed). |
| PT34 PB15 | Terminal tap on a `file.kt:42` link vs a Problems row for the same line. Both must land on line 42 exactly (0-based +1 wrapper vs 1-based raw paths). |
| PT35 PR14 | Leave the panel open with rows, edit the file ABOVE an error (shift lines), without saving. Record whether the panel row's line number goes stale vs the squiggle (predicted: squiggle shifts, row doesn't). |
| PT36 PB06 | Rapid tab switching between two code files. Record any jank from lint-on-switch (main-thread scan) on a large file, and whether lint rows for the previous file linger or clear. |

## Source-confirmed gaps for master fix-plan input

| Gap | Priority below owner's established co-top tier | Bounded source finding |
|---|---|---|
| PR01 | MEDIUM-HIGH, cancel is a no-op + state clobber | `BuildRunner.currentProcess` is declared (line 47) but NEVER assigned in `runBuild` — the build runs via `execOnce`, which keeps the Process private. `cancelBuild()` destroys null and sets CANCELLED (lines 212-220), so the gradle build keeps running in proot; when the execOnce coroutine eventually completes it overwrites status to SUCCESS/FAILED after the CANCELLED state, and a "cancelled" build can end up labeled SUCCESSFUL. `execOnceWithProcess` (P25-3) exists precisely for cancellable runs and is unused here. Same class as the S01 unchecked-outcome pattern. READ A/build/BuildRunner.kt:47,108-160,212-231; A/terminal/ProotInstaller.kt:1438-1443. PT12/PT13. |
| PR02 | MEDIUM, badge source divergence | The RUN side-panel badge (`runBadgeCount`) computes its own count from `LintChecker.check` on disk content in a 3s poll — it ignores LSP, build, and matcher problems entirely and double-lints outside the central store; the Explorer badge correctly reads the store (PB16). Two badges over one file can disagree. READ A/ui/screens/ProjectShellScreen.kt:3172-3184,3206,3287. PT19/PT20. |
| PR03 | MEDIUM, build output truncated + not streamed | `execOnce` caps captured output at MAX_LINES=2000 (reader thread stops adding), so a verbose gradle build can lose its BUILD SUCCESSFUL/FAILED marker and late compile errors: status then falls to the else-FAILED branch with 0 parsed problems, or worse, a marker-truncation mislabel. Additionally `_buildOutput` is written only at start and completion — the Build panel's "live output" shows nothing for the whole multi-minute build. Timeout also returns a plain string the caller cannot distinguish from real output. READ A/terminal/ProotInstaller.kt:1396-1425; A/build/BuildRunner.kt:108-160. PT14/PT15/PT33. |
| PR04 | MEDIUM, quick-fix dead end | `Diagnostic.quickFixes` is never populated by any producer (LSP bridge builds rows without it, lint/build converters never set it) and never rendered — grep shows zero usage outside the model. VS Code's markers view offers a Quick Fix entry per row backed by code actions; ours is a dead field. READ A/diagnostics/DiagnosticManager.kt:57; A/diagnostics/DiagnosticConverter.kt:20-90. PT17. |
| PR05 | MEDIUM, relatedInformation invisible | `fromLsp` parses relatedInformation into every row (related messages with their own uri+line), but no UI consumes it — the panel, squiggles, and popups render nothing for related locations. VS Code renders them as child nodes. Misleading for duplicate-class/unused-import chains where the related location is the actionable one. READ A/diagnostics/DiagnosticConverter.kt:52-66. PT16. |
| PR06 | LOW-MEDIUM, matcher ingestion breadth | The build-ish regex (gradlew?|make|gcc|...|python3?|node|npm|npx|yarn|tsc|cargo|dotnet|cmake) turns ANY matching run command into an ingestion candidate and then applies compiler formats to arbitrary output — a node script printing `path(12,5): error X: msg` publishes false BUILD rows. VS Code matchers are per-task opt-in; ours is global and only partially suppressed by the format regexes. READ A/diagnostics/ProblemMatcher.kt:33-36,47-50,159-170. PT09/PT10. |
| PR07 | LOW-MEDIUM, placebo menu commands | The Problems bottom-tab commands "Filter" and "Show Errors Only" only fire notifications ("tap a problem to jump to source", "Error-only filter toggled") and change NOTHING — the toggle claim is a false report in the same family as SR03's success-without-verification. READ A/ui/screens/ProjectShellScreen.kt:5039-5041. PT29. |
| PR08 | LOW-MEDIUM, empty-file build rows | GradleErrorParser emits task-FAILED and "What went wrong" rows with `file=""` line 0; fromBuildProblems publishes them with uri "file://" and a 0-based-looking range (line 0), grouped under an empty path, and a tap sends "" into the jump chain (canonicalPathOrSelf("") → no tab match → new tab with an empty path) — same failure family as the problems-jump BUG-A. READ A/build/GradleErrorParser.kt:41-52,102-116; A/diagnostics/DiagnosticConverter.kt:182-210; A/ui/screens/ProjectShellScreen.kt:4777-4786. PT18. |
| PR09 | LOW-MEDIUM, preset filter is substring | ProblemsPreset (explorer badge) drops the bare FILENAME into the search box, which substring-matches message, path, code, sourceName and sourceId — a badge tap for `Main.kt` also shows `MainViewModel.kt` rows and any message containing "main". READ A/ui/panes/AdvancedProblemsPanel.kt:71-76,100-112; A/ui/screens/ProjectShellScreen.kt:1405-1409. PT26. |
| PR10 | LOW, dead panel file | `ui/panes/ProblemsPanel.kt` (the simple per-file panel, 399 lines) has no callers anywhere — superseded by AdvancedProblemsPanel; dead code that will drift. READ A/ui/panes/ProblemsPanel.kt:46. |
| PR11 | LOW, lint cost on switch/open | publishLintDiagnostics runs the full LintChecker+LintAnalyzer scan synchronously on the caller's thread from tab switch/open/save paths (LaunchedEffect bodies); for large files this is main-thread string scanning on every switch, plus the 3s badge poll re-reads the file from disk — three linting passes exist over the same content (open, save, badge). READ A/ui/panes/EditorPane.kt:723,1713,2092; A/ui/screens/ProjectShellScreen.kt:3172-3184. PT36. |
| PR12 | LOW, task catalogue drift | BuildPanel's dropdown offers 6 gradle tasks; TaskRunnerPanel offers the 8-task catalogue including installDebug/bundleRelease/assemble — two overlapping hardcoded lists that have already drifted; no tasks.json-style single source. READ A/ui/panes/BuildPanel.kt:72-79; A/project/TaskRunner.kt:50-58. |
| PR13 | LOW, run state not durable | TaskRunner's runs map and BuildRunner's status are in-memory StateFlows — an app kill mid-build leaves a RUNNING tile forever on relaunch while a zombie gradle may still hold proot; no persistence or startup reconciliation. READ A/project/TaskRunner.kt:63-66; A/build/BuildRunner.kt:29-50. PT31. |
| PR14 | LOW, central rows unshifted between publishes | Squiggle ranges are shift-corrected through EditShiftHelper while typing, but the central store keeps the last-published ranges until the next LSP republish or lint pass — panel line numbers go stale relative to the editor mid-edit. PLAN A's canonical store should own both. READ A/editor/EditShiftHelper.kt:1-76; A/diagnostics/DiagnosticManager.kt:42-60. PT35. |

**Boundary:** Editor owns squiggle rendering, LSP owns server lifecycle and republish cadence, Notifications owns action handling, Chat owns attach UX, Tabs owns tab reuse. The strongest rows are PR01 (cancel no-op with status clobber — an S01-family unchecked operation), PR03 (output cap can mislabel build status on verbose builds), and PR04/PR05 (model fields that promise parity the UI never delivers). All remain below the owner's co-top tier G01/TB01/TB03/EX04/EX05/EX07. Do not mark any PT case passed or implement a fix based on this audit alone.
